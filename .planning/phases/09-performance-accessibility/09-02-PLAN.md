---
plan: 09-02
title: "Bundle analysis + use client audit + Suspense"
phase: 9
wave: 1
depends_on: []
files_modified:
  - buildera-frontend/src/components/sections/TechStackSection.tsx
  - buildera-frontend/src/app/blog/[slug]/page.tsx
  - buildera-frontend/src/app/case-studies/[slug]/page.tsx
  - buildera-frontend/src/app/guides/[slug]/page.tsx
autonomous: true
requirements_addressed:
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - "Every \"use client\" file has been classified against the allowed list with a documented reason"
    - "No section component marked \"use client\" purely for CSS animation remains a client component"
    - "ISR detail pages (blog/case-study/guide) wrap async content sections in Suspense with a stable-height fallback"
    - "next build succeeds and the bundle composition is recorded as a baseline"
  goal_backward:
    - "PERF-04/PERF-05 require minimal client JS — every unjustified \"use client\" adds bundle weight and hurts LCP"
    - "Suspense boundaries on ISR pages prevent layout shift (CLS) during streaming"
---

<objective>
Audit the 36 `"use client"` files against the allowed list (forms, tab switcher, popups/widgets, analytics, and components that use `motion.X` elements) and add Suspense boundaries to ISR detail pages.

Research finding: most REVIEW-flagged sections (CTASection, HeroSection, WhyBuilderaSection, SolutionsGridSection, CaseStudiesPreview, TestimonialsSection) genuinely render `motion.X` elements and MUST stay client. `TechStackSection.tsx` uses only `useState` (a tab/filter switcher — allowed). The actionable work is: (1) document each classification, (2) confirm no section is client purely for CSS animation, (3) add Suspense to the three ISR pages, (4) capture a build baseline.

Purpose: Keep client JS minimal and eliminate streaming layout shift.
Output: Documented audit table in SUMMARY, Suspense boundaries on ISR pages, build baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-performance-accessibility/09-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task id="09-02-T1">
<title>Classify all "use client" components and convert any unjustified ones</title>
<read_first>
- buildera-frontend/src/components/sections/TechStackSection.tsx (uses useState only — tab/filter switcher)
- buildera-frontend/src/components/sections/CTASection.tsx (uses motion.section/motion.p — keep)
- buildera-frontend/src/components/sections/HeroSection.tsx (uses motion.* — keep)
- buildera-frontend/src/components/sections/WhyBuilderaSection.tsx (uses motion.* — keep)
- buildera-frontend/src/components/sections/SolutionsGridSection.tsx (uses motion.* — keep)
- buildera-frontend/src/components/sections/CaseStudiesPreview.tsx (uses motion.* — keep)
- buildera-frontend/src/components/sections/TestimonialsSection.tsx (uses motion.* + Image — keep)
</read_first>
<action>
Run `grep -rln '"use client"' buildera-frontend/src --include="*.tsx"` to get the full list. For each file, classify against the allowed list: forms, tab switcher, popups/widgets, analytics, Next.js error boundary, and any component that renders a `motion.X` element or calls a motion hook (`useInView`, `useScroll`, `useTransform`, `AnimatePresence`, `whileInView`). A file is JUSTIFIED if it matches any allowed category. A file is UNJUSTIFIED only if it has `"use client"` but uses NO React state/effect/event handlers, NO motion.X elements, and NO motion hooks. For UNJUSTIFIED files only, remove the `"use client"` directive and confirm it still builds as a Server Component. Per research, expect zero conversions among the REVIEW sections (they all use motion.X); TechStackSection stays client (useState filter). Record the full classification table (file → category → keep/convert) in the SUMMARY. Do NOT remove `"use client"` from any file that renders `motion.` elements — motion v12 createMotionComponent is client-only and breaks static generation otherwise (per STATE.md locked decision).
</action>
<acceptance_criteria>
- SUMMARY contains a classification table covering every file returned by `grep -rln '"use client"' buildera-frontend/src --include="*.tsx"`
- Every REVIEW section that contains `motion.` still has `"use client"` (verify: `grep -L '"use client"' buildera-frontend/src/components/sections/{CTASection,HeroSection,WhyBuilderaSection,SolutionsGridSection,CaseStudiesPreview,TestimonialsSection}.tsx` returns nothing)
- Any file converted to Server Component contains no `useState`/`useEffect`/`motion.`/event handlers
- `cd buildera-frontend && npm run build` exits 0
</acceptance_criteria>
</task>

<task id="09-02-T2">
<title>Add Suspense boundaries to ISR detail pages</title>
<read_first>
- buildera-frontend/src/app/blog/[slug]/page.tsx (ISR page being modified)
- buildera-frontend/src/app/case-studies/[slug]/page.tsx (ISR page being modified)
- buildera-frontend/src/app/guides/[slug]/page.tsx (ISR page being modified)
</read_first>
<action>
In each of the three ISR detail pages, wrap the async body content (the portion that awaits the per-slug fetch and renders related posts / below-hero content) in a React `<Suspense>` boundary with a fixed-height skeleton fallback so streaming does not shift layout. Import `Suspense` from `react`. The fallback must reserve height matching the streamed content (e.g. `<div className="animate-pulse h-64 bg-muted rounded" />` or taller) to keep CLS < 0.1. Do NOT wrap the above-fold hero in Suspense — the hero with `priority` image should render immediately as the LCP element. Keep the existing `generateStaticParams` and `generateMetadata` untouched.
</action>
<acceptance_criteria>
- `grep -n "Suspense" buildera-frontend/src/app/blog/[slug]/page.tsx` matches
- `grep -n "Suspense" buildera-frontend/src/app/case-studies/[slug]/page.tsx` matches
- `grep -n "Suspense" buildera-frontend/src/app/guides/[slug]/page.tsx` matches
- Each file imports Suspense from "react"
- `cd buildera-frontend && npm run build` exits 0; generateStaticParams still present in all three
</acceptance_criteria>
</task>

<task id="09-02-T3">
<title>Capture bundle baseline</title>
<read_first>
- buildera-frontend/next.config.ts (confirm optimizePackageImports already includes motion from 09-01; if running before 09-01 lands, note motion may be absent)
</read_first>
<action>
Run `next build` from `buildera-frontend` and record the route-by-route First Load JS sizes from the build output table into the SUMMARY as a baseline. This baseline lets 09-05 confirm bundle size did not regress. No code changes in this task — it is a measurement step. If `@next/bundle-analyzer` is desired, it may be added as a devDependency only (never a runtime dep) and run via `ANALYZE=true next build`, but this is optional; the build output table is sufficient as the baseline.
</action>
<acceptance_criteria>
- SUMMARY contains a table of First Load JS per route from the build output
- `cd buildera-frontend && npm run build` exits 0
- No new runtime (non-dev) dependency added to package.json
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd buildera-frontend
grep -rln '"use client"' src --include="*.tsx" | wc -l
# Cross-check count against SUMMARY classification table
grep -L '"use client"' src/components/sections/CTASection.tsx src/components/sections/HeroSection.tsx src/components/sections/WhyBuilderaSection.tsx src/components/sections/SolutionsGridSection.tsx src/components/sections/CaseStudiesPreview.tsx src/components/sections/TestimonialsSection.tsx
# Expect no output (all still client)
for f in src/app/blog/[slug]/page.tsx src/app/case-studies/[slug]/page.tsx src/app/guides/[slug]/page.tsx; do grep -q "Suspense" "$f" && echo "OK $f" || echo "MISSING Suspense: $f"; done
npm run build
```
</verification>

<success_criteria>
- Every "use client" file classified with documented reason
- No section is client purely for CSS animation
- Suspense boundaries on all three ISR detail pages with stable-height fallbacks
- Build baseline recorded; build passes
</success_criteria>

<output>
Create `.planning/phases/09-performance-accessibility/09-02-SUMMARY.md` when done
</output>
