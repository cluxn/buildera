---
plan: 09-04
title: "Contact page enhancements + search-enabled 404 page"
phase: 9
wave: 2
depends_on:
  - 09-03
files_modified:
  - buildera-frontend/src/components/sections/contact/ContactInfo.tsx
  - buildera-frontend/src/app/contact/page.tsx
  - buildera-frontend/src/app/not-found.tsx
  - buildera-frontend/src/components/layout/NotFoundSearch.tsx
autonomous: true
requirements_addressed:
  - UX-01
  - UX-02
must_haves:
  truths:
    - "ContactInfo displays a clickable phone number (tel: link) prominently"
    - "Contact page shows a timezone indicator (IST UTC+5:30, business hours)"
    - "Response-time copy is consistent between page body and metadata"
    - "not-found.tsx renders a search input wired to /api/search plus suggested popular pages"
    - "404 search is a \"use client\" child component; not-found.tsx itself stays a Server Component"
  goal_backward:
    - "UX-01 requires response guarantee + map + direct phone + timezone — phone and timezone are missing per research"
    - "UX-02 requires the 404 page to rescue visitors via search wired to the existing /api/search endpoint"
---

<objective>
Complete UX-01 (contact page) and UX-02 (404 page). Research verified the contact page already has the response guarantee, map, and WhatsApp, but is missing a prominent direct phone number and a timezone indicator, and has a response-time inconsistency (body says "4 business hours", metadata says "24 hours"). The 404 page is a 3-link stub and needs a search input + suggested pages.

Depends on 09-03 so the new ContactInfo phone link, the 404 search input, and suggested-page links inherit the focus-visible ring, 48px touch targets, and accessible labelling patterns established there.

Purpose: Close the two UX requirements that convert dead-ends and contact intent into booked calls.
Output: ContactInfo with phone + timezone, reconciled response copy, rebuilt not-found.tsx with NotFoundSearch client component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-performance-accessibility/09-RESEARCH.md
@.planning/phases/09-performance-accessibility/09-03-SUMMARY.md
@CLAUDE.md
@buildera-frontend/src/app/contact/page.tsx
@buildera-frontend/src/components/sections/contact/ContactInfo.tsx
@buildera-frontend/src/app/not-found.tsx
</context>

<tasks>

<task id="09-04-T1">
<title>Add phone number + timezone to contact page; reconcile response copy</title>
<read_first>
- buildera-frontend/src/components/sections/contact/ContactInfo.tsx (currently renders Email, WhatsApp, Office — no phone, no timezone)
- buildera-frontend/src/app/contact/page.tsx (metadata says "within 24 hours"; body card says "4 business hours")
- buildera-frontend/src/lib/api.ts (confirm the settings shape — company_phone field name)
</read_first>
<action>
In `ContactInfo.tsx`: add a `phone?: string` prop and render a prominent "Call Us" block (mirroring the existing Email/WhatsApp card style with `IconPhone` from @tabler/icons-react) as a `tel:` anchor using the passed phone, falling back to a sensible display only if provided. Apply the 48px touch-target and focus-visible patterns from 09-03 to the new link. Add a timezone indicator line near the Office block: "We're in IST (UTC+5:30) — Mon–Fri 9am–6pm". In `contact/page.tsx`: pass `settings.company_phone` into `<ContactInfo phone={...} />`, and reconcile the response-time wording — make the `generateMetadata` description and the Response Guarantee card agree (use "within 4 business hours" in both, since the guarantee card is the customer promise). Do not alter the LocalBusiness JSON-LD telephone field logic beyond ensuring it still reads settings.company_phone.
</action>
<acceptance_criteria>
- `grep -n "tel:" buildera-frontend/src/components/sections/contact/ContactInfo.tsx` matches
- `grep -niE "IST|UTC\+5:30|time ?zone" buildera-frontend/src/components/sections/contact/ContactInfo.tsx` matches
- `grep -n "phone" buildera-frontend/src/app/contact/page.tsx` shows ContactInfo receiving a phone prop
- Response-time wording matches between generateMetadata description and the Response Guarantee card (no "24 hours" vs "4 business hours" mismatch)
- `cd buildera-frontend && npm run build` exits 0
</acceptance_criteria>
</task>

<task id="09-04-T2">
<title>Build NotFoundSearch client component</title>
<read_first>
- buildera-frontend/src/lib/api.ts (search helper / fetch pattern; confirm GET /api/search?q= contract and any typed result shape)
- buildera-frontend/src/app/not-found.tsx (current stub — for matching layout classes)
</read_first>
<action>
Create `buildera-frontend/src/components/layout/NotFoundSearch.tsx` as a `"use client"` component. It renders a labelled search input (with an accessible `aria-label` and a 48px-height submit control) and, on submit, navigates to `/search?q={encoded query}` via `useRouter` from `next/navigation` — reusing the existing `/search` results page rather than duplicating result rendering. Do NOT fetch search results at render time (per research pitfall: not-found.tsx has no route context). The input must trim empty queries and not navigate on blank submit. Use the project's brand button/input styling and the focus-visible ring from 09-03. Keep it self-contained (no props required).
</action>
<acceptance_criteria>
- File `buildera-frontend/src/components/layout/NotFoundSearch.tsx` exists and starts with `"use client"`
- `grep -n "useRouter" buildera-frontend/src/components/layout/NotFoundSearch.tsx` matches
- `grep -n "/search?q=" buildera-frontend/src/components/layout/NotFoundSearch.tsx` matches (or constructs that URL)
- Empty/whitespace query does not navigate (guard present)
- Search input has an aria-label and the submit control is min 48px tall
</acceptance_criteria>
</task>

<task id="09-04-T3">
<title>Rebuild not-found.tsx with search + suggested pages</title>
<read_first>
- buildera-frontend/src/app/not-found.tsx (3-link stub being replaced)
- buildera-frontend/src/components/layout/NotFoundSearch.tsx (created in T2)
</read_first>
<action>
Replace the body of `not-found.tsx` (keeping it a Server Component — no `"use client"` at page level) so it: renders the friendly 404 heading/message, imports and renders `<NotFoundSearch />` for the interactive search, and shows a "Popular pages" section with a hardcoded list of `<Link>`s (Homepage `/`, Services `/services`, Solutions `/solutions`, Blog `/blog`, Contact `/contact`). Use the site's layout container and spacing classes so it matches the rest of the site. Suggested-page links must meet the 48px touch target and focus-visible patterns from 09-03.
</action>
<acceptance_criteria>
- `grep -n "NotFoundSearch" buildera-frontend/src/app/not-found.tsx` matches
- `not-found.tsx` does NOT contain `"use client"` (page stays a Server Component): `grep -c '"use client"' buildera-frontend/src/app/not-found.tsx` returns 0
- Suggested-page links include hrefs `/`, `/services`, `/solutions`, `/blog`, `/contact` (verify: `grep -oE 'href="/(services|solutions|blog|contact)?"' buildera-frontend/src/app/not-found.tsx`)
- `cd buildera-frontend && npm run build` exits 0
- Navigating to a nonexistent path renders the search input and suggested pages
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd buildera-frontend
grep -n "tel:" src/components/sections/contact/ContactInfo.tsx
grep -niE "IST|UTC\+5:30" src/components/sections/contact/ContactInfo.tsx
test -f src/components/layout/NotFoundSearch.tsx && head -1 src/components/layout/NotFoundSearch.tsx
grep -c '"use client"' src/app/not-found.tsx   # expect 0
grep -n "NotFoundSearch" src/app/not-found.tsx
npm run build
```
Manual: load /contact (phone link + timezone visible, response copy consistent); visit /this-page-does-not-exist (search box + popular pages render; submitting a query routes to /search?q=).
</verification>

<success_criteria>
- Contact page shows clickable phone + timezone; response-time copy consistent
- not-found.tsx is a Server Component rendering NotFoundSearch + suggested pages
- 404 search routes to /search?q= and ignores blank queries
- Build passes
</success_criteria>

<output>
Create `.planning/phases/09-performance-accessibility/09-04-SUMMARY.md` when done
</output>
