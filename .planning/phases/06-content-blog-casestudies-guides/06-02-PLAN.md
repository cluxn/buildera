---
plan: "06-02"
wave: 1
phase: 6
depends_on: []
files_modified:
  - buildera-backend/database/migrations/2026_05_29_000003_add_key_metrics_to_case_studies_table.php
  - buildera-backend/app/Models/CaseStudy.php
  - buildera-backend/app/Models/Guide.php
  - buildera-backend/app/Models/Testimonial.php
  - buildera-backend/app/Filament/Resources/CaseStudyResource.php
  - buildera-backend/app/Filament/Resources/CaseStudyResource/Pages/ListCaseStudies.php
  - buildera-backend/app/Filament/Resources/CaseStudyResource/Pages/CreateCaseStudy.php
  - buildera-backend/app/Filament/Resources/CaseStudyResource/Pages/EditCaseStudy.php
  - buildera-backend/app/Filament/Resources/GuideResource.php
  - buildera-backend/app/Filament/Resources/GuideResource/Pages/ListGuides.php
  - buildera-backend/app/Filament/Resources/GuideResource/Pages/CreateGuide.php
  - buildera-backend/app/Filament/Resources/GuideResource/Pages/EditGuide.php
  - buildera-backend/app/Filament/Resources/TestimonialResource.php
  - buildera-backend/app/Filament/Resources/TestimonialResource/Pages/ListTestimonials.php
  - buildera-backend/app/Filament/Resources/TestimonialResource/Pages/CreateTestimonial.php
  - buildera-backend/app/Filament/Resources/TestimonialResource/Pages/EditTestimonial.php
  - buildera-backend/app/Observers/ContentObserver.php
  - buildera-backend/app/Providers/AppServiceProvider.php
  - buildera-backend/app/Http/Controllers/Api/CaseStudyController.php
  - buildera-backend/app/Http/Controllers/Api/GuideController.php
  - buildera-backend/app/Http/Controllers/Api/TestimonialController.php
requirements_addressed:
  - ADM-05
  - ADM-06
  - ADM-07
  - ADM-09
  - ADM-17
  - CONT-07
autonomous: true

must_haves:
  truths:
    - "CaseStudyResource, GuideResource, TestimonialResource all use Schema::make() and $navigationGroup = 'Content'"
    - "CaseStudy has key_metrics JSON column; Filament renders it as repeatable label+value fields"
    - "TestimonialResource has service_category and industry fields for tagging"
    - "GET /api/case-studies, /api/guides, /api/testimonials all enforce scopePublished()"
    - "ContentObserver registers CaseStudy, Guide, Testimonial with correct cache tags"
  artifacts:
    - path: "buildera-backend/app/Filament/Resources/CaseStudyResource.php"
      provides: "Full CRUD with hero image, problem/solution/results RichEditor, key metrics repeater"
    - path: "buildera-backend/app/Filament/Resources/GuideResource.php"
      provides: "Guide CRUD with cover image, body, resource type, published_at"
    - path: "buildera-backend/app/Filament/Resources/TestimonialResource.php"
      provides: "Testimonial CRUD with service_category + industry tagging, is_featured toggle"
  key_links:
    - from: "CaseStudyResource"
      to: "ContentObserver"
      via: "CaseStudy::observe registers for tag 'case_studies'"
    - from: "TestimonialController"
      to: "Testimonial scopePublished"
      via: "All queries chain scopePublished()"
---

<objective>
Create Filament resources for CaseStudy, Guide, and Testimonial — completing the Content nav group. Add key_metrics JSON column to case_studies table. Wire all three models into ContentObserver. Upgrade CaseStudy, Guide, and Testimonial API controllers to return structured responses consumed by Phase 6 frontend pages.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@CLAUDE.md

<existing_patterns>
- Read `app/Filament/Resources/BlogPostResource.php` (just created in 06-01) — replicate exact tab/field structure
- Read `app/Models/BlogPost.php` — replicate HasSlug, scopePublished, $casts patterns
- CaseStudy, Guide, Testimonial models already exist in migrations from Phase 1 — check columns before adding fields
- ContentObserver already updated in 06-01 for BlogPost — extend it further here
- All Filament resources: Schema::make() not Form::make(); $navigationGroup = 'Content'
</existing_patterns>
</context>

<tasks>

<task id="06-02-01" type="execute">
  <title>Migration — add key_metrics JSON to case_studies table</title>
  <read_first>
    - buildera-backend/database/migrations/ (find the original case_studies migration to confirm column list)
    - buildera-backend/app/Models/CaseStudy.php (existing model)
  </read_first>
  <action>
    Create `database/migrations/2026_05_29_000003_add_key_metrics_to_case_studies_table.php`:
    - `Schema::table('case_studies', function (Blueprint $table) { $table->json('key_metrics')->nullable()->after('results'); })`

    Run `php artisan migrate`.

    Update `app/Models/CaseStudy.php`:
    - Add `'key_metrics'` to $fillable
    - Add `'key_metrics' => 'array'` to $casts
    - Ensure `scopePublished()` exists: `where('is_published', true)->where(fn($q) => $q->whereNull('published_at')->orWhere('published_at', '<=', now()))`
    - Add HasSlug trait with `generateSlugsFrom('title')->saveSlugsTo('slug')->doNotGenerateSlugsOnUpdate()`
    - Verify $fillable includes: title, slug, client_name, industry, hero_image, hero_image_alt, problem, solution, results, key_metrics, testimonial_quote, testimonial_author, is_featured, is_published, published_at, seo_title, seo_description
  </action>
  <acceptance_criteria>
    - `php artisan migrate` exits 0
    - `DESCRIBE case_studies` shows `key_metrics` as JSON/text nullable column
    - CaseStudy model $casts includes `'key_metrics' => 'array'`
  </acceptance_criteria>
</task>

<task id="06-02-02" type="execute">
  <title>CaseStudyResource — Filament CRUD with repeater key metrics</title>
  <read_first>
    - buildera-backend/app/Filament/Resources/BlogPostResource.php (tab structure to replicate)
    - buildera-backend/app/Models/CaseStudy.php (just updated — use $fillable as field guide)
  </read_first>
  <action>
    Create `app/Filament/Resources/CaseStudyResource.php`:

    **Navigation:**
    - `$navigationGroup = 'Content'`, `$navigationIcon = 'heroicon-o-briefcase'`, `$navigationSort = 2`

    **Schema tabs:**

    Tab "Details":
    - `TextInput::make('title')->required()->maxLength(200)->live()->afterStateUpdated(fn($s,$set) => $set('slug', Str::slug($s)))`
    - `TextInput::make('slug')->required()->unique(ignoreRecord: true)`
    - `TextInput::make('client_name')->nullable()->maxLength(150)->placeholder('Anonymous Client (optional)')`
    - `Select::make('industry')->options(['Manufacturing' => 'Manufacturing', 'Retail / E-Commerce' => 'Retail / E-Commerce', 'Hospitality' => 'Hospitality', 'Logistics' => 'Logistics', 'Finance' => 'Finance', 'Healthcare' => 'Healthcare', 'Real Estate' => 'Real Estate', 'Professional Services' => 'Professional Services'])->required()`
    - `FileUpload::make('hero_image')->image()->acceptedFileTypes(['image/*'])->disk('public')->directory('case-studies')`
    - `TextInput::make('hero_image_alt')->maxLength(200)->label('Image Alt Text')`

    Tab "Content":
    - `RichEditor::make('problem')->required()->label('Problem Statement')->columnSpanFull()`
    - `RichEditor::make('solution')->required()->label('Our Solution')->columnSpanFull()`
    - `RichEditor::make('results')->required()->label('Results & Outcomes')->columnSpanFull()`
    - `Repeater::make('key_metrics')->schema([TextInput::make('label')->required()->maxLength(60), TextInput::make('value')->required()->maxLength(60)])->label('Key Result Metrics (e.g. "40% faster processing")')->addActionLabel('Add Metric')->columnSpanFull()`
    - `TextInput::make('testimonial_quote')->nullable()->maxLength(500)->label('Client Quote (optional)')`
    - `TextInput::make('testimonial_author')->nullable()->maxLength(100)->label('Quote Attribution')`

    Tab "Publishing":
    - `Toggle::make('is_published')`, `Toggle::make('is_featured')`, `DateTimePicker::make('published_at')->nullable()`

    Tab "SEO":
    - `TextInput::make('seo_title')->maxLength(70)`, `Textarea::make('seo_description')->maxLength(160)->rows(3)`

    **Table:** title, industry, is_featured, is_published toggle, published_at
    **Create Pages:** ListCaseStudies, CreateCaseStudy, EditCaseStudy
  </action>
  <acceptance_criteria>
    - CaseStudyResource.php contains `Schema::make(`
    - CaseStudyResource.php contains `Repeater::make('key_metrics')`
    - /admin/case-studies loads without PHP error
    - Repeater allows adding label+value metric pairs
  </acceptance_criteria>
</task>

<task id="06-02-03" type="execute">
  <title>Guide model update + GuideResource Filament CRUD</title>
  <read_first>
    - buildera-backend/database/migrations/ (find guides migration — check columns: title, slug, category, description, body, cover_image, resource_type, external_link, published_at, is_published, seo_*)
    - buildera-backend/app/Models/Guide.php (existing model)
    - buildera-backend/app/Filament/Resources/BlogPostResource.php (tab pattern)
  </read_first>
  <action>
    **1. Update `app/Models/Guide.php`:**
    - Add HasSlug trait: `generateSlugsFrom('title')->saveSlugsTo('slug')->doNotGenerateSlugsOnUpdate()`
    - $fillable: title, slug, category, description, body, cover_image, cover_image_alt, resource_type, external_link, published_at, is_published, seo_title, seo_description
    - $casts: published_at → datetime, is_published → boolean
    - Add scopePublished() if missing

    **2. Create `app/Filament/Resources/GuideResource.php`:**
    - `$navigationGroup = 'Content'`, `$navigationIcon = 'heroicon-o-book-open'`, `$navigationSort = 3`

    Tab "Content":
    - `TextInput::make('title')->required()->live()->afterStateUpdated(fn($s,$set) => $set('slug', Str::slug($s)))`
    - `TextInput::make('slug')->required()->unique(ignoreRecord: true)`
    - `Select::make('category')->options(['Software Development' => 'Software Dev', 'Salesforce' => 'Salesforce', 'DevOps' => 'DevOps', 'AI & Automation' => 'AI & Automation', 'Business Operations' => 'Business Operations', 'Hiring Tech Teams' => 'Hiring Tech Teams'])->required()`
    - `Select::make('resource_type')->options(['article' => 'Article', 'template' => 'Template', 'checklist' => 'Checklist', 'video' => 'Video'])->required()`
    - `Textarea::make('description')->required()->maxLength(300)->rows(3)`
    - `RichEditor::make('body')->required()->columnSpanFull()`

    Tab "Media":
    - `FileUpload::make('cover_image')->image()->acceptedFileTypes(['image/*'])->disk('public')->directory('guides')`
    - `TextInput::make('cover_image_alt')->maxLength(200)`
    - `TextInput::make('external_link')->url()->nullable()->label('External Link (optional)')`

    Tab "Publishing" + Tab "SEO" (same as BlogPostResource)

    **Create Pages:** ListGuides, CreateGuide, EditGuide
  </action>
  <acceptance_criteria>
    - GuideResource.php contains `Schema::make(`
    - Guide model has HasSlug, scopePublished, $casts
    - /admin/guides loads without PHP error
    - resource_type Select has 4 options: article, template, checklist, video
  </acceptance_criteria>
</task>

<task id="06-02-04" type="execute">
  <title>TestimonialResource — service_category + industry tagging, is_featured toggle</title>
  <read_first>
    - buildera-backend/database/migrations/ (find testimonials migration — check columns: quote, client_name, job_title, company, star_rating, service_category, is_featured, display_order, is_published)
    - buildera-backend/app/Models/Testimonial.php (existing model)
    - buildera-backend/app/Filament/Resources/LeadResource.php (table action patterns)
  </read_first>
  <action>
    **1. Update `app/Models/Testimonial.php`:**
    - $fillable: quote, client_name, job_title, company, company_logo, company_logo_alt, star_rating, service_category, industry, is_featured, display_order, is_published
    - $casts: is_featured → boolean, is_published → boolean, star_rating → integer
    - Add scopePublished() if missing
    - Add scopeForService($service): `->where('service_category', $service)` static scope

    **2. Create `app/Filament/Resources/TestimonialResource.php`:**
    - `$navigationGroup = 'Content'`, `$navigationIcon = 'heroicon-o-chat-bubble-left-right'`, `$navigationSort = 6`

    Schema fields (single tab — simpler resource):
    - `Textarea::make('quote')->required()->maxLength(500)->rows(4)->columnSpanFull()`
    - `TextInput::make('client_name')->required()->maxLength(100)`
    - `TextInput::make('job_title')->nullable()->maxLength(100)`
    - `TextInput::make('company')->nullable()->maxLength(100)`
    - `FileUpload::make('company_logo')->image()->disk('public')->directory('testimonials')->nullable()`
    - `TextInput::make('company_logo_alt')->maxLength(200)->nullable()`
    - `Select::make('star_rating')->options([5=>'5 Stars',4=>'4 Stars',3=>'3 Stars'])->default(5)->required()`
    - `Select::make('service_category')->options(['Website Development'=>'Website Dev','Salesforce Development'=>'Salesforce Dev','DevOps Development'=>'DevOps','AI Agent Development'=>'AI Agents','Software Development'=>'Software Dev','Hire a Developer'=>'Hire a Dev'])->nullable()`
    - `Select::make('industry')->options(['Manufacturing'=>'Manufacturing','Retail / E-Commerce'=>'Retail','Hospitality'=>'Hospitality','Logistics'=>'Logistics','Finance'=>'Finance','Healthcare'=>'Healthcare','Real Estate'=>'Real Estate','Professional Services'=>'Professional Services'])->nullable()`
    - `TextInput::make('display_order')->numeric()->default(0)`
    - `Toggle::make('is_featured')`, `Toggle::make('is_published')`

    **Table:** quote (truncated), client_name, service_category, industry, star_rating, is_featured toggle, is_published toggle, display_order
    **Create Pages:** ListTestimonials, CreateTestimonial, EditTestimonial
  </action>
  <acceptance_criteria>
    - TestimonialResource.php contains `Schema::make(`
    - TestimonialResource has service_category and industry Select fields
    - /admin/testimonials loads without PHP error
    - Testimonial model $fillable includes service_category and industry
  </acceptance_criteria>
</task>

<task id="06-02-05" type="execute">
  <title>Register CaseStudy, Guide, Testimonial in ContentObserver + upgrade API controllers</title>
  <read_first>
    - buildera-backend/app/Observers/ContentObserver.php (current state after 06-01 additions)
    - buildera-backend/app/Providers/AppServiceProvider.php (current state after 06-01)
    - buildera-backend/app/Http/Controllers/Api/CaseStudyController.php (current — upgrade paginate)
    - buildera-backend/app/Http/Controllers/Api/GuideController.php (current)
    - buildera-backend/app/Http/Controllers/Api/TestimonialController.php (current)
    - buildera-backend/routes/api.php (verify routes)
  </read_first>
  <action>
    **1. Update ContentObserver.php** — add cases for CaseStudy (tag: 'case_studies'), Guide (tag: 'guides'), Testimonial (tag: 'testimonials')

    **2. Update AppServiceProvider.php boot():**
    ```php
    CaseStudy::observe(ContentObserver::class);
    Guide::observe(ContentObserver::class);
    Testimonial::observe(ContentObserver::class);
    ```

    **3. Upgrade CaseStudyController:**
    - `index()`: `->scopePublished()->latest('published_at')->paginate(12)` with optional `?industry=` filter
    - Response shape: data[] with id, title, slug, client_name, industry, hero_image (Storage::url), hero_image_alt, key_metrics (array), published_at
    - `show($slug)`: full detail including problem, solution, results, testimonial_quote, testimonial_author

    **4. Upgrade GuideController:**
    - `index()`: `->scopePublished()->latest('published_at')->paginate(12)` with optional `?category=` and `?resource_type=` filters
    - Response: id, title, slug, category, description, resource_type, cover_image (Storage::url), cover_image_alt, published_at
    - `show($slug)`: full detail including body, external_link

    **5. Upgrade TestimonialController:**
    - `index()`: `->scopePublished()->orderBy('display_order')->get()` (no pagination — typically <50 items)
    - Optional filters: `?service_category=`, `?industry=`, `?featured=1`
    - Response: id, quote, client_name, job_title, company, company_logo (Storage::url), star_rating, service_category, industry, is_featured

    Ensure routes/api.php has entries for all three controllers (index + show).
  </action>
  <acceptance_criteria>
    - AppServiceProvider.php registers CaseStudy, Guide, Testimonial observers
    - ContentObserver handles 'case_studies', 'guides', 'testimonials' cache tags
    - GET /api/case-studies returns paginated JSON (data[], per_page=12)
    - GET /api/case-studies?industry=Manufacturing filters correctly
    - GET /api/guides?resource_type=checklist returns only checklists
    - GET /api/testimonials?service_category=Salesforce+Development returns Salesforce testimonials
    - Unpublished items absent from all three endpoints (REL-06)
    - `php artisan route:list --path=api` shows case-studies, guides, testimonials routes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat | ASVS | Component | Mitigation |
|--------|------|-----------|------------|
| XSS via RichEditor content (problem/solution/results) | V5 | CaseStudy body fields | Admin trust boundary — content from authenticated Filament users only |
| Arbitrary file type in company_logo upload | V12 | TestimonialResource FileUpload | `->image()->acceptedFileTypes(['image/*'])` on all FileUpload fields |
| Unauthenticated resource access | V4 | All 3 Filament resources | filament-shield Content group RBAC (Owner + Editor) |
| key_metrics injection via Repeater | V5 | CaseStudyResource Repeater | Filament Repeater handles validation; maxLength on label/value fields |
</threat_model>

<verification>
- `php artisan migrate` — key_metrics column on case_studies
- /admin/case-studies, /admin/guides, /admin/testimonials all load
- GET /api/case-studies, /api/guides, /api/testimonials return structured JSON
- All endpoints return 0 results when no published content exists (not a 500 error)
- ContentObserver handles all 6 model types (BlogPost + Author + Category + CaseStudy + Guide + Testimonial)
- `php artisan test` exits 0 (existing test suite unbroken)
</verification>

<success_criteria>
- Content nav group in Filament has 6 resources: BlogPost, Author, Category, CaseStudy, Guide, Testimonial
- All resources use Schema::make() — zero Form::make() instances
- ISR auto-fires on every content save for all 3 model types
- API endpoints return clean structured JSON consumed by frontend
- REL-06 enforced on all 3 controllers
</success_criteria>

<output>
Create `.planning/phases/06-content-blog-casestudies-guides/06-02-SUMMARY.md` when done.
</output>
