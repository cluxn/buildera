---
id: "02-05"
phase: 02-backend-core
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - buildera-backend/app/Http/Controllers/Api/RevalidateController.php
  - buildera-backend/app/Jobs/RevalidationJob.php
  - buildera-backend/app/Observers/ContentObserver.php
  - buildera-backend/app/Providers/AppServiceProvider.php
  - buildera-backend/routes/api.php
autonomous: true
requirements:
  - ADM-17
  - REL-01
---

<objective>
Wire the ISR revalidation pipeline: POST /api/revalidate endpoint (HMAC-verified, triggers Next.js revalidation), a ContentObserver that fires a RevalidationJob on every published content model save, and the RevalidationJob that POSTs to Next.js with the HMAC header. When an admin saves a blog post in Filament, the queue worker picks up the job within seconds and Next.js purges the cached page.

This plan also seeds the DatabaseSeeder to call SettingsSeeder (if not already done in 02-01).

Output: Saving a BlogPost in Filament dispatches a RevalidationJob to the queue. The job POSTs {"tag":"blog_posts"} to NEXTJS_REVALIDATE_URL with HMAC header. POST /api/revalidate verifies the HMAC and returns 200.
</objective>

<execution_context>
@C:\Users\ASUS\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ASUS\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\ASUS\Desktop\Buildera\.planning\PROJECT.md
@C:\Users\ASUS\Desktop\Buildera\.planning\phases\02-backend-core\02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RevalidationJob and ContentObserver</name>
  <read_first>
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Providers\AppServiceProvider.php
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\.env
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Jobs\FireLeadWebhookJob.php
  </read_first>
  <action>
Working directory: C:\Users\ASUS\Desktop\Buildera\buildera-backend\

Step 1 — Create app/Jobs/RevalidationJob.php:
  Namespace: App\Jobs
  Class: RevalidationJob implements ShouldQueue
  Use Dispatchable, InteractsWithQueue, Queueable, SerializesModels
  Constructor: public function __construct(public readonly string $tag)
  
  Method: handle(): void
    $url = env('NEXTJS_REVALIDATE_URL');
    $secret = env('NEXTJS_REVALIDATE_SECRET');
    if (empty($url) || empty($secret)) {
        \Log::warning('RevalidationJob: NEXTJS_REVALIDATE_URL or NEXTJS_REVALIDATE_SECRET not configured');
        return;
    }
    $payload = json_encode(['tag' => $this->tag]);
    $signature = hash_hmac('sha256', $payload, $secret);
    
    try {
        $response = \Illuminate\Support\Facades\Http::timeout(10)
            ->withHeaders(['X-Revalidate-Signature' => $signature])
            ->post($url, ['tag' => $this->tag]);
        
        if (!$response->successful()) {
            \Log::warning('RevalidationJob: Next.js revalidate returned ' . $response->status(), ['tag' => $this->tag]);
        }
    } catch (\Exception $e) {
        \Log::error('RevalidationJob failed: ' . $e->getMessage(), ['tag' => $this->tag]);
        throw $e; // re-throw so queue worker retries
    }

Step 2 — Create app/Observers/ContentObserver.php:
  Namespace: App\Observers
  Class: ContentObserver
  
  This observer is reused across all publishable content models.
  It needs to map model class → revalidation tag.
  
  Private method: getTag(Model $model): ?string
    $map = [
        \App\Models\BlogPost::class    => 'blog_posts',
        \App\Models\CaseStudy::class   => 'case_studies',
        \App\Models\Guide::class       => 'guides',
        \App\Models\Testimonial::class => 'testimonials',
    ];
    return $map[get_class($model)] ?? null;
  
  Method: saved(Model $model): void
    $tag = $this->getTag($model);
    if ($tag === null) return;
    // Only revalidate if is_published changed or content fields changed
    \App\Jobs\RevalidationJob::dispatch($tag);
  
  Method: deleted(Model $model): void
    $tag = $this->getTag($model);
    if ($tag !== null) {
        \App\Jobs\RevalidationJob::dispatch($tag);
    }
  
  Wrap entire saved() and deleted() in try/catch — observer must never crash the admin save.

Step 3 — Register ContentObserver in AppServiceProvider::boot():
  Read current AppServiceProvider.php. After the existing Lead/NewsletterSubscriber observers, add:
    BlogPost::observe(ContentObserver::class);
    CaseStudy::observe(ContentObserver::class);
    Guide::observe(ContentObserver::class);
    Testimonial::observe(ContentObserver::class);
  Add imports: use App\Models\BlogPost; use App\Models\CaseStudy; use App\Models\Guide; use App\Models\Testimonial; use App\Observers\ContentObserver;
  </action>
  <verify>
    <automated>Test-Path "C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Jobs\RevalidationJob.php"</automated>
    <automated>Test-Path "C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Observers\ContentObserver.php"</automated>
    <automated>grep "BlogPost::observe\|ContentObserver" C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Providers\AppServiceProvider.php</automated>
    <automated>grep "hash_hmac\|X-Revalidate-Signature" C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Jobs\RevalidationJob.php</automated>
  </verify>
  <acceptance_criteria>
    - RevalidationJob.php uses hash_hmac('sha256', $payload, $secret) to sign the request
    - RevalidationJob.php returns early (no-op) if URL or secret not configured
    - RevalidationJob.php re-throws exceptions so queue retries
    - ContentObserver.php has getTag() map covering all 4 publishable content models
    - ContentObserver.php fires RevalidationJob on saved() and deleted()
    - ContentObserver.php wraps in try/catch
    - AppServiceProvider registers ContentObserver for BlogPost, CaseStudy, Guide, Testimonial
  </acceptance_criteria>
  <done>RevalidationJob with HMAC signing created. ContentObserver fires revalidation on save/delete for all publishable models. Observers registered in AppServiceProvider.</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/revalidate endpoint with HMAC verification</name>
  <read_first>
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\routes\api.php
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Jobs\RevalidationJob.php
  </read_first>
  <action>
Working directory: C:\Users\ASUS\Desktop\Buildera\buildera-backend\

Step 1 — Create app/Http/Controllers/Api/RevalidateController.php:
  Namespace: App\Http\Controllers\Api
  Class: RevalidateController
  
  Method: handle(Request $request): JsonResponse
    Verify HMAC signature:
    $secret = env('NEXTJS_REVALIDATE_SECRET');
    $signature = $request->header('X-Revalidate-Signature');
    $payload = $request->getContent();
    $expected = hash_hmac('sha256', $payload, $secret);
    if (!hash_equals($expected, (string) $signature)) {
        return response()->json(['message' => 'Unauthorized'], 401);
    }
    
    $tag = $request->input('tag');
    if (empty($tag)) {
        return response()->json(['message' => 'tag is required'], 422);
    }
    
    // This endpoint is called BY Next.js to acknowledge a revalidation request
    // OR can be used to manually trigger revalidation from external tools
    return response()->json(['message' => 'Revalidation acknowledged', 'tag' => $tag], 200);

  NOTE: The revalidation flow goes:
    Admin saves content → ContentObserver → RevalidationJob queued → Job POSTs to Next.js /api/revalidate
    This Laravel endpoint is for external triggers or acknowledgement only.

Step 2 — Update routes/api.php — add revalidation route:
  Read current api.php. At the bottom, add:

// ISR revalidation trigger — HMAC-verified, no rate limit (internal use only)
Route::post('/revalidate', [\App\Http\Controllers\Api\RevalidateController::class, 'handle']);

Step 3 — Add NEXTJS_REVALIDATE_SECRET to AppServiceProvider required env vars:
  Read current AppServiceProvider.php. The required vars array currently has:
    ['APP_KEY', 'DB_HOST', 'DB_DATABASE', 'DB_USERNAME', 'RESEND_API_KEY', 'APP_API_KEY']
  Add 'NEXTJS_REVALIDATE_SECRET' to this array so boot fails fast if it's missing.
  Update the array to include it.

Step 4 — Verify all routes:
  php artisan route:list --path=api 2>&1
  </action>
  <verify>
    <automated>php artisan route:list --path=api 2>&1</automated>
    <automated>grep "NEXTJS_REVALIDATE_SECRET" C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Providers\AppServiceProvider.php</automated>
    <automated>grep "hash_hmac\|hash_equals" C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Http\Controllers\Api\RevalidateController.php</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/revalidate registered in routes with no middleware (HMAC is the security)
    - RevalidateController verifies HMAC with hash_equals() (timing-safe)
    - RevalidateController returns 401 if signature invalid
    - RevalidateController returns 422 if 'tag' field missing
    - NEXTJS_REVALIDATE_SECRET added to AppServiceProvider required env vars list
  </acceptance_criteria>
  <done>RevalidateController with HMAC verification created. POST /api/revalidate route registered. NEXTJS_REVALIDATE_SECRET added to required env vars.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend revalidation endpoint scaffold</name>
  <read_first>
    - C:\Users\ASUS\Desktop\Buildera\buildera-frontend\src\lib\api.ts
    - C:\Users\ASUS\Desktop\Buildera\buildera-frontend\src\app\not-found.tsx
  </read_first>
  <action>
Working directory: C:\Users\ASUS\Desktop\Buildera\buildera-frontend\

Create src/app/api/revalidate/route.ts — the Next.js API route that receives revalidation requests from Laravel:

  import { revalidateTag } from 'next/cache';
  import { NextRequest, NextResponse } from 'next/server';
  import crypto from 'crypto';

  export async function POST(request: NextRequest): Promise<NextResponse> {
    const secret = process.env.NEXTJS_REVALIDATE_SECRET;
    if (!secret) {
      return NextResponse.json({ message: 'Not configured' }, { status: 500 });
    }

    const body = await request.text();
    const signature = request.headers.get('x-revalidate-signature') ?? '';
    const expected = crypto.createHmac('sha256', secret).update(body).digest('hex');

    if (!crypto.timingSafeEqual(Buffer.from(expected), Buffer.from(signature))) {
      return NextResponse.json({ message: 'Unauthorized' }, { status: 401 });
    }

    let tag: string;
    try {
      const parsed = JSON.parse(body);
      tag = parsed.tag;
      if (!tag || typeof tag !== 'string') throw new Error('missing tag');
    } catch {
      return NextResponse.json({ message: 'Invalid payload' }, { status: 422 });
    }

    revalidateTag(tag);
    return NextResponse.json({ revalidated: true, tag }, { status: 200 });
  }

Also add NEXTJS_REVALIDATE_SECRET to buildera-frontend/.env.example:
  Read current .env.example. Add if not present:
  NEXTJS_REVALIDATE_SECRET=your-revalidate-secret   # Must match NEXTJS_REVALIDATE_SECRET in Laravel .env

Add to buildera-frontend/.env.local (create if not exists):
  NEXTJS_REVALIDATE_SECRET=dev-revalidate-secret-change-in-production
  (Same value as in buildera-backend/.env)

Run TypeScript check:
  cd C:\Users\ASUS\Desktop\Buildera\buildera-frontend && npx tsc --noEmit 2>&1 | head -20
  </action>
  <verify>
    <automated>Test-Path "C:\Users\ASUS\Desktop\Buildera\buildera-frontend\src\app\api\revalidate\route.ts"</automated>
    <automated>grep "timingSafeEqual\|revalidateTag" C:\Users\ASUS\Desktop\Buildera\buildera-frontend\src\app\api\revalidate\route.ts</automated>
    <automated>cd C:\Users\ASUS\Desktop\Buildera\buildera-frontend; npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/revalidate/route.ts exists and exports POST handler
    - Handler uses crypto.timingSafeEqual for timing-safe signature comparison
    - Handler calls revalidateTag(tag) on valid request
    - Handler returns 401 for invalid signature, 422 for missing tag
    - npx tsc --noEmit exits 0
    - .env.example documents NEXTJS_REVALIDATE_SECRET
  </acceptance_criteria>
  <done>Next.js /api/revalidate route created with HMAC verification and revalidateTag() call. TypeScript passes.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. php artisan route:list --path=api — shows POST api/revalidate with no middleware
2. grep "BlogPost::observe\|ContentObserver" buildera-backend/app/Providers/AppServiceProvider.php
3. grep "NEXTJS_REVALIDATE_SECRET" buildera-backend/app/Providers/AppServiceProvider.php
4. Test-Path buildera-frontend/src/app/api/revalidate/route.ts
5. npx tsc --noEmit (from buildera-frontend/) — exit 0
6. php artisan config:clear && php artisan list — no errors
</verification>

<success_criteria>
1. ContentObserver registered for all 4 publishable models
2. RevalidationJob uses hash_hmac HMAC signature in POST to Next.js
3. POST /api/revalidate verifies HMAC and calls revalidateTag()
4. NEXTJS_REVALIDATE_SECRET in required env vars (boot fails if missing)
5. TypeScript passes on frontend revalidate route
6. Full Phase 2 API surface: GET /api/settings, GET /api/blog-posts, POST /api/leads (auth+throttle), POST /api/subscribers (throttle), POST /api/revalidate (HMAC) all registered
</success_criteria>

<output>
Create .planning/phases/02-backend-core/02-05-SUMMARY.md when done
</output>
