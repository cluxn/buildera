---
id: "02-04"
phase: 02-backend-core
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - buildera-backend/app/Models/NewsletterSubscriber.php
  - buildera-backend/app/Http/Controllers/Api/SubscriberController.php
  - buildera-backend/app/Http/Controllers/Api/UnsubscribeController.php
  - buildera-backend/app/Jobs/SendNewsletterWelcomeJob.php
  - buildera-backend/app/Jobs/FireNewsletterWebhookJob.php
  - buildera-backend/app/Filament/Resources/NewsletterSubscriberResource.php
  - buildera-backend/app/Observers/AuditLogObserver.php
  - buildera-backend/app/Providers/AppServiceProvider.php
  - buildera-backend/routes/api.php
autonomous: true
requirements:
  - LEAD-03
  - REL-04
  - REL-05
  - REL-08
---

<objective>
Build the newsletter subscriber endpoint with rate limiting, duplicate handling, signed unsubscribe token, and queued side effects. Wire the AuditLog observer to Lead and NewsletterSubscriber so every create/update is logged. Build the NewsletterSubscriberResource in Filament.

n8n webhook for newsletter: env('N8N_NEWSLETTER_WEBHOOK_URL') — already in .env.
config/services.php already has n8n.newsletter_webhook_url from plan 02-03.

Output: POST /api/subscribers with new email → 201, unsubscribe_token generated, queued jobs fired. Same email again → 200 with message "already subscribed". GET /api/unsubscribe?token=... → unsubscribes and returns 200. AuditLog rows created on lead/subscriber save.
</objective>

<execution_context>
@C:\Users\ASUS\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ASUS\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\ASUS\Desktop\Buildera\.planning\PROJECT.md
@C:\Users\ASUS\Desktop\Buildera\.planning\phases\02-backend-core\02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NewsletterSubscriber model and queued jobs</name>
  <read_first>
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\database\migrations\2026_01_01_000016_create_newsletter_subscribers_table.php
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Jobs\FireLeadWebhookJob.php
  </read_first>
  <action>
Working directory: C:\Users\ASUS\Desktop\Buildera\buildera-backend\

Step 1 — Create app/Models/NewsletterSubscriber.php:
  Namespace: App\Models
  Class: NewsletterSubscriber extends Model
  fillable: ['email', 'name', 'status', 'unsubscribe_token', 'subscribed_at', 'unsubscribed_at']
  Cast: 'subscribed_at' => 'datetime', 'unsubscribed_at' => 'datetime'
  
  Static method: generateToken(): string
    return \Illuminate\Support\Str::random(64);
  
  Method: unsubscribe(): void
    $this->update(['status' => 'unsubscribed', 'unsubscribed_at' => now()]);

Step 2 — Create app/Jobs/SendNewsletterWelcomeJob.php:
  Namespace: App\Jobs
  Class: SendNewsletterWelcomeJob implements ShouldQueue
  Use Dispatchable, InteractsWithQueue, Queueable, SerializesModels
  Constructor: public function __construct(public readonly NewsletterSubscriber $subscriber)
  Method: handle(): void
    Send a simple welcome email with the unsubscribe link.
    Unsubscribe URL: config('app.url') . '/api/unsubscribe?token=' . $this->subscriber->unsubscribe_token
    Wrap in try/catch, log errors. Don't fail job on email error.

Step 3 — Create app/Jobs/FireNewsletterWebhookJob.php:
  Namespace: App\Jobs
  Class: FireNewsletterWebhookJob implements ShouldQueue
  Constructor: public function __construct(public readonly array $payload)
  Method: handle(): void
    $url = config('services.n8n.newsletter_webhook_url', env('N8N_NEWSLETTER_WEBHOOK_URL'));
    if (empty($url)) return;
    $response = \Illuminate\Support\Facades\Http::timeout(10)->post($url, $this->payload);
    if (!$response->successful()) {
        \Log::warning('n8n newsletter webhook failed', ['status' => $response->status()]);
    }
  </action>
  <verify>
    <automated>Test-Path "C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Models\NewsletterSubscriber.php"</automated>
    <automated>grep "generateToken\|unsubscribe" C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Models\NewsletterSubscriber.php</automated>
    <automated>Test-Path "C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Jobs\FireNewsletterWebhookJob.php"</automated>
  </verify>
  <acceptance_criteria>
    - NewsletterSubscriber.php has generateToken() and unsubscribe() methods
    - SendNewsletterWelcomeJob includes unsubscribe URL in email body
    - FireNewsletterWebhookJob checks for empty URL before posting
    - Both jobs implement ShouldQueue
  </acceptance_criteria>
  <done>NewsletterSubscriber model and both newsletter queued jobs created.</done>
</task>

<task type="auto">
  <name>Task 2: SubscriberController, UnsubscribeController, and routes</name>
  <read_first>
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\routes\api.php
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Models\NewsletterSubscriber.php
  </read_first>
  <action>
Working directory: C:\Users\ASUS\Desktop\Buildera\buildera-backend\

Step 1 — Create app/Http/Controllers/Api/SubscriberController.php:
  Class: SubscriberController
  Method: store(Request $request): JsonResponse
  
  Validation:
    'email' => ['required', 'email', 'max:255'],
    'name'  => ['nullable', 'string', 'max:255'],
  
  Duplicate check:
    $existing = NewsletterSubscriber::where('email', $request->email)->first();
    if ($existing) {
        if ($existing->status === 'unsubscribed') {
            // Re-subscribe: update status and token
            $existing->update([
                'status' => 'subscribed',
                'subscribed_at' => now(),
                'unsubscribed_at' => null,
                'unsubscribe_token' => NewsletterSubscriber::generateToken(),
            ]);
            $subscriber = $existing;
        } else {
            return response()->json(['message' => 'Already subscribed.'], 200);
        }
    } else {
        $subscriber = NewsletterSubscriber::create([
            'email' => $request->email,
            'name' => $request->name,
            'status' => 'subscribed',
            'subscribed_at' => now(),
            'unsubscribe_token' => NewsletterSubscriber::generateToken(),
        ]);
    }
  
  Dispatch jobs:
    SendNewsletterWelcomeJob::dispatch($subscriber);
    FireNewsletterWebhookJob::dispatch($subscriber->toArray());
  
  Return: response()->json(['message' => 'Subscribed successfully.'], 201)

Step 2 — Create app/Http/Controllers/Api/UnsubscribeController.php:
  Class: UnsubscribeController
  Method: handle(Request $request): JsonResponse
    $token = $request->query('token');
    if (empty($token)) {
        return response()->json(['message' => 'Invalid token.'], 422);
    }
    $subscriber = NewsletterSubscriber::where('unsubscribe_token', $token)->first();
    if (!$subscriber) {
        return response()->json(['message' => 'Token not found.'], 404);
    }
    if ($subscriber->status === 'unsubscribed') {
        return response()->json(['message' => 'Already unsubscribed.'], 200);
    }
    $subscriber->unsubscribe();
    return response()->json(['message' => 'Unsubscribed successfully.'], 200);

Step 3 — Update routes/api.php — add subscriber routes:
  Read the current api.php. At the bottom, add:

// Newsletter subscribers — rate limited 3/hr, no API key required
Route::post('/subscribers', [\App\Http\Controllers\Api\SubscriberController::class, 'store'])
    ->middleware(['throttle:3,60']);

// Unsubscribe via signed token — public, no auth
Route::get('/unsubscribe', [\App\Http\Controllers\Api\UnsubscribeController::class, 'handle']);

Step 4 — Verify routes:
  php artisan route:list --path=api/subscribers 2>&1
  php artisan route:list --path=api/unsubscribe 2>&1
  </action>
  <verify>
    <automated>php artisan route:list --path=api/subscribers 2>&1</automated>
    <automated>php artisan route:list --path=api/unsubscribe 2>&1</automated>
    <automated>grep "throttle:3,60" C:\Users\ASUS\Desktop\Buildera\buildera-backend\routes\api.php</automated>
    <automated>grep "re-subscribe\|status.*subscribed\|generateToken" C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Http\Controllers\Api\SubscriberController.php</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/subscribers has throttle:3,60 middleware, no api.key middleware
    - GET /api/unsubscribe has no middleware (public, token-validated in controller)
    - SubscriberController handles re-subscribe (status was unsubscribed)
    - SubscriberController returns 200 "Already subscribed" for existing active subscriber
    - UnsubscribeController returns 404 for unknown token, 200 for valid token
  </acceptance_criteria>
  <done>SubscriberController with re-subscribe handling. UnsubscribeController with token validation. Both routes registered.</done>
</task>

<task type="auto">
  <name>Task 3: AuditLog observer and NewsletterSubscriberResource</name>
  <read_first>
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\database\migrations\2026_01_01_000022_create_audit_logs_table.php
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Providers\AppServiceProvider.php
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Models\Lead.php
  </read_first>
  <action>
Working directory: C:\Users\ASUS\Desktop\Buildera\buildera-backend\

Step 1 — Create app/Models/AuditLog.php:
  Namespace: App\Models
  Class: AuditLog extends Model
  fillable: ['user_id', 'auditable_type', 'auditable_id', 'event', 'old_values', 'new_values', 'ip_address', 'user_agent']
  Cast: 'old_values' => 'array', 'new_values' => 'array'
  Read migration to confirm exact column names.

Step 2 — Create app/Observers/AuditLogObserver.php:
  Namespace: App\Observers
  Class: AuditLogObserver
  
  Method: created(Model $model): void
    AuditLog::create([
        'user_id'        => auth()->id(), // null for API-created records
        'auditable_type' => get_class($model),
        'auditable_id'   => $model->getKey(),
        'event'          => 'created',
        'new_values'     => $model->toArray(),
        'old_values'     => null,
        'ip_address'     => request()?->ip(),
        'user_agent'     => request()?->userAgent(),
    ]);
  
  Method: updated(Model $model): void
    AuditLog::create([
        'user_id'        => auth()->id(),
        'auditable_type' => get_class($model),
        'auditable_id'   => $model->getKey(),
        'event'          => 'updated',
        'new_values'     => $model->getChanges(),
        'old_values'     => $model->getOriginal(),
        'ip_address'     => request()?->ip(),
    ]);
  
  Wrap entire method body in try/catch — audit log must never crash the main request.

Step 3 — Register observer in AppServiceProvider::boot():
  Read the current AppServiceProvider.php (has env validation). Add after the env validation loop:
    Lead::observe(AuditLogObserver::class);
    NewsletterSubscriber::observe(AuditLogObserver::class);
  Add imports: use App\Models\Lead; use App\Models\NewsletterSubscriber; use App\Observers\AuditLogObserver;

Step 4 — Create NewsletterSubscriberResource Filament resource:
  Create app/Filament/Resources/NewsletterSubscriberResource.php
  navigationGroup: 'Leads & CRM'
  navigationLabel: 'Newsletter Subscribers'
  navigationIcon: 'heroicon-o-envelope'
  navigationSort: 2
  
  table columns: email, name, status (badge), subscribed_at (sortable), unsubscribed_at
  defaultSort: subscribed_at desc
  filters: SelectFilter status (['subscribed', 'unsubscribed'])
  
  No create/edit pages — subscribers come via API. Only index + view.
  
  Create Pages/ListNewsletterSubscribers.php and ViewNewsletterSubscriber.php in Resources/NewsletterSubscriberResource/Pages/
  </action>
  <verify>
    <automated>Test-Path "C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Observers\AuditLogObserver.php"</automated>
    <automated>grep "Lead::observe\|NewsletterSubscriber::observe" C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Providers\AppServiceProvider.php</automated>
    <automated>Test-Path "C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Filament\Resources\NewsletterSubscriberResource.php"</automated>
    <automated>php artisan route:list --path=admin 2>&1 | Select-String "newsletter\|subscriber" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - AuditLogObserver.php wraps all logic in try/catch
    - created() and updated() both log to audit_logs table
    - AppServiceProvider registers observer for Lead AND NewsletterSubscriber
    - AuditLog model has array casts on old_values and new_values
    - NewsletterSubscriberResource in 'Leads & CRM' group
    - php artisan route:list --path=admin shows newsletter subscriber routes
  </acceptance_criteria>
  <done>AuditLogObserver created (wrapped in try/catch) and registered for Lead + NewsletterSubscriber models. NewsletterSubscriberResource Filament resource created under Leads & CRM.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. php artisan route:list --path=api/subscribers — POST with throttle:3,60
2. php artisan route:list --path=api/unsubscribe — GET, no middleware
3. grep "Lead::observe" buildera-backend/app/Providers/AppServiceProvider.php
4. Test-Path buildera-backend/app/Observers/AuditLogObserver.php
5. php artisan config:clear && php artisan list — no errors
</verification>

<success_criteria>
1. POST /api/subscribers with new email returns 201
2. POST /api/subscribers with same active email returns 200 "Already subscribed"
3. GET /api/unsubscribe?token=valid returns 200 "Unsubscribed successfully"
4. Lead creation triggers AuditLog row (try/catch prevents crash if DB issue)
5. NewsletterSubscriber resource visible in Filament Leads & CRM group
6. Both jobs implement ShouldQueue
</success_criteria>

<output>
Create .planning/phases/02-backend-core/02-04-SUMMARY.md when done
</output>
