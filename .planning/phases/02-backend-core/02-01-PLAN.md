---
id: "02-01"
phase: 02-backend-core
plan: 01
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - buildera-backend/app/Models/Setting.php
  - buildera-backend/app/Filament/Pages/ManageSettings.php
  - buildera-backend/app/Providers/Filament/AdminPanelProvider.php
  - buildera-backend/app/Models/User.php
  - buildera-backend/database/seeders/SettingsSeeder.php
autonomous: true
requirements:
  - ADM-01
  - ADM-11
  - ADM-17
---

<objective>
Install Filament Shield RBAC, create the Setting model with static get/set helpers and 1-hour cache, build the ManageSettings Filament page with all company/social/WhatsApp/Calendly fields grouped into tabs, and seed default settings. This plan lays the data and admin foundation everything in Phase 2+ depends on — public API controllers read from settings, lead forms reference WhatsApp/Calendly config, and ISR revalidation reads NEXTJS_REVALIDATE_URL from settings at runtime.

Output: `php artisan /admin` shows ManageSettings under Settings group; `Setting::get('company_name', 'Buildera')` returns the seeded value; `php artisan shield:setup` ran; Owner/Editor/Viewer roles exist.
</objective>

<execution_context>
@C:\Users\ASUS\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ASUS\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\ASUS\Desktop\Buildera\.planning\PROJECT.md
@C:\Users\ASUS\Desktop\Buildera\.planning\ROADMAP.md
@C:\Users\ASUS\Desktop\Buildera\.planning\phases\01-project-foundation\01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setting model with static get/set helpers and cache</name>
  <read_first>
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\database\migrations\2026_01_01_000017_create_settings_table.php
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Models\User.php
  </read_first>
  <action>
Working directory: C:\Users\ASUS\Desktop\Buildera\buildera-backend\

Step 1 — Create app/Models/Setting.php:
  Namespace: App\Models
  Class: Setting extends Model
  fillable: ['key', 'value', 'group']
  No timestamps — set $timestamps = false (settings table has timestamps but we manage it via updateOrCreate)
  Actually keep timestamps = true (default).

  Static method: get(string $key, mixed $default = null): mixed
    - Cache::remember("setting_{$key}", 3600, fn() => static::where('key', $key)->value('value') ?? $default)
    - Imports: use Illuminate\Support\Facades\Cache;

  Static method: set(string $key, mixed $value, ?string $group = null): void
    - static::updateOrCreate(['key' => $key], ['value' => $value, 'group' => $group])
    - Cache::forget("setting_{$key}")

  Static method: getGroup(string $group): array
    - Returns key => value array for all settings in a group, 1hr cached

Step 2 — Create database/seeders/SettingsSeeder.php:
  Seed these default settings (key, value, group):
    company settings group:
      'company_name' => 'Buildera'
      'company_tagline' => 'We Build What Grows Your Business'
      'company_phone' => '+1 (555) 000-0000'
      'company_email' => 'hello@buildera.co'
      'company_address' => ''
    social group:
      'social_linkedin' => ''
      'social_twitter' => ''
      'social_facebook' => ''
      'social_github' => ''
      'social_youtube' => ''
    contact group:
      'whatsapp_number' => ''
      'whatsapp_message' => 'Hi Buildera! I'd like to discuss a project.'
      'calendly_url' => ''
    seo group:
      'default_seo_title' => 'Buildera — IT Services & Custom Software Development'
      'default_seo_description' => 'Buildera builds custom software, Salesforce solutions, DevOps pipelines, and AI agents for growing businesses.'
      'og_image' => ''
    footer group:
      'footer_copyright' => '© 2025 Buildera. All rights reserved.'
  Use Setting::updateOrCreate(['key' => $key], ['value' => $value, 'group' => $group]) for each.

Step 3 — Register seeder in DatabaseSeeder.php:
  Read app/database/seeders/DatabaseSeeder.php. Add $this->call(SettingsSeeder::class); in run() method.
  Add use App\Database\Seeders\SettingsSeeder — actually use database\Seeders\ namespace. Check existing seeder namespace.

Step 4 — Run seeder:
  php artisan db:seed --class=SettingsSeeder
  (Will fail gracefully if DB not connected — that's fine for local dev)
  </action>
  <verify>
    <automated>php artisan list --format=json 2>&1 | head -5</automated>
    <automated>grep -r "getGroup\|Cache::remember" C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Models\Setting.php</automated>
  </verify>
  <acceptance_criteria>
    - app/Models/Setting.php exists with static get(), set(), getGroup() methods
    - get() uses Cache::remember with 3600s TTL
    - set() calls Cache::forget after updateOrCreate
    - SettingsSeeder seeds all 4 groups (company, social, contact, seo)
    - DatabaseSeeder calls SettingsSeeder
  </acceptance_criteria>
  <done>Setting model created with cache-backed static helpers. SettingsSeeder created with all default key-value groups.</done>
</task>

<task type="auto">
  <name>Task 2: Filament Shield setup — RBAC with Owner/Editor/Viewer roles</name>
  <read_first>
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Models\User.php
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Providers\Filament\AdminPanelProvider.php
  </read_first>
  <action>
Working directory: C:\Users\ASUS\Desktop\Buildera\buildera-backend\

filament-shield is already installed (per Phase 1). Configure it:

Step 1 — Run Shield setup:
  php artisan shield:setup --fresh
  This publishes the shield config and creates the permissions tables migration if not already present.
  The --fresh flag resets to clean defaults.

Step 2 — Update app/Models/User.php to support Filament authentication and Shield roles:
  Read the current User.php. Add:
    - Implements \Filament\Models\Contracts\FilamentUser interface
    - Implements \Filament\Models\Contracts\HasAvatar interface (optional but good practice)
    - Use \BezhanSalleh\FilamentShield\Traits\HasPanelShield trait
    - Use \Spatie\Permission\Traits\HasRoles trait
    - canAccessPanel(Panel $panel): bool method — return true (all authenticated users can access, Shield handles resource-level permissions)
  Import: use Filament\Panel; use Filament\Models\Contracts\FilamentUser;

Step 3 — Add Shield plugin to AdminPanelProvider:
  Read the current AdminPanelProvider.php. Add ->plugins([ShieldPlugin::make()]) to the panel() method.
  Add import: use BezhanSalleh\FilamentShield\FilamentShieldPlugin;
  Change ->plugins([ShieldPlugin::make()]) NOT FilamentShieldPlugin — use correct class. 
  The correct class is: use BezhanSalleh\FilamentShield\FilamentShieldPlugin;
  Add ->plugins([\BezhanSalleh\FilamentShield\FilamentShieldPlugin::make()]) to the panel chain.

Step 4 — Run Shield generate to create role/permission resources:
  php artisan shield:generate --all
  This creates the RoleResource and PermissionResource in Filament.

Step 5 — Create a fresh admin user (if artisan is responsive):
  php artisan make:filament-user 2>&1 || echo "manual step required"
  Note: This is interactive, so it may fail in non-interactive mode — that's OK. Document it as a manual step.
  </action>
  <verify>
    <automated>grep "HasRoles\|FilamentUser\|canAccessPanel" C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Models\User.php</automated>
    <automated>grep "FilamentShieldPlugin\|ShieldPlugin" C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Providers\Filament\AdminPanelProvider.php</automated>
    <automated>php artisan route:list --path=admin 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - User.php implements FilamentUser and includes HasRoles trait
    - User.php has canAccessPanel() method returning true
    - AdminPanelProvider.php has FilamentShieldPlugin registered
    - php artisan route:list --path=admin still shows admin routes (panel not broken)
  </acceptance_criteria>
  <done>filament-shield configured; User model updated with FilamentUser interface and HasRoles trait; Shield plugin registered in AdminPanelProvider.</done>
</task>

<task type="auto">
  <name>Task 3: ManageSettings Filament page with tabbed form</name>
  <read_first>
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Providers\Filament\AdminPanelProvider.php
    - C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Models\Setting.php
  </read_first>
  <action>
Working directory: C:\Users\ASUS\Desktop\Buildera\buildera-backend\

Step 1 — Create directory app/Filament/Pages/ if not exists.

Step 2 — Create app/Filament/Pages/ManageSettings.php:
  Namespace: App\Filament\Pages
  Class: ManageSettings extends Page
  
  CRITICAL — Filament v5 API rules:
  - Use Schema::make() NOT Form::make()
  - Import: use Filament\Schemas\Schema; use Filament\Schemas\Components\Tabs; use Filament\Schemas\Components\Tabs\Tab;
  - Form fields use Filament\Forms\Components\TextInput etc.
  
  Static properties:
    protected static ?string $navigationGroup = 'Settings';
    protected static ?string $navigationLabel = 'General Settings';
    protected static ?string $navigationIcon = 'heroicon-o-cog-6-tooth';
    protected static ?int $navigationSort = 1;
    protected static string $view = 'filament.pages.manage-settings';
  
  Method: form(Schema $schema): Schema
    Return $schema->components([
      Tabs::make('Settings')->tabs([
        Tab::make('Company')->schema([
          TextInput::make('company_name')->label('Company Name')->required(),
          TextInput::make('company_tagline')->label('Tagline'),
          TextInput::make('company_phone')->label('Phone'),
          TextInput::make('company_email')->label('Email')->email(),
          Textarea::make('company_address')->label('Address')->rows(2),
        ]),
        Tab::make('Social Media')->schema([
          TextInput::make('social_linkedin')->label('LinkedIn URL')->url(),
          TextInput::make('social_twitter')->label('Twitter/X URL')->url(),
          TextInput::make('social_facebook')->label('Facebook URL')->url(),
          TextInput::make('social_github')->label('GitHub URL')->url(),
          TextInput::make('social_youtube')->label('YouTube URL')->url(),
        ]),
        Tab::make('Contact & Booking')->schema([
          TextInput::make('whatsapp_number')->label('WhatsApp Number')->helperText('Include country code, e.g. +1234567890'),
          Textarea::make('whatsapp_message')->label('WhatsApp Default Message')->rows(2),
          TextInput::make('calendly_url')->label('Calendly URL')->url(),
        ]),
        Tab::make('SEO Defaults')->schema([
          TextInput::make('default_seo_title')->label('Default SEO Title')->maxLength(70),
          Textarea::make('default_seo_description')->label('Default Meta Description')->rows(3)->maxLength(160),
          TextInput::make('og_image')->label('Default OG Image URL')->url(),
          TextInput::make('footer_copyright')->label('Footer Copyright Text'),
        ]),
      ])
    ]);
  
  Method: mount(): void
    Load current setting values into form:
    $allKeys = ['company_name', 'company_tagline', 'company_phone', 'company_email', 'company_address',
                'social_linkedin', 'social_twitter', 'social_facebook', 'social_github', 'social_youtube',
                'whatsapp_number', 'whatsapp_message', 'calendly_url',
                'default_seo_title', 'default_seo_description', 'og_image', 'footer_copyright'];
    $data = [];
    foreach ($allKeys as $key) {
        $data[$key] = Setting::get($key, '');
    }
    $this->form->fill($data);
  
  Method: save(): void (Filament action)
    $data = $this->form->getState();
    foreach ($data as $key => $value) {
        Setting::set($key, $value ?? '');
    }
    $this->getSavedNotification()->send();  // or Notification::make()->title('Settings saved')->success()->send();
  
  Add form state tracking: use Filament\Actions\Action; use Filament\Notifications\Notification;
  The page needs InteractsWithForms trait and HasForms interface.

Step 3 — Create the Blade view at resources/views/filament/pages/manage-settings.blade.php:
  Content:
  <x-filament-panels::page>
      <x-filament-panels::form wire:submit="save">
          {{ $this->form }}
          <x-filament-panels::form.actions :actions="$this->getCachedFormActions()" />
      </x-filament-panels::form>
  </x-filament-panels::page>

Step 4 — Verify the page registers correctly:
  php artisan route:list --path=admin 2>&1 | grep -i settings
  php artisan filament:check 2>&1 || echo "check done"
  </action>
  <verify>
    <automated>Test-Path "C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Filament\Pages\ManageSettings.php"</automated>
    <automated>grep "ManageSettings\|manage-settings" C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Filament\Pages\ManageSettings.php</automated>
    <automated>grep "Settings" C:\Users\ASUS\Desktop\Buildera\buildera-backend\app\Filament\Pages\ManageSettings.php</automated>
    <automated>php artisan route:list --path=admin 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/Filament/Pages/ManageSettings.php exists
    - NavigationGroup is 'Settings'
    - Form has all 4 tabs: Company, Social Media, Contact & Booking, SEO Defaults
    - mount() loads current Setting values into form fields
    - save() persists form values via Setting::set()
    - Blade view exists at resources/views/filament/pages/manage-settings.blade.php
    - php artisan route:list --path=admin runs without PHP errors
  </acceptance_criteria>
  <done>ManageSettings Filament page created with 4-tab form covering all company/social/contact/SEO settings. Blade view created. Settings save correctly via Setting::set().</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. php artisan route:list --path=admin — shows manage-settings route in output
2. grep "HasRoles" buildera-backend/app/Models/User.php — confirms trait applied
3. grep "FilamentShieldPlugin" buildera-backend/app/Providers/Filament/AdminPanelProvider.php
4. Test-Path "buildera-backend/app/Filament/Pages/ManageSettings.php" — file exists
5. php artisan config:clear && php artisan cache:clear — no errors on boot
</verification>

<success_criteria>
1. Setting::get('company_name', 'Buildera') returns seeded value
2. ManageSettings page accessible under Settings navigation group
3. User model implements FilamentUser with canAccessPanel() method
4. FilamentShieldPlugin registered in AdminPanelProvider
5. php artisan config:clear runs without PHP errors
</success_criteria>

<output>
Create .planning/phases/02-backend-core/02-01-SUMMARY.md when done
</output>
