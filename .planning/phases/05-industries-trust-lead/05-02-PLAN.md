---
plan: "05-02"
wave: 1
phase: 5
depends_on: []
files_modified:
  - buildera-frontend/src/app/about/page.tsx
  - buildera-frontend/src/app/how-we-work/page.tsx
  - buildera-frontend/src/app/faq/page.tsx
  - buildera-frontend/src/app/privacy/page.tsx
  - buildera-frontend/src/app/terms/page.tsx
  - buildera-frontend/src/components/sections/trust/AboutHero.tsx
  - buildera-frontend/src/components/sections/trust/TeamSection.tsx
  - buildera-frontend/src/components/sections/trust/ValuesSection.tsx
  - buildera-frontend/src/components/sections/trust/AboutCta.tsx
  - buildera-frontend/src/components/sections/trust/ProcessTimeline.tsx
  - buildera-frontend/src/components/sections/trust/ProcessCta.tsx
  - buildera-frontend/src/components/sections/trust/FaqAccordion.tsx
  - buildera-frontend/src/components/ui/AnimatedTimeline.tsx
requirements_addressed:
  - TRUST-01
  - TRUST-02
  - TRUST-03
  - TRUST-04
  - TRUST-05
autonomous: true

must_haves:
  truths:
    - "All 5 trust pages accessible at /about, /how-we-work, /faq, /privacy, /terms"
    - "FAQPage JSON-LD script tag present on /faq (for SEO Phase 7 extension)"
    - "AnimatedTimeline is use client; all page shells are Server Components"
    - "Privacy and Terms render long-form text with correct prose styling"
    - "About page has Clutch badge / trust signal near CTA"
  artifacts:
    - path: "buildera-frontend/src/app/about/page.tsx"
      provides: "About Us page — company story, values, team"
    - path: "buildera-frontend/src/app/how-we-work/page.tsx"
      provides: "6-step animated process timeline"
    - path: "buildera-frontend/src/app/faq/page.tsx"
      provides: "Accordion FAQ + FAQPage JSON-LD schema"
    - path: "buildera-frontend/src/components/ui/AnimatedTimeline.tsx"
      provides: "Animated 6-step process timeline (use client)"
  key_links:
    - from: "src/app/how-we-work/page.tsx"
      to: "src/components/ui/AnimatedTimeline.tsx"
      via: "renders AnimatedTimeline client component"
    - from: "src/app/faq/page.tsx"
      to: "shadcn Accordion"
      via: "npx shadcn add accordion (already installed from 04-01)"
---

<objective>
Build all 5 trust/credibility pages: About Us, How We Work, FAQ, Privacy Policy, Terms of Service. These pages handle visitor due diligence — the "who are you really?" check decision-makers do before booking a call. Every page ends with a CTA to /book-a-call.

No new data infrastructure needed — all content is hardcoded.
</objective>

<execution_context>
@.planning/ROADMAP.md (Phase 5 requirements: TRUST-01 → TRUST-05)
@.planning/STATE.md
</execution_context>

<context>
@CLAUDE.md

<existing_patterns>
From Phase 4 (reuse these patterns):
- Section background alternation: bg-background → bg-[var(--brand-surface)] → bg-background
- Gradient CTA: `linear-gradient(135deg, var(--brand-gradient-from), var(--brand-gradient-to))`
- Glassmorphism card: `background: var(--brand-glass); backdrop-filter: blur(12px);`
- Eyebrow: `text-xs font-semibold uppercase tracking-widest text-[var(--brand-primary)]`
- All motion imports from "motion/react" — never "framer-motion"
- "use client" ONLY on animated leaf components — all page shells are Server Components
- Accordion already installed (shadcn/ui) from plan 04-01 (ServiceFaq.tsx)
</existing_patterns>
</context>

<tasks>

<task id="05-02-01" type="execute">
  <title>About Us page — hero, values, team, CTA</title>
  <read_first>
    - buildera-frontend/src/components/sections/industry/IndustryCta.tsx (gradient CTA pattern)
    - buildera-frontend/src/components/ui/Breadcrumb.tsx (breadcrumb pattern)
    - buildera-frontend/src/components/sections/HomeWhyBuildera.tsx (if exists — values framing)
  </read_first>
  <action>
    **1. Create `src/components/sections/trust/AboutHero.tsx`** — Server Component.
    - Full-width section `py-20 bg-background`.
    - Eyebrow: "ABOUT BUILDERA". H1: "We Build Software That Moves Indian Businesses Forward" (text-5xl font-bold tracking-tight).
    - Sub: "Buildera is an IT services company helping Indian SMBs replace manual processes and legacy systems with custom software that actually fits the way they work. We've shipped 150+ projects across 6 service lines since 2018."
    - Two trust badges inline: "150+ Projects Delivered" | "50+ Active Clients" | "6 Service Lines" — rendered as flex strip with `|` separators, `text-sm text-muted-foreground`.
    - Hero orb divs (same `hero-orb-1 hero-orb-2` classes as homepage hero).
    - No image placeholder — copy-only hero.

    **2. Create `src/components/sections/trust/ValuesSection.tsx`** — Server Component.
    - Background `bg-[var(--brand-surface)]`. `py-16`.
    - Eyebrow "HOW WE OPERATE", H2 "The Principles Behind Every Project".
    - 4-value grid `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6`:
      1. Client First — "We don't start building until we understand your real problem. Requirements change; we adapt." Icon: `IconUsers`.
      2. No Bullshit Timelines — "We give you honest estimates. If a scope change affects delivery, you hear it in the same call." Icon: `IconCalendar`.
      3. Built to Last — "We architect for the next 5 years, not the next demo. No shortcuts that become your problem 6 months later." Icon: `IconShield`.
      4. You Own Everything — "Full source code, no vendor lock-in, no black-box. Your IP is yours from day one." Icon: `IconCode`.
    - Card style: `bg-background border border-border rounded-xl p-6`. Icon in `w-10 h-10 rounded-lg bg-[var(--brand-primary)]/10 text-[var(--brand-primary)] flex items-center justify-center mb-4`. H4 title, body text `text-sm text-muted-foreground`.

    **3. Create `src/components/sections/trust/TeamSection.tsx`** — Server Component.
    - Background `bg-background`. `py-16`.
    - Eyebrow "THE TEAM", H2 "People Who Ship, Not Just Plan".
    - Sub: "Our core team of engineers, designers, and strategists has shipped software across manufacturing, retail, healthcare, and finance. Every client project has a dedicated PM, lead dev, and QA engineer."
    - 3 placeholder team cards `grid grid-cols-1 md:grid-cols-3 gap-6`:
      - Founder / Lead Architect — "12 years building enterprise software. Obsessed with clean architecture and honest client communication."
      - Head of Delivery — "Ex-consultant who joined to build, not to deck. Owns project timelines and client happiness."
      - Technical Lead — "Full-stack engineer who's shipped systems processing ₹500Cr+ in transactions. Prefers boring, reliable tech."
    - Card: `bg-[var(--brand-surface)] rounded-xl p-6 text-center`. Avatar placeholder: `w-20 h-20 rounded-full bg-[var(--brand-primary)]/10 mx-auto mb-4 flex items-center justify-center` with `IconUser` icon (48px). Name in `font-semibold`, role in `text-sm text-[var(--brand-primary)] mb-2`, bio in `text-sm text-muted-foreground`.

    **4. Create `src/components/sections/trust/AboutCta.tsx`** — Server Component.
    - Same gradient as ServiceCta (`linear-gradient(135deg, var(--brand-gradient-from), var(--brand-gradient-to))`).
    - Eyebrow "READY TO BUILD?", H2 "Let's Talk About Your Project". Sub: "15-minute intro call. No sales pitch. Just a conversation about what you're trying to build and whether we're the right fit."
    - Clutch-style trust signal: `⭐ 4.9/5 on Clutch • 150+ Projects Delivered • 98% Retention Rate`.
    - CTA button `bg-white text-[var(--brand-primary)] font-semibold` → `/book-a-call`.

    **5. Create `src/app/about/page.tsx`** — async Server Component.
    ```tsx
    import { Breadcrumb } from '@/components/ui/Breadcrumb'
    import { AboutHero } from '@/components/sections/trust/AboutHero'
    import { ValuesSection } from '@/components/sections/trust/ValuesSection'
    import { TeamSection } from '@/components/sections/trust/TeamSection'
    import { AboutCta } from '@/components/sections/trust/AboutCta'

    export const metadata = { title: 'About Buildera | IT Services Company', description: 'Learn who we are, how we work, and why 50+ Indian businesses trust Buildera to build their software.' }

    export default function AboutPage() {
      return (
        <main>
          <Breadcrumb items={[{ label: 'Home', href: '/' }, { label: 'About Us' }]} />
          <AboutHero />
          <ValuesSection />
          <TeamSection />
          <AboutCta />
        </main>
      )
    }
    ```
  </action>
  <acceptance_criteria>
    - /about loads with 4 sections: hero, values grid, team grid, gradient CTA
    - No "use client" on any component (all Server Components)
    - Breadcrumb renders Home > About Us
    - metadata exported with title and description
    - Hero orb classes present (for visual consistency with homepage)
  </acceptance_criteria>
</task>

<task id="05-02-02" type="execute">
  <title>How We Work page — animated 6-step process timeline</title>
  <read_first>
    - buildera-frontend/src/components/ui/SequentialSteps.tsx (existing step animation pattern)
    - buildera-frontend/src/components/sections/trust/AboutCta.tsx (just created — reuse for ProcessCta)
  </read_first>
  <action>
    **1. Create `src/components/ui/AnimatedTimeline.tsx`** — "use client".
    - Props: `{ steps: TimelineStep[] }` where `TimelineStep = { number: number; phase: string; title: string; description: string; duration: string; deliverables: string[] }`.
    - Single `useInView(ref, { once: true, margin: '-80px' })` ref on container div.
    - Each step renders as `motion.div` with `initial={{ opacity: 0, x: -20 }}`, `animate={isInView ? { opacity: 1, x: 0 } : {}}`, `transition={{ delay: i * 0.15, duration: 0.5, ease: 'easeOut' }}`.
    - Layout: left column is timeline (step number circle + vertical connector line); right column is content card.
    - Step circle: `w-12 h-12 rounded-full bg-[var(--brand-primary)] text-white font-bold text-xl flex items-center justify-center flex-shrink-0`.
    - Connector line (not after last step): `w-0.5 bg-border flex-grow ml-6 mt-2 min-h-[60px]`.
    - Content card: `bg-background border border-border rounded-xl p-6 flex-1 mb-6`.
      - Phase label: `text-xs font-semibold uppercase tracking-widest text-[var(--brand-primary)] mb-1`.
      - Title: `text-xl font-semibold mb-2`.
      - Duration badge: `inline-flex px-2 py-0.5 rounded-full bg-[var(--brand-surface)] text-xs text-muted-foreground mb-3`.
      - Description: `text-sm text-muted-foreground mb-4`.
      - Deliverables: `flex flex-wrap gap-2` — each as `text-xs px-3 py-1 rounded-full border border-border text-foreground`.
    - If `useReducedMotion()`, render static.
    - Import from "motion/react" — NOT "framer-motion".

    **2. Create `src/components/sections/trust/ProcessTimeline.tsx`** — Server Component (shell only; imports AnimatedTimeline client leaf).
    - Background `bg-background`. `py-20`.
    - Eyebrow "HOW WE WORK", H2 "From Discovery to Delivery — A Process That Actually Works".
    - Sub: "Most software projects fail because requirements are vague, timelines are fiction, and the client finds out too late. We've designed our process to eliminate all three."
    - Renders `<AnimatedTimeline steps={PROCESS_STEPS} />`.
    - `PROCESS_STEPS` hardcoded in this file (not in AnimatedTimeline):
      ```
      1. Discovery Call (Phase: WEEK 1, Duration: 30–60 min)
         "We learn your actual problem — not the symptom. No NDAs required for this call."
         Deliverables: Problem definition, Initial scope, Go/no-go decision

      2. Requirements Workshop (Phase: WEEK 1–2, Duration: 2–5 days)
         "We document every workflow, edge case, and integration before a line of code is written."
         Deliverables: Functional requirements doc, Wireframes, Tech stack recommendation

      3. Proposal & Timeline (Phase: WEEK 2, Duration: 1–2 days)
         "A fixed-price proposal with an honest timeline. No change-order surprises."
         Deliverables: Project proposal, Payment schedule, Kickoff date

      4. Build & Weekly Demos (Phase: WEEKS 3–10, Duration: Project-dependent)
         "You see working software every week — not a presentation. Feedback is built in."
         Deliverables: Weekly builds, Bug reports resolved in 48hrs, Mid-point review call

      5. Testing & QA (Phase: FINAL 2 WEEKS, Duration: 10–14 days)
         "We break it before your users do. UAT with your team before handoff."
         Deliverables: QA report, UAT sign-off, Load testing results

      6. Launch & Support (Phase: LAUNCH + 30 DAYS, Duration: Ongoing)
         "We stay on for 30 days post-launch. Your team gets training and documentation."
         Deliverables: Production deployment, Team training, 30-day support window
      ```

    **3. Create `src/components/sections/trust/ProcessCta.tsx`** — Server Component.
    - Background `bg-[var(--brand-surface)]`. `py-16`.
    - H2 "Ready to Start the Process?", sub "The Discovery Call is free. Book 30 minutes with our team."
    - Two CTAs: "Book a Free Call" → /book-a-call (primary), "See Our Work" → /case-studies (outline).
    - Trust strip: "No obligation • Free consultation • Response within 4 hours".

    **4. Create `src/app/how-we-work/page.tsx`** — Server Component.
    ```tsx
    export const metadata = { title: 'How We Work | Buildera', description: 'Our 6-step software delivery process from discovery to launch. No vague timelines, no change-order surprises.' }

    export default function HowWeWorkPage() {
      return (
        <main>
          <Breadcrumb items={[{ label: 'Home', href: '/' }, { label: 'How We Work' }]} />
          <ProcessTimeline />
          <ProcessCta />
        </main>
      )
    }
    ```
  </action>
  <acceptance_criteria>
    - AnimatedTimeline.tsx has "use client" directive
    - AnimatedTimeline imports from "motion/react" not "framer-motion"
    - useReducedMotion guard in AnimatedTimeline
    - /how-we-work loads with animated timeline (6 steps visible)
    - ProcessTimeline.tsx has NO "use client" — it's a Server Component shell
    - All 6 steps have phase label, duration badge, and deliverables chips
  </acceptance_criteria>
</task>

<task id="05-02-03" type="execute">
  <title>FAQ page with accordion + JSON-LD schema</title>
  <read_first>
    - buildera-frontend/src/components/sections/service/ServiceFaq.tsx (existing accordion pattern)
    - buildera-frontend/src/components/sections/trust/AboutCta.tsx (gradient CTA to reuse)
  </read_first>
  <action>
    **1. Create `src/components/sections/trust/FaqAccordion.tsx`** — Server Component (shadcn Accordion is already SSR-compatible).
    - Background `bg-background`. `py-20`.
    - Eyebrow "FAQ", H2 "Frequently Asked Questions".
    - Sub: "The questions we get on every sales call — answered honestly."
    - Use shadcn `Accordion type="single" collapsible` (same pattern as ServiceFaq.tsx from plan 04-01).
    - 12 FAQ items hardcoded (split into 2 columns on desktop with `grid grid-cols-1 lg:grid-cols-2 gap-x-12`):
      ```
      1. How long does a typical project take?
         "Depends entirely on scope. A focused CRM integration takes 3–4 weeks. A full custom ERP takes 4–6 months. We give you a specific timeline in the proposal — not a range."

      2. Do you work with fixed-price or time-and-material contracts?
         "Both, depending on project type. Well-defined projects (which we push for) are fixed-price. Ongoing retainer work is T&M. We'll recommend the right model after the discovery workshop."

      3. What happens if requirements change mid-project?
         "Changes happen — we plan for them. If a change affects timeline or budget, we tell you in the same meeting, not after we've built it. Small adjustments are absorbed; significant scope additions get a change order."

      4. Do I own the source code?
         "100%. Full source code, no lock-in, no licensing fees. You can take it in-house or use any developer to maintain it after delivery."

      5. What technologies do you use?
         "We choose the stack that fits the problem — not the one our team prefers. Typical stack: Next.js / React for frontend, Laravel / Node.js for backend, MySQL / PostgreSQL for data. Salesforce where CRM is the core need."

      6. How do I know the project is on track?
         "Weekly demo calls — you see working software, not slides. We use a shared project board you can access any time. No news is never good news with us; we over-communicate."

      7. What's included in post-launch support?
         "30 days of bug-fix support is included in every project. After that, we offer monthly retainer plans for ongoing support and feature development."

      8. Can you integrate with our existing systems (Tally, SAP, Zoho)?
         "Yes — integrations are one of our core strengths. We've connected systems to Tally, Zoho CRM, Salesforce, SAP, IndiaMart, and 30+ other platforms. We'll assess integration complexity in the requirements workshop."

      9. Do you sign NDAs?
         "Yes, before any detailed requirements conversation. Standard NDA template is available — most clients use ours, some bring their own."

      10. How do payments work?
          "Typically: 40% upfront, 40% at midpoint milestone, 20% on delivery. Payment schedule is outlined in the proposal. We accept bank transfer and UPI."

      11. Do you work with startups or only established businesses?
          "Both. We work with funded startups that need to ship an MVP, and with established businesses that need to replace legacy systems. The discovery process is the same either way."

      12. What makes Buildera different from a freelancer or offshore agency?
          "Accountability. A freelancer disappears after delivery. An offshore agency sends someone different every week. We're a dedicated team — same PM, same lead dev, same QA — from discovery to post-launch support."
      ```
    - Each `AccordionItem`: trigger `text-base font-semibold`, content `text-sm text-muted-foreground leading-relaxed`.

    **2. Create `src/app/faq/page.tsx`** — Server Component.
    - Add `FAQPage` JSON-LD script tag in `<head>` via Next.js `<Script>` tag with `strategy="beforeInteractive"` OR use `generateMetadata` + `<script type="application/ld+json">` as a JSX element in the page body:
      ```tsx
      const faqSchema = {
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "mainEntity": FAQ_ITEMS.map(faq => ({
          "@type": "Question",
          "name": faq.question,
          "acceptedAnswer": { "@type": "Answer", "text": faq.answer }
        }))
      }
      ```
    - Render as `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqSchema) }} />` inside `<main>`.
    - Page structure: Breadcrumb → FaqAccordion → AboutCta (reuse).
    - Export metadata: `{ title: 'FAQ | Buildera', description: 'Answers to the most common questions about working with Buildera — timelines, pricing, ownership, and more.' }`

    NOTE: Export FAQ_ITEMS array from `FaqAccordion.tsx` so the FAQ page can use it for both rendering and JSON-LD schema generation.
  </action>
  <acceptance_criteria>
    - /faq loads with 12 FAQ items in shadcn accordion
    - FAQPage JSON-LD script present in rendered HTML
    - FaqAccordion.tsx has NO "use client" (accordion is SSR-compatible)
    - FAQ_ITEMS exported from FaqAccordion.tsx for schema reuse
    - metadata exported with title and description
  </acceptance_criteria>
</task>

<task id="05-02-04" type="execute">
  <title>Privacy Policy + Terms of Service pages</title>
  <read_first>
    - buildera-frontend/src/app/about/page.tsx (page shell pattern)
    - buildera-frontend/src/components/ui/Breadcrumb.tsx
  </read_first>
  <action>
    **1. Create `src/app/privacy/page.tsx`** — Server Component.
    - Export metadata: `{ title: 'Privacy Policy | Buildera', description: 'How Buildera collects, uses, and protects your personal information.' }`
    - Breadcrumb: Home → Privacy Policy.
    - Content rendered in `<article className="container mx-auto px-8 max-w-4xl py-16 prose prose-slate max-w-none">`.
    - Use Tailwind typography via inline `<h2>`, `<h3>`, `<p>`, `<ul>` tags styled with Tailwind (NOT @tailwindcss/typography plugin — not installed).
    - Content sections (hardcoded):
      ```
      Last updated: January 2025

      1. Information We Collect
         - Contact information: name, email, phone number when you submit a form
         - Usage data: pages visited, time on site (via Google Analytics with consent)
         - Technical data: IP address, browser type, device type

      2. How We Use Your Information
         - Respond to your enquiries and provide requested services
         - Send project updates and relevant communications
         - Improve our website and service offerings
         - Comply with legal obligations

      3. Data Sharing
         We do not sell your personal data. We share data only with service providers
         necessary to operate our services (email delivery, analytics), bound by
         confidentiality agreements.

      4. Data Retention
         Contact form submissions retained for 3 years. You may request deletion at
         any time by emailing info@buildera.co.

      5. Cookies
         We use essential cookies for site functionality and analytics cookies with your
         consent. You can withdraw consent at any time via our cookie settings.

      6. Your Rights
         Under applicable data protection laws, you have the right to access, correct,
         delete, or object to processing of your personal data.

      7. Contact
         For privacy-related queries: info@buildera.co | Buildera, India
      ```
    - Style headings as `text-xl font-semibold mb-3 mt-8`, paragraphs as `text-muted-foreground mb-4 leading-relaxed`, lists as `list-disc pl-6 text-muted-foreground mb-4 space-y-1`.

    **2. Create `src/app/terms/page.tsx`** — Server Component.
    - Export metadata: `{ title: 'Terms of Service | Buildera', description: 'Terms governing the use of Buildera\'s website and services.' }`
    - Breadcrumb: Home → Terms of Service.
    - Same prose container style as privacy page.
    - Content sections:
      ```
      Last updated: January 2025

      1. Acceptance of Terms
         By accessing this website or engaging Buildera's services, you agree to these terms.

      2. Services
         Buildera provides custom software development, IT consulting, and related services.
         Specific deliverables, timelines, and payment terms are defined in individual project
         agreements or proposals.

      3. Intellectual Property
         Upon full payment, clients receive full ownership of custom-developed code and
         assets. Buildera retains rights to reusable frameworks, methodologies, and
         pre-existing components.

      4. Confidentiality
         Both parties agree to keep confidential any non-public information shared during
         the engagement. NDAs are available on request.

      5. Limitation of Liability
         Buildera's liability is limited to the fees paid for the specific project in which
         the issue arose. We are not liable for indirect, consequential, or incidental damages.

      6. Governing Law
         These terms are governed by the laws of India. Disputes subject to jurisdiction of
         courts in [City], India.

      7. Changes to Terms
         We may update these terms. Continued use of our services constitutes acceptance
         of updated terms.

      8. Contact
         For legal queries: info@buildera.co
      ```
  </action>
  <acceptance_criteria>
    - /privacy and /terms both load without errors
    - Both pages have breadcrumb navigation
    - Both export metadata with title and description
    - All text uses muted-foreground / foreground Tailwind classes — no inline styles
    - No "use client" on either page
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-02-01 | XSS | FAQ JSON-LD schema | mitigate | JSON.stringify encodes output; no user input in schema — all hardcoded copy |
| T-05-02-02 | Information Disclosure | Team section | accept | Placeholder team bios only — no real employee PII until client provides it |
</threat_model>

<verification>
- All 5 pages accessible: /about, /how-we-work, /faq, /privacy, /terms
- No "use client" on page shells or section components (only AnimatedTimeline)
- FAQPage JSON-LD present in /faq source
- npx tsc --noEmit passes in buildera-frontend/
- npm run build completes with no errors
</verification>

<success_criteria>
- All 5 trust pages load with full content
- About page builds trust: values, team section, gradient CTA
- How We Work has animated 6-step timeline (AnimatedTimeline client component)
- FAQ page has 12 items in accordion + JSON-LD schema
- Privacy and Terms have well-structured long-form content
- Every page ends with a path to /book-a-call
</success_criteria>

<output>
Create `.planning/phases/05-industries-trust-lead/05-02-SUMMARY.md` when done.
</output>
