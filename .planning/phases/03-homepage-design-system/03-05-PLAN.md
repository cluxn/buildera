---
phase: 03-homepage-design-system
plan: 05
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - buildera-frontend/src/components/sections/ServicesTabSection.tsx
  - buildera-frontend/src/components/ui/ServiceCard.tsx
  - buildera-frontend/src/components/sections/SolutionsGridSection.tsx
  - buildera-frontend/src/components/ui/SolutionTile.tsx
  - buildera-frontend/src/components/sections/ClientLogosMarquee.tsx
  - buildera-frontend/src/app/page.tsx
autonomous: true
requirements:
  - HOME-02
  - HOME-03
  - COMP-02
  - DESIGN-07
  - DESIGN-08

must_haves:
  truths:
    - "Services tab section renders 6 tab pills, all sub-services visible per active tab"
    - "Tab switching animates with AnimatePresence — old cards exit, new cards stagger in"
    - "Active tab has sliding underline indicator via layoutId"
    - "Sub-service cards show icon, title, description, 'Learn More' link, hover lift + glow"
    - "Solutions grid shows all 18 tiles in 4-column layout with diagonal stagger"
    - "Client logos marquee scrolls continuously via CSS, pauses on hover"
    - "Logos render in grayscale, color on hover"
  artifacts:
    - path: "buildera-frontend/src/components/sections/ServicesTabSection.tsx"
      provides: "Tab switcher with AnimatePresence card animation"
      contains: "use client"
    - path: "buildera-frontend/src/components/ui/ServiceCard.tsx"
      provides: "Individual sub-service card"
    - path: "buildera-frontend/src/components/sections/SolutionsGridSection.tsx"
      provides: "18-tile solutions grid with diagonal stagger"
    - path: "buildera-frontend/src/components/ui/SolutionTile.tsx"
      provides: "Individual solution tile component"
    - path: "buildera-frontend/src/components/sections/ClientLogosMarquee.tsx"
      provides: "Pure CSS marquee with placeholder logos"
  key_links:
    - from: "buildera-frontend/src/components/sections/ServicesTabSection.tsx"
      to: "buildera-frontend/src/components/ui/ServiceCard.tsx"
      via: "Renders ServiceCard for each sub-service in active tab"
      pattern: "ServiceCard"
    - from: "buildera-frontend/src/app/page.tsx"
      to: "buildera-frontend/src/components/sections/ServicesTabSection.tsx"
      via: "page.tsx renders ServicesTabSection after StatsBar"
      pattern: "ServicesTabSection"
---

<objective>
Build three homepage content sections: Services tab section with 6-tab animated switcher and sub-service cards, Solutions grid with 18 tiles and diagonal stagger animation, and Client logos marquee with CSS infinite scroll. Wire all three into page.tsx.

Purpose: Services section shows breadth of capability. Solutions section shows specific problems solved. Client logos provide social proof through recognized brand affiliations.
Output: ServicesTabSection, ServiceCard, SolutionsGridSection, SolutionTile, ClientLogosMarquee components. page.tsx updated with these 3 sections.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-design-system/03-01-SUMMARY.md
@.planning/phases/03-homepage-design-system/03-RESEARCH.md
@.planning/phases/03-homepage-design-system/03-UI-SPEC.md
@.planning/phases/03-homepage-design-system/03-PATTERNS.md
@.planning/phases/03-homepage-design-system/03-CONTEXT.md

<interfaces>
From buildera-frontend/src/app/globals.css (added in 03-01):
```css
.marquee-track { display: flex; width: max-content; animation: marquee 30s linear infinite; }
.marquee-track:hover { animation-play-state: paused; }
.logo-item { filter: grayscale(100%) opacity(0.6); transition: filter 0.3s ease, opacity 0.3s ease; }
.logo-item:hover { filter: grayscale(0%) opacity(1); }
```

SERVICES_MENU const exported from SiteNav.tsx (created in 03-02):
```typescript
const SERVICES_MENU = [
  { category: "Website Development", icon: "IconWorldWww", slug: "website-development",
    subServices: ["Custom Websites", "E-Commerce", "App Development", "PWA"] },
  // ... 5 more categories
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServicesTabSection + ServiceCard</name>
  <files>buildera-frontend/src/components/sections/ServicesTabSection.tsx, buildera-frontend/src/components/ui/ServiceCard.tsx</files>
  <read_first>
    - buildera-frontend/src/components/layout/SiteNav.tsx (SERVICES_MENU const definition — copy or import this constant)
    - .planning/phases/03-homepage-design-system/03-PATTERNS.md (Pattern Assignments 13, 14 — ServicesTabSection, ServiceCard)
    - .planning/phases/03-homepage-design-system/03-RESEARCH.md (Code Examples: AnimatePresence Tab Switcher section — key prop requirement, layoutId pattern, exit/enter animation values)
    - .planning/phases/03-homepage-design-system/03-UI-SPEC.md (Section 3 Services animation contract, Copywriting Contract: eyebrow "What We Build", H2 "Everything Your Business Needs, Built Right")
    - .planning/phases/03-homepage-design-system/03-CONTEXT.md (D-18 horizontal tab pills, D-19 all sub-services shown, D-20 card contents, D-21 tab switching animation)
  </read_first>
  <action>
    Create ServicesTabSection.tsx as a "use client" component:
    - Import useState from react
    - Import motion, AnimatePresence from motion/react
    - Import cn from @/lib/utils
    - Import ServiceCard from @/components/ui/ServiceCard
    - Import relevant Tabler icons for each category: IconWorldWww, IconCloud, IconSettings, IconRobot, IconCode, IconUsers from @tabler/icons-react

    Define SERVICES_DATA constant (same data as SERVICES_MENU in SiteNav.tsx but with additional description per sub-service). Each service category: category name, Tabler icon component reference (not string — actual component), slug, subServices array of objects with name, slug (kebab-case of name), and a 1-sentence description. Sub-service slugs derived from names (e.g., "Custom Websites" becomes "custom-websites").

    The 6 categories and their sub-services are locked per SERVICES_MENU in UI-SPEC.md. Each sub-service needs a brief 1-sentence description for the card. Write genuine, SMB-relevant descriptions.

    Component structure:
    - Section wrapper: py-16 lg:py-24 bg-background
    - Container: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
    - Section header with scroll reveal (motion.div whileInView): eyebrow label "What We Build" (text-xs font-medium uppercase tracking-widest text-[var(--brand-primary)] mb-3), H2 "Everything Your Business Needs, Built Right" (text-4xl font-bold tracking-tight text-foreground mb-4), descriptor paragraph
    - Tab bar: relative flex gap-2 overflow-x-auto snap-x pb-2 mb-8, with scrollbar-hidden styling. Each tab is a button with: cn() conditional classes, whitespace-nowrap snap-start min-h-[48px], active state text-[var(--brand-primary)], inactive state text-muted-foreground hover:text-foreground. Show the Tabler icon (size-4) next to category name on desktop.
    - Active tab underline: motion.div with layoutId="tab-indicator", absolute bottom-0 left-0 right-0 h-0.5 bg-[var(--brand-primary)], transition type spring stiffness 500 damping 40
    - Card grid below tabs: wrapped in AnimatePresence mode="wait". Inside: motion.div with key={activeTab} (CRITICAL — key must change per Pitfall 6). Grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4.
    - Exit animation on motion.div: exit opacity 0 scale 0.95 y 8, transition duration 0.15 ease easeIn
    - Each card wrapped in motion.div: initial opacity 0 scale 0.97 y 12, animate opacity 1 scale 1 y 0, transition delay index * 0.05 duration 0.25 ease easeOut
    - Each card renders ServiceCard with sub-service data
    - Footer link below grid: "Explore All Services" link to /services, text-sm font-medium text-[var(--brand-primary)] hover:underline

    Create ServiceCard.tsx as a component (rendered inside the "use client" ServicesTabSection, so it can use motion props):
    - Props: name (string), slug (string), parentSlug (string), description (string), icon (React component or element), className (optional string)
    - Render as a Link to /services/{parentSlug} (links to category page, not sub-service — sub-service pages are Phase 4)
    - Card structure: flex flex-col gap-3 p-4 rounded-lg border border-border bg-card
    - Hover: use CSS hover states — hover:shadow-[0_0_0_2px_var(--brand-primary)] hover:-translate-y-1 transition-all duration-200 (per D-20, D-26)
    - Content: icon at top (size-6 text-[var(--brand-primary)]), bold title (font-semibold text-foreground text-sm), 1-line description (text-muted-foreground text-xs line-clamp-1), "Learn More" text at bottom (text-xs font-medium text-[var(--brand-primary)] mt-auto)
    - Use cn() for class composition
  </action>
  <verify>
    <automated>cd buildera-frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <acceptance_criteria>
    - ServicesTabSection.tsx has "use client" as first line
    - ServicesTabSection uses useState for activeTab
    - ServicesTabSection uses AnimatePresence mode="wait" wrapping tab content
    - Tab content motion.div has key={activeTab} that changes per tab (Pitfall 6)
    - Active tab has motion.div with layoutId="tab-indicator" (DESIGN-08)
    - Tab bar has overflow-x-auto for mobile horizontal scroll (D-18)
    - All 6 service categories are represented as tabs
    - Each tab shows ALL sub-services (D-19 — no truncation)
    - Card stagger delay is index * 0.05 (50ms per card)
    - ServiceCard links to /services/{parentSlug}
    - ServiceCard has hover lift (-translate-y-1) and border glow (shadow-[0_0_0_2px_var(--brand-primary)])
    - All icons from @tabler/icons-react
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>ServicesTabSection renders 6-tab animated switcher with AnimatePresence cross-fade, layoutId sliding underline, and staggered ServiceCard reveals. All 6 service categories and their sub-services are visible with hover micro-interactions.</done>
</task>

<task type="auto">
  <name>Task 2: Create SolutionsGridSection + SolutionTile + ClientLogosMarquee + wire into page.tsx</name>
  <files>buildera-frontend/src/components/sections/SolutionsGridSection.tsx, buildera-frontend/src/components/ui/SolutionTile.tsx, buildera-frontend/src/components/sections/ClientLogosMarquee.tsx, buildera-frontend/src/app/page.tsx</files>
  <read_first>
    - buildera-frontend/src/app/page.tsx (current state after 03-04 — has Hero and StatsBar, placeholder comments for remaining sections)
    - buildera-frontend/src/app/globals.css (marquee-track, logo-item CSS classes)
    - .planning/phases/03-homepage-design-system/03-PATTERNS.md (Pattern Assignments 15, 16 — SolutionsGridSection, ClientLogosMarquee)
    - .planning/phases/03-homepage-design-system/03-UI-SPEC.md (Section 4 Solutions animation — diagonal cascade delay formula, Section 5 Client logos marquee animation, Copywriting Contract: eyebrows and H2s)
    - .planning/phases/03-homepage-design-system/03-CONTEXT.md (D-22 solutions grid spec, D-23 marquee spec)
    - .planning/REQUIREMENTS.md (SOL-03 for the 18 solution names)
  </read_first>
  <action>
    Create SolutionsGridSection.tsx as a Server Component (no "use client"):
    - Import motion from motion/react
    - Import SolutionTile from @/components/ui/SolutionTile
    - Import relevant Tabler icons for each solution (select appropriate icons from @tabler/icons-react for each of the 18 solutions)

    Define SOLUTIONS constant array with all 18 solutions from REQUIREMENTS.md SOL-03: Operations Management, Vendor Management, OTA Channel Partner, Supply Chain, Project Management, Accounting Management, Warehouse Management, Hotels & Resorts, Financial Management, Fleet Management, Airbnb & Vacation Rental, HR Management, Lead Management, Sales Management, CRM, India Mart Automation, ERP, Manufacturing/Production. Each with name, slug (kebab-case), and a Tabler icon component reference.

    Component structure:
    - Section wrapper: py-16 lg:py-24 bg-[var(--brand-surface)]
    - Container: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
    - Section header (motion.div with whileInView scroll reveal): eyebrow "Problems We Solve", H2 "Find the Solution to Your Problem"
    - Grid: grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 mt-12
    - Each solution wrapped in motion.div with diagonal stagger: initial opacity 0 y 20, whileInView opacity 1 y 0, viewport once true margin "-80px", transition delay (colIndex + rowIndex) * 0.05 duration 0.35 ease easeOut. Calculate colIndex as index % 4, rowIndex as Math.floor(index / 4).
    - Render SolutionTile inside each motion.div
    - Footer link: "View All Solutions" linking to /solutions

    Create SolutionTile.tsx as a Server Component:
    - Props: name (string), slug (string), icon (React element), className (optional)
    - Render as Link to /solutions/{slug}
    - Card: flex items-center gap-3 p-3 rounded-lg border border-border bg-card hover:shadow-[0_0_0_2px_var(--brand-primary)] hover:-translate-y-1 transition-all duration-200
    - Icon: size-5 text-[var(--brand-primary)]
    - Name: text-sm font-medium text-foreground
    - Use cn() for class composition

    Create ClientLogosMarquee.tsx as a Server Component (no "use client"):
    - No motion import — pure CSS animation (zero JS, D-23)
    - Define LOGOS constant array with 8 placeholder logo objects. Each logo is an inline SVG representing a generic company logo silhouette (abstract shapes — circles, rectangles, combined letterforms). Use neutral gray fills. Each has a name and SVG JSX element. Make them look like plausible company logos without copying any real brand. Size each SVG at roughly 120x40 viewBox.
    - Section wrapper: py-12 bg-background overflow-hidden
    - Section heading: text-center, eyebrow "Trusted by businesses across India" (text-sm text-muted-foreground font-medium mb-8)
    - Marquee container: overflow-hidden
    - Inner div with className="marquee-track" (CSS animation from globals.css)
    - Render logos TWICE inside marquee-track (duplication for seamless CSS loop): [...LOGOS, ...LOGOS].map
    - Each logo wrapped in div with className="logo-item mx-8 flex-shrink-0 flex items-center h-12"
    - aria-hidden="true" on the marquee container (decorative content)

    Update page.tsx to import and render the 3 new sections:
    - Add imports for ServicesTabSection, SolutionsGridSection, ClientLogosMarquee
    - Insert after StatsBarSection in the locked order: ServicesTabSection, SolutionsGridSection, ClientLogosMarquee
    - Keep placeholder comments for WhyBuildera, Testimonials, CaseStudies, CTA (added in 03-06)
  </action>
  <verify>
    <automated>cd buildera-frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - SolutionsGridSection.tsx is NOT "use client" — Server Component
    - SolutionsGridSection defines 18 solutions matching SOL-03 names
    - Solutions grid uses lg:grid-cols-4 layout
    - Diagonal stagger uses delay formula (colIndex + rowIndex) * 0.05
    - SolutionTile links to /solutions/{slug}
    - SolutionTile has hover lift and border glow
    - ClientLogosMarquee.tsx is NOT "use client" — Server Component
    - ClientLogosMarquee uses CSS class "marquee-track" (no motion import)
    - Logos are rendered twice in the marquee-track div (for seamless loop)
    - Each logo div has class "logo-item" (grayscale filter from CSS)
    - page.tsx renders sections in locked order: Hero, StatsBar, ServicesTab, SolutionsGrid, ClientLogos
    - npm run build exits 0
  </acceptance_criteria>
  <done>SolutionsGridSection renders 18 solution tiles with diagonal cascade stagger animation. ClientLogosMarquee renders 8 placeholder logos in seamless CSS infinite scroll with grayscale-to-color hover. page.tsx updated with all 3 new sections in locked order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | All content in this plan is hardcoded — no API data consumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | Hardcoded service/solution links | accept | All link destinations are hardcoded relative paths; no user input |
| T-03-14 | Tampering | SVG logo placeholders | accept | All SVGs are hardcoded inline JSX; no external sources; no user data in SVG attributes |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
cd buildera-frontend && npm run build
# Validates: All section components compile, Server/Client boundaries correct, page.tsx composition valid
</verification>

<success_criteria>
- Services tab section shows 6 clickable tabs with animated switching
- Tab content swaps with AnimatePresence cross-fade + staggered card entry
- Active tab has sliding underline indicator
- Solutions grid shows 18 tiles in 4-column layout on desktop
- Tiles animate in with diagonal cascade on scroll
- Logo marquee scrolls continuously, pauses on hover
- Logos display in grayscale, color on hover
- All sections appear in correct order on homepage
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/03-homepage-design-system/03-05-SUMMARY.md` when done
</output>
