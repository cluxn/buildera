---
phase: 03-homepage-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - buildera-frontend/src/app/globals.css
  - buildera-frontend/src/app/layout.tsx
  - buildera-frontend/src/lib/api.ts
  - buildera-frontend/src/app/not-found.tsx
  - buildera-frontend/src/app/error.tsx
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05
  - INFRA-03
  - NAV-08

must_haves:
  truths:
    - "Inter font renders as the global typeface (not system sans-serif)"
    - "Brand tokens produce visible color when used as bg-[var(--brand-primary)]"
    - "glass-card and glass-nav utility classes apply backdrop-filter blur"
    - "Dark mode brand tokens activate under .dark class"
    - "Skip-to-content link is visible on keyboard focus"
    - "not-found.tsx and error.tsx render with navigation links"
    - "shadcn Badge, Card, Tabs, Separator components are installed"
  artifacts:
    - path: "buildera-frontend/src/app/globals.css"
      provides: "Brand tokens, glassmorphism utilities, animation keyframes, marquee CSS"
      contains: "--brand-primary: hsl(217 91% 60%)"
    - path: "buildera-frontend/src/app/layout.tsx"
      provides: "Skip-to-content link, main wrapper with id"
      contains: "skip-to-content"
    - path: "buildera-frontend/src/lib/api.ts"
      provides: "fetchNavItems, fetchFooterLinks, fetchSettings typed helpers"
      exports: ["fetchNavItems", "fetchFooterLinks", "fetchSettings", "NavItem", "FooterLink", "Settings"]
    - path: "buildera-frontend/src/components/ui/badge.tsx"
      provides: "shadcn Badge component"
    - path: "buildera-frontend/src/components/ui/card.tsx"
      provides: "shadcn Card component"
  key_links:
    - from: "buildera-frontend/src/app/globals.css"
      to: "buildera-frontend/src/app/layout.tsx"
      via: "--font-sans: var(--font-inter) activates Inter loaded in layout.tsx"
      pattern: "--font-sans.*--font-inter"
    - from: "buildera-frontend/src/lib/api.ts"
      to: "GET /api/settings"
      via: "fetchSettings with cache tag"
      pattern: "fetchSettings.*tags.*settings"
---

<objective>
Establish the complete design system foundation for Phases 3-10: fix the broken Inter font wiring, add 7 brand tokens to :root, define glassmorphism utility classes, add all CSS keyframe animations (orbs, marquee, shimmer, gradient pulse, SVG pulse), install 4 shadcn components, add typed API fetch helpers, wire skip-to-content accessibility, and update error pages with navigation.

Purpose: Every subsequent plan (03-02 through 03-06 and all Phases 4-10) depends on these tokens, utilities, and API helpers existing. This is the foundational layer.
Output: globals.css with full design system, api.ts with 3 typed helpers, layout.tsx with skip-to-content, 4 shadcn components installed, error pages updated.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-design-system/03-RESEARCH.md
@.planning/phases/03-homepage-design-system/03-UI-SPEC.md
@.planning/phases/03-homepage-design-system/03-PATTERNS.md
@.planning/phases/03-homepage-design-system/03-CONTEXT.md

<interfaces>
From buildera-frontend/src/lib/api.ts (current state):
```typescript
export class ApiError extends Error {
  constructor(public readonly status: number, message: string)
}
export async function fetchFromApi<T>(path: string, options?: RequestInit): Promise<T>
```

From buildera-frontend/src/app/layout.tsx (current state):
```typescript
const inter = Inter({ subsets: ["latin"], variable: "--font-inter", display: "swap" });
// className={inter.variable} on <html> element
```

From buildera-frontend/src/app/globals.css line 10 (the bug):
```css
--font-sans: var(--font-sans); /* circular self-reference */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components + fix globals.css with complete design system</name>
  <files>buildera-frontend/src/app/globals.css, buildera-frontend/src/components/ui/badge.tsx, buildera-frontend/src/components/ui/card.tsx, buildera-frontend/src/components/ui/tabs.tsx, buildera-frontend/src/components/ui/separator.tsx</files>
  <read_first>
    - buildera-frontend/src/app/globals.css (current state — identify line 10 self-reference bug, :root block end, .dark block end, @layer base block end)
    - buildera-frontend/src/components/ui/button.tsx (reference pattern for shadcn base-nova component structure)
    - buildera-frontend/components.json (confirm style: base-nova, iconLibrary mismatch)
    - .planning/phases/03-homepage-design-system/03-UI-SPEC.md (Color Contract section, Glassmorphism Utility Contract, Animation Contract)
    - .planning/phases/03-homepage-design-system/03-PATTERNS.md (Pattern Assignment 1: globals.css — exact tokens and utility blocks to add)
    - .planning/phases/03-homepage-design-system/03-CONTEXT.md (D-08 brand tokens, D-09 dark mode, D-10 glassmorphism, D-11 font fix, D-02 orb keyframes, D-23 marquee)
  </read_first>
  <action>
    Step 1: Install 4 shadcn components by running each command sequentially in buildera-frontend directory:
    - npx shadcn add badge
    - npx shadcn add card
    - npx shadcn add tabs
    - npx shadcn add separator
    After each install, inspect the generated file for any lucide-react imports. Replace lucide icon imports with @tabler/icons-react equivalents (ChevronDown to IconChevronDown, X to IconX, Check to IconCheck per SP-5 in PATTERNS.md).

    Step 2: Fix the font wiring bug in globals.css. On line 10 inside the @theme inline block, change the self-referential --font-sans: var(--font-sans) to --font-sans: var(--font-inter) per D-11. The --font-inter CSS variable is injected by next/font/google via className={inter.variable} on the html element in layout.tsx.

    Step 3: Add 7 brand tokens to the :root block in globals.css, AFTER the existing shadcn tokens (after --sidebar-ring line) but BEFORE the closing brace. Tokens per D-08:
    --brand-primary: hsl(217 91% 60%), --brand-primary-dark: hsl(217 91% 45%), --brand-primary-light: hsl(217 91% 75%), --brand-gradient-from: hsl(217 91% 60%), --brand-gradient-to: hsl(242 75% 40%), --brand-surface: hsl(217 60% 97%), --brand-glass: hsl(217 91% 60% / 8%)

    Step 4: Add dark mode brand tokens to the .dark block in globals.css, AFTER existing dark shadcn tokens (after --sidebar-ring line) but BEFORE closing brace. Dark tokens per D-09:
    --brand-primary: hsl(217 91% 65%), --brand-primary-dark: hsl(217 91% 55%), --brand-primary-light: hsl(217 91% 80%), --brand-surface: hsl(217 30% 12%), --brand-glass: hsl(217 91% 65% / 12%). Gradient tokens unchanged in dark mode.

    Step 5: Add utility classes AFTER the @layer base block (after line 130). Use Tailwind 4 @utility directive. Define glass-card utility (background: var(--brand-glass), backdrop-filter: blur(12px), -webkit-backdrop-filter: blur(12px), border: 1px solid hsl(217 91% 60% / 15%), border-radius: var(--radius-lg)). Define glass-nav utility (background: hsl(0 0% 100% / 80%), backdrop-filter: blur(12px), -webkit-backdrop-filter: blur(12px), border-bottom: 1px solid var(--brand-glass)).

    Step 6: Add CSS keyframe animations AFTER the utility blocks. Include: @keyframes orb-float (translate + scale per D-02), .hero-orb-1 and .hero-orb-2 classes (positioned absolute, radial-gradient backgrounds, blur 80-100px, 8-10s animation), @keyframes svg-pulse (translateY 0 to -12px per D-06), .hero-illustration class (6s alternate), @keyframes marquee (translateX 0 to -50% per D-23), .marquee-track class (display flex, width max-content, animation marquee 30s linear infinite), .marquee-track:hover (animation-play-state paused), .logo-item class (grayscale 100% opacity 0.6, transition 0.3s), .logo-item:hover (grayscale 0% opacity 1), @keyframes gradient-pulse (opacity 0.85 to 1), .cta-button-shimmer::after (linear-gradient shimmer effect), .cta-button-shimmer:hover::after (translateX -100% to 100%, transition 0.5s). All exact values are in PATTERNS.md Pattern Assignment 1.
  </action>
  <verify>
    <automated>cd buildera-frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <acceptance_criteria>
    - globals.css line 10 reads --font-sans: var(--font-inter) (not var(--font-sans))
    - globals.css :root block contains --brand-primary: hsl(217 91% 60%) and all 6 other brand tokens
    - globals.css .dark block contains --brand-primary: hsl(217 91% 65%) and other dark overrides
    - globals.css contains @utility glass-card with backdrop-filter: blur(12px)
    - globals.css contains @utility glass-nav with background: hsl(0 0% 100% / 80%)
    - globals.css contains @keyframes orb-float, @keyframes marquee, @keyframes gradient-pulse, @keyframes svg-pulse
    - globals.css contains .hero-orb-1, .hero-orb-2, .marquee-track, .logo-item, .cta-button-shimmer classes
    - buildera-frontend/src/components/ui/badge.tsx exists
    - buildera-frontend/src/components/ui/card.tsx exists
    - buildera-frontend/src/components/ui/tabs.tsx exists
    - buildera-frontend/src/components/ui/separator.tsx exists
    - No lucide-react imports exist in any of the 4 installed shadcn component files (grep -r "lucide" src/components/ui/ returns no results for these files)
    - npm run build exits 0
  </acceptance_criteria>
  <done>globals.css contains the complete design system (font fix, 7 brand tokens in :root, dark mode overrides, glass-card and glass-nav utilities, all 4 keyframe animation blocks with their CSS classes). All 4 shadcn components installed with lucide icons replaced by tabler equivalents.</done>
</task>

<task type="auto">
  <name>Task 2: Add API helpers to api.ts + update layout.tsx + update error pages</name>
  <files>buildera-frontend/src/lib/api.ts, buildera-frontend/src/app/layout.tsx, buildera-frontend/src/app/not-found.tsx, buildera-frontend/src/app/error.tsx</files>
  <read_first>
    - buildera-frontend/src/lib/api.ts (current state — fetchFromApi signature, ApiError class)
    - buildera-frontend/src/app/layout.tsx (current state — Inter setup, body structure)
    - buildera-frontend/src/app/not-found.tsx (current state — to update with nav links and design system styling)
    - buildera-frontend/src/app/error.tsx (current state — to update with nav links and design system styling)
    - .planning/phases/03-homepage-design-system/03-PATTERNS.md (Pattern Assignment 2: layout.tsx additions, Pattern Assignment 3: api.ts type definitions and helpers)
    - .planning/phases/03-homepage-design-system/03-RESEARCH.md (Code Examples: API Fetch Helpers section)
    - .planning/CLIENT-CONTEXT.md (contact details for SETTINGS_FALLBACK)
  </read_first>
  <action>
    Step 1: Extend api.ts with type definitions and helpers. Add AFTER the existing fetchFromApi function:
    - Export interface NavItem with fields: id (number), label (string), url (string), group ('solutions' | 'work' | 'resources'), display_order (number)
    - Export interface FooterLink with fields: id (number), label (string), url (string), column ('services' | 'solutions' | 'company' | 'resources'), display_order (number)
    - Export interface Settings with fields: company_name, email, phone, address, calendly_url, whatsapp_number, whatsapp_enabled (boolean), linkedin_url, instagram_url, twitter_url, footer_tagline, stat_projects, stat_clients, stat_years, stat_satisfaction (all string except whatsapp_enabled)
    - Define const SETTINGS_FALLBACK of type Settings with values from CLIENT-CONTEXT.md: company_name 'Buildera', email 'info@buildera.co', phone '+91 82994 06767', address '117/Q/457/10A Indrapuri Sharda Nagar, Kanpur 208025', whatsapp_number '+918299406767', whatsapp_enabled false, footer_tagline 'Building technology that grows businesses.', stat_projects '150', stat_clients '50', stat_years '6', stat_satisfaction '98', all social URLs empty string, calendly_url empty string. Export SETTINGS_FALLBACK.
    - Add fetchNavItems(): returns fetchFromApi NavItem array with next tags ['nav-items'], catch returns empty array
    - Add fetchFooterLinks(): returns fetchFromApi FooterLink array with next tags ['footer-links'], catch returns empty array
    - Add fetchSettings(): returns fetchFromApi Settings with next tags ['settings'], catch returns SETTINGS_FALLBACK

    Step 2: Update layout.tsx. Do NOT add SiteNav/SiteFooter imports yet (those components do not exist until plans 03-02 and 03-03). Add skip-to-content link and main wrapper only:
    - Add a skip-to-content anchor element as the first child of body: a href="#main-content" with classes sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-50 focus:bg-[var(--brand-primary)] focus:text-white focus:px-4 focus:py-2 focus:rounded-lg, text "Skip to content"
    - Wrap {children} in a main element with id="main-content"

    Step 3: Update not-found.tsx to use brand design system styling. Add a centered layout with: H1 "Page not found" using text-4xl font-bold tracking-tight, descriptive text using text-muted-foreground, a link back to homepage "/" styled with text-[var(--brand-primary)] hover:underline, and secondary links to /services and /contact. Import Link from next/link for navigation.

    Step 4: Update error.tsx to use brand design system styling. Keep the "use client" directive. Add centered layout with: H1 "Something went wrong" using text-4xl font-bold tracking-tight, descriptive text, a "Try again" button that calls reset(), and a link back to homepage. Use Button component from @/components/ui/button for the reset action.
  </action>
  <verify>
    <automated>cd buildera-frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - api.ts exports NavItem, FooterLink, Settings interfaces
    - api.ts exports SETTINGS_FALLBACK constant with company_name 'Buildera' and phone '+91 82994 06767'
    - api.ts exports fetchNavItems, fetchFooterLinks, fetchSettings functions
    - Each fetch helper uses .catch(() => fallback) pattern — fetchNavItems catches to [], fetchFooterLinks catches to [], fetchSettings catches to SETTINGS_FALLBACK
    - Each fetch helper passes next: { tags: [...] } for ISR cache tagging
    - layout.tsx contains a element with href="#main-content" and class sr-only
    - layout.tsx contains main element with id="main-content"
    - not-found.tsx contains link to "/" homepage
    - error.tsx contains "use client" directive and reset() call
    - npm run build exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>api.ts has 3 typed interfaces, SETTINGS_FALLBACK constant, and 3 fetch helpers with ISR cache tags and error fallbacks. layout.tsx has skip-to-content accessibility link and main landmark wrapper. not-found.tsx and error.tsx render with design system styling and navigation links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time fetch | Next.js fetches from Laravel API during build — untrusted external response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | globals.css brand tokens | accept | CSS variables are static build artifacts; no user input involved |
| T-03-02 | Information Disclosure | SETTINGS_FALLBACK in api.ts | accept | Fallback contains only public company contact info (phone, email, address) — no secrets |
| T-03-03 | Tampering | API response shapes | mitigate | TypeScript interfaces enforce expected shapes; .catch() fallbacks prevent crash on malformed response |
| T-03-SC | Tampering | npx shadcn add installs | accept | shadcn CLI scaffolds from official registry; no new npm packages installed to node_modules |
</threat_model>

<verification>
cd buildera-frontend && npm run build
# Build must exit 0 — validates TypeScript, font wiring, imports, and all component files
</verification>

<success_criteria>
- Inter font renders correctly (not system sans-serif) when dev server is running
- bg-[var(--brand-primary)] produces visible blue (#3B82F6 equivalent) in browser
- glass-card class applies backdrop-filter blur when used on an element
- 4 shadcn components (badge, card, tabs, separator) are installed and importable
- 3 API fetch helpers (fetchNavItems, fetchFooterLinks, fetchSettings) are exported from api.ts
- Skip-to-content link appears on keyboard Tab press
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/03-homepage-design-system/03-01-SUMMARY.md` when done
</output>
