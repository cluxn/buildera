---
phase: 03-homepage-design-system
plan: 06
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - buildera-frontend/src/components/sections/WhyBuilderaSection.tsx
  - buildera-frontend/src/components/ui/AnimatedRingStat.tsx
  - buildera-frontend/src/components/ui/FeatureCheckList.tsx
  - buildera-frontend/src/components/ui/MiniMetricsCard.tsx
  - buildera-frontend/src/components/ui/AnimatedBarChart.tsx
  - buildera-frontend/src/components/sections/TestimonialsSection.tsx
  - buildera-frontend/src/components/ui/TestimonialCard.tsx
  - buildera-frontend/src/components/sections/CaseStudiesPreview.tsx
  - buildera-frontend/src/components/ui/CaseStudyPreviewCard.tsx
  - buildera-frontend/src/components/sections/CTASection.tsx
  - buildera-frontend/src/app/page.tsx
autonomous: true
requirements:
  - HOME-05
  - HOME-06
  - HOME-07
  - COMP-03
  - COMP-06
  - DESIGN-01

must_haves:
  truths:
    - "Why Buildera section renders 3-column layout with ring stat, feature comparison, and metrics card"
    - "SVG ring draws itself on scroll into view"
    - "Feature comparison shows Buildera vs In-House vs Freelancer with animated checkmarks"
    - "Bar chart bars grow from bottom with spring animation on scroll"
    - "Testimonials section renders 3 glassmorphism cards with depth stagger"
    - "Testimonial cards show star rating, quote, name, company"
    - "Case studies preview shows 3 cards with animated result metrics"
    - "Bottom CTA section has gradient background with shimmer button"
    - "All sections animate on scroll with whileInView + viewport once"
    - "page.tsx imports all 9 homepage sections (HeroSection, StatsBarSection from 03-04; ServicesTabSection, SolutionsGridSection, ClientLogosMarquee from 03-05; WhyBuilderaSection, TestimonialsSection, CaseStudiesPreview, CTASection from this plan) and compiles with npm run build exit 0"
  artifacts:
    - path: "buildera-frontend/src/components/sections/WhyBuilderaSection.tsx"
      provides: "3-column differentiation section shell"
    - path: "buildera-frontend/src/components/ui/AnimatedRingStat.tsx"
      provides: "SVG ring draw animation + percentage counter"
      contains: "use client"
    - path: "buildera-frontend/src/components/ui/FeatureCheckList.tsx"
      provides: "Comparison table with animated checkmarks"
      contains: "use client"
    - path: "buildera-frontend/src/components/ui/MiniMetricsCard.tsx"
      provides: "Bar chart + project outcome feed"
      contains: "use client"
    - path: "buildera-frontend/src/components/sections/TestimonialsSection.tsx"
      provides: "3 glassmorphism testimonial cards"
    - path: "buildera-frontend/src/components/sections/CTASection.tsx"
      provides: "Gradient CTA with shimmer button"
    - path: "buildera-frontend/src/app/page.tsx"
      provides: "Complete homepage composition — all 9 sections assembled"
      exports: ["default"]
  key_links:
    - from: "buildera-frontend/src/app/page.tsx"
      to: "All section components in this plan + 03-04 + 03-05"
      via: "page.tsx imports and renders all 9 sections in locked narrative order"
      pattern: "HeroSection|StatsBarSection|ServicesTabSection|SolutionsGridSection|ClientLogosMarquee|WhyBuilderaSection|TestimonialsSection|CaseStudiesPreview|CTASection"
    - from: "buildera-frontend/src/app/page.tsx"
      to: "buildera-frontend/src/lib/api.ts"
      via: "fetchSettings() called at build time to pass settings to StatsBarSection"
      pattern: "fetchSettings"
---

<objective>
Build the final 4 homepage sections (Why Buildera, Testimonials, Case Studies Preview, Bottom CTA) and assemble the complete page.tsx by importing ALL 9 homepage sections: HeroSection and StatsBarSection from plan 03-04, ServicesTabSection, SolutionsGridSection, and ClientLogosMarquee from plan 03-05, and the 4 sections created here.

Purpose: These sections complete the homepage narrative arc — differentiation, social validation, evidence, and conversion action. page.tsx is assembled exclusively here to eliminate Wave 3 write conflicts.
Output: 10 component files (4 sections + 6 UI components), page.tsx fully composed with all 9 sections in locked narrative order.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-design-system/03-01-SUMMARY.md
@.planning/phases/03-homepage-design-system/03-RESEARCH.md
@.planning/phases/03-homepage-design-system/03-UI-SPEC.md
@.planning/phases/03-homepage-design-system/03-PATTERNS.md
@.planning/phases/03-homepage-design-system/03-CONTEXT.md

<interfaces>
From buildera-frontend/src/app/globals.css (added in 03-01):
```css
@utility glass-card {
  background: var(--brand-glass);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid hsl(217 91% 60% / 15%);
  border-radius: var(--radius-lg);
}
.cta-button-shimmer::after { /* shimmer gradient effect */ }
.cta-button-shimmer:hover::after { /* translateX animation */ }
```

Sections created in 03-04 (available for import):
- src/components/sections/HeroSection.tsx — exports HeroSection (Server Component)
- src/components/sections/StatsBarSection.tsx — exports StatsBarSection, accepts Settings prop

Sections created in 03-05 (available for import):
- src/components/sections/ServicesTabSection.tsx — exports ServicesTabSection
- src/components/sections/SolutionsGridSection.tsx — exports SolutionsGridSection
- src/components/sections/ClientLogosMarquee.tsx — exports ClientLogosMarquee
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WhyBuilderaSection + AnimatedRingStat + FeatureCheckList + MiniMetricsCard + AnimatedBarChart</name>
  <files>buildera-frontend/src/components/sections/WhyBuilderaSection.tsx, buildera-frontend/src/components/ui/AnimatedRingStat.tsx, buildera-frontend/src/components/ui/FeatureCheckList.tsx, buildera-frontend/src/components/ui/MiniMetricsCard.tsx, buildera-frontend/src/components/ui/AnimatedBarChart.tsx</files>
  <read_first>
    - .planning/phases/03-homepage-design-system/03-PATTERNS.md (Pattern Assignments 19-22 — WhyBuilderaSection, AnimatedRingStat, FeatureCheckList, AnimatedBarChart)
    - .planning/phases/03-homepage-design-system/03-RESEARCH.md (Code Examples: SVG Ring Draw Animation, Don't Hand-Roll section)
    - .planning/phases/03-homepage-design-system/03-UI-SPEC.md (Section 6 Why Buildera animation contract — 3 column specs, Column 1/2/3 detailed layouts, Copywriting Contract: eyebrow "Why Choose Buildera", H2 "Why Smart Businesses Choose Buildera")
    - .planning/phases/03-homepage-design-system/03-CONTEXT.md (COMP-06 spec)
  </read_first>
  <action>
    Create AnimatedRingStat.tsx as a "use client" component:
    - Import motion, useReducedMotion from motion/react
    - Props: percentage (number 0-100), label (string)
    - Use useReducedMotion to gate animation
    - Render a div with flex flex-col items-center gap-4
    - SVG: width 120, height 120, viewBox 0 0 120 120
    - Track circle: cx 60, cy 60, r 52, fill none, stroke hsl(217 91% 60% / 15%), strokeWidth 8
    - Animated fill circle: motion.circle with cx 60, cy 60, r 52, fill none, stroke var(--brand-primary), strokeWidth 8, strokeLinecap round, strokeDasharray 326.7 (2*pi*52), initial pathLength 0 opacity 0, whileInView pathLength (percentage/100 or 1 if reduced motion) opacity 1, viewport once true, transition duration 1.5 (0 if reduced) ease easeInOut delay 0.3, style rotate -90 transformOrigin "60px 60px"
    - Center text element: x 60, y 60, textAnchor middle, dominantBaseline central, text-2xl font-bold fill-foreground, content "{percentage}%"
    - Label p below SVG: text-sm font-medium text-muted-foreground
    - Below the ring: 3 checklist items with motion.div stagger. Items: "Dedicated project manager", "Weekly progress updates", "Post-launch support included". Each with IconCheck from @tabler/icons-react (size-4 text-[var(--brand-primary)]) and text. Stagger: whileInView with delay index * 0.15 after 0.8s initial delay.

    Create FeatureCheckList.tsx as a "use client" component:
    - Import motion from motion/react
    - Import IconCheck, IconX from @tabler/icons-react
    - Import cn from @/lib/utils
    - Define COMPARISON_ROWS constant: 5 rows per UI-SPEC Column 2 spec — Cost Effective (B:true, IH:false, FL:true), Delivery Speed (B:true, IH:false, FL:false), Communication (B:true, IH:false, FL:true), Accountability (B:true, IH:true, FL:false), Expertise Depth (B:true, IH:true, FL:false)
    - Render card with p-6 rounded-lg border border-border bg-card
    - Header row: feature label, "Buildera", "In-House", "Freelancer" column headers
    - H3: "Buildera vs. the alternatives" (font-semibold text-foreground mb-4)
    - Each row is motion.div with grid grid-cols-4 gap-2 items-center text-sm, initial opacity 0 x -8, whileInView opacity 1 x 0, viewport once true, transition delay index * 0.15 type spring stiffness 400 damping 25
    - Each check/x icon wrapped in motion.span: initial scale 0 opacity 0, whileInView scale 1 opacity 1, viewport once true, transition delay (row_index * 0.15 + cell_index * 0.05) type spring stiffness 400 damping 25
    - Buildera checkmarks: text-[var(--brand-primary)]. Others: text-muted-foreground.

    Create AnimatedBarChart.tsx as a "use client" component:
    - Import motion from motion/react
    - Props: bars (array of { height: number 0-100, label?: string })
    - Render flex items-end gap-2 h-24 container
    - Each bar: motion.div with w-full bg-[var(--brand-primary)] rounded-t, style originY 1 (grows from bottom), initial scaleY 0, whileInView scaleY (bar.height / 100), viewport once true, transition delay index * 0.1 type spring stiffness 120 damping 20
    - Height of each bar div: h-full (parent container is h-24, scaleY controls visible height)
    - Label below each bar: text-[10px] text-muted-foreground

    Create MiniMetricsCard.tsx as a "use client" component:
    - Import motion from motion/react
    - Import AnimatedBarChart from ./AnimatedBarChart
    - Render card with p-6 rounded-lg border border-border bg-card
    - H3: "Typical Project Impact" (font-semibold text-foreground mb-4)
    - AnimatedBarChart with 4 bars: heights [40, 65, 80, 92], labels ["Q1", "Q2", "Q3", "Q4"]
    - Separator/divider below chart: border-t border-border my-4
    - Project outcome feed: 3 items, each motion.div with initial opacity 0 y 8, whileInView opacity 1 y 0, viewport once true, transition delay (0.6 + index * 0.2) duration 0.4
    - Items: "Warehouse Mgmt System" with "40% faster ops", "Salesforce CRM Rollout" with "3x lead pipeline", "E-Commerce Platform" with "60% more conversions"
    - Each item: flex items-start gap-2, small colored dot (w-1.5 h-1.5 rounded-full bg-[var(--brand-primary)] mt-1.5), project name (font-medium text-sm text-foreground), result text (text-xs text-muted-foreground)

    Create WhyBuilderaSection.tsx as a Server Component (no "use client"):
    - Import motion from motion/react
    - Import AnimatedRingStat, FeatureCheckList, MiniMetricsCard from @/components/ui/
    - Section wrapper: py-16 lg:py-24 bg-background
    - Container: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
    - Section header (motion.div with scroll reveal): eyebrow "Why Choose Buildera", H2 "Why Smart Businesses Choose Buildera"
    - Grid: grid md:grid-cols-3 gap-8 mt-12
    - 3 columns, each wrapped in motion.div with initial opacity 0 y 24, whileInView opacity 1 y 0, viewport once true, transition delay (colIndex * 0.2) duration 0.5
    - Column 1: AnimatedRingStat with percentage 98, label "Client Satisfaction"
    - Column 2: FeatureCheckList
    - Column 3: MiniMetricsCard
  </action>
  <verify>
    <automated>cd buildera-frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <acceptance_criteria>
    - AnimatedRingStat.tsx has "use client", renders SVG ring with motion.circle pathLength animation
    - AnimatedRingStat respects useReducedMotion (duration 0 when true)
    - Ring track stroke is hsl(217 91% 60% / 15%), fill stroke is var(--brand-primary)
    - 3 checklist items below ring with IconCheck from @tabler/icons-react
    - FeatureCheckList.tsx has "use client", renders 5 comparison rows with animated checkmarks
    - Buildera column shows all 5 checks in brand-primary color
    - Spring animation: stiffness 400, damping 25
    - AnimatedBarChart.tsx has "use client", renders bars with scaleY from 0, originY 1 (bottom)
    - MiniMetricsCard.tsx has "use client", contains AnimatedBarChart + 3 project outcome items
    - WhyBuilderaSection.tsx is NOT "use client" — Server Component shell
    - 3 columns stagger with delay 0, 0.2, 0.4
    - No lucide-react imports in any file
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>WhyBuilderaSection renders 3-column differentiation layout. Column 1: animated SVG ring stat (98% client satisfaction) with checklist. Column 2: animated comparison table (Buildera vs In-House vs Freelancer). Column 3: animated bar chart with project outcome feed. All animations trigger on scroll.</done>
</task>

<task type="auto">
  <name>Task 2: Create TestimonialsSection + CaseStudiesPreview + CTASection + assemble complete page.tsx</name>
  <files>buildera-frontend/src/components/sections/TestimonialsSection.tsx, buildera-frontend/src/components/ui/TestimonialCard.tsx, buildera-frontend/src/components/sections/CaseStudiesPreview.tsx, buildera-frontend/src/components/ui/CaseStudyPreviewCard.tsx, buildera-frontend/src/components/sections/CTASection.tsx, buildera-frontend/src/app/page.tsx</files>
  <read_first>
    - buildera-frontend/src/app/globals.css (glass-card utility class, cta-button-shimmer class)
    - buildera-frontend/src/components/sections/HeroSection.tsx (export name — created in 03-04)
    - buildera-frontend/src/components/sections/StatsBarSection.tsx (export name, Settings prop — created in 03-04)
    - buildera-frontend/src/components/sections/ServicesTabSection.tsx (export name — created in 03-05)
    - buildera-frontend/src/components/sections/SolutionsGridSection.tsx (export name — created in 03-05)
    - buildera-frontend/src/components/sections/ClientLogosMarquee.tsx (export name — created in 03-05)
    - .planning/phases/03-homepage-design-system/03-PATTERNS.md (Pattern Assignments 17, 18, 23 — TestimonialsSection, TestimonialCard, CTASection)
    - .planning/phases/03-homepage-design-system/03-UI-SPEC.md (Section 7 Testimonials animation — card depth stagger, Section 8 Case Studies — metric counter reveal, Section 9 CTA — gradient pulse + shimmer, Copywriting Contract for all section copy)
    - .planning/phases/03-homepage-design-system/03-CONTEXT.md (D-12 section backgrounds, COMP-03 Clutch badge)
  </read_first>
  <action>
    Create TestimonialCard.tsx as a Server Component:
    - Import cn from @/lib/utils
    - Import IconStar from @tabler/icons-react
    - Props: quote (string), name (string), title (string), company (string), rating (number 1-5), className (optional)
    - Render div with cn("glass-card p-6 flex flex-col gap-4", className)
    - Star rating row: flex gap-1, render 'rating' number of IconStar with size-4 text-[var(--brand-primary)] fill-current (filled stars), then (5 - rating) IconStar with size-4 text-border (empty stars)
    - Quote: blockquote with text-foreground text-base leading-relaxed, wrapped in curly quotes
    - Attribution: mt-auto div with p font-semibold text-sm text-foreground (name), p text-xs text-muted-foreground ("{title}, {company}")

    Create TestimonialsSection.tsx as a Server Component:
    - Import motion from motion/react
    - Import TestimonialCard from @/components/ui/TestimonialCard
    - Import IconStar from @tabler/icons-react (for Clutch badge)
    - Define TESTIMONIALS constant: 3 hardcoded testimonials with realistic-looking content about Buildera services. Use names/titles/companies that sound genuine but are fabricated. Include quotes about specific outcomes. All 5-star ratings.
      Example testimonials (executor should write complete, genuine-sounding quotes):
      1. Manufacturing client about ERP implementation
      2. E-commerce client about website development
      3. Professional services client about Salesforce CRM
    - Define depth stagger arrays: yOffsets [40, 24, 56], delays [0, 0.15, 0.3]
    - Section wrapper: py-16 lg:py-24 bg-background
    - Container: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
    - Section header with scroll reveal: eyebrow "What Our Clients Say", H2 "Don't Take Our Word For It"
    - COMP-03: Clutch-style badge below heading. Render a simple badge element: flex items-center gap-2 bg-card border border-border rounded-full px-4 py-2 w-fit mx-auto mb-8 (or left-aligned). Show a star icon + "4.9/5.0 on Clutch" text in text-sm font-medium. This is a static placeholder — actual Clutch widget is admin-configured later.
    - Grid: grid md:grid-cols-3 gap-6 mt-12
    - Each testimonial wrapped in motion.div with initial opacity 0 y yOffsets[index], whileInView opacity 1 y 0, viewport once true margin "-60px", transition delay delays[index] duration 0.6 ease easeOut

    Create CaseStudyPreviewCard.tsx as a "use client" component:
    - Import motion, useMotionValue, useSpring, useTransform, useInView from motion/react
    - Import useRef, useEffect from react
    - Props: title (string), industry (string), metricValue (number), metricLabel (string), description (string)
    - Card: rounded-lg border border-border bg-card p-6 flex flex-col gap-4 hover:-translate-y-1 hover:shadow-[0_0_0_2px_var(--brand-primary)] transition-all duration-200
    - Industry badge at top: text-xs font-medium uppercase tracking-wider text-[var(--brand-primary)] bg-[var(--brand-glass)] px-3 py-1 rounded-full w-fit
    - Title: text-lg font-semibold text-foreground
    - Animated metric: use useMotionValue + useSpring + useTransform pattern (same as AnimatedCounter but inline). Ref on the metric span, useInView once true, count from 0 to metricValue on inView, display as "{rounded}% {metricLabel}". Spring: stiffness 60 damping 20. Delay 0.4s after card enters view.
    - Description: text-sm text-muted-foreground line-clamp-2
    - "Read Case Study" link at bottom: text-sm font-medium text-[var(--brand-primary)] hover:underline

    Create CaseStudiesPreview.tsx as a Server Component shell:
    - Import motion from motion/react
    - Import CaseStudyPreviewCard from @/components/ui/CaseStudyPreviewCard
    - Define 3 hardcoded case study previews with realistic content:
      1. "Warehouse Management System for Logistics Company", industry "Logistics", metricValue 40, metricLabel "faster operations", description about streamlined inventory tracking
      2. "Salesforce CRM Rollout for Trading Firm", industry "Finance", metricValue 200, metricLabel "increase in lead pipeline" (display as "200%"), description about sales process automation
      3. "Custom E-Commerce Platform for Retail SMB", industry "Retail", metricValue 60, metricLabel "more conversions", description about online sales growth
    - Section wrapper: py-16 lg:py-24 bg-[var(--brand-surface)]
    - Container: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
    - Section header with scroll reveal: eyebrow "Proven Results", H2 "Results We've Delivered"
    - Grid: grid md:grid-cols-3 gap-6 mt-12
    - Each card wrapped in motion.div with initial opacity 0 y 24, whileInView opacity 1 y 0, viewport once true, transition delay index * 0.2 duration 0.5

    Create CTASection.tsx as a Server Component (CSS animations only, no hooks):
    - Import motion from motion/react
    - Section wrapper: motion.section with py-24 relative overflow-hidden, style with inline background linear-gradient(135deg, var(--brand-gradient-from), var(--brand-gradient-to))
    - Container: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center
    - Headline: motion.h2 with text-4xl lg:text-5xl font-bold text-white mb-4 tracking-tight, initial opacity 0 y 24 scale 0.97, whileInView opacity 1 y 0 scale 1, viewport once true, transition duration 0.6 ease easeOut. Text: "Ready to build something that grows your business?"
    - Subtext: motion.p with text-white/80 text-lg mb-8 max-w-xl mx-auto, delay 0.2. Text: "Book a free 30-minute discovery call. No pressure, no pitch deck — just a conversation about what you need."
    - CTA button: a element linking to /book-a-call with classes "cta-button-shimmer relative inline-flex items-center gap-2 bg-white text-[var(--brand-primary)] font-semibold px-8 py-4 rounded-lg hover:bg-white/90 transition-colors overflow-hidden min-h-[48px]". Text: "Book a Free Discovery Call". Wrapped in motion.div with delay 0.4.
    - Eyebrow label above heading: "Ready to Start?" (text-sm text-white/60 uppercase tracking-widest font-medium mb-4)

    Assemble complete page.tsx as an async Server Component:
    - This is the SOLE owner of page.tsx assembly for Phase 3. Do not reference any prior page.tsx content — create it fresh.
    - Import fetchSettings from @/lib/api
    - Import all 9 section components:
      - HeroSection from @/components/sections/HeroSection (created in 03-04)
      - StatsBarSection from @/components/sections/StatsBarSection (created in 03-04)
      - ServicesTabSection from @/components/sections/ServicesTabSection (created in 03-05)
      - SolutionsGridSection from @/components/sections/SolutionsGridSection (created in 03-05)
      - ClientLogosMarquee from @/components/sections/ClientLogosMarquee (created in 03-05)
      - WhyBuilderaSection from @/components/sections/WhyBuilderaSection (created in this plan Task 1)
      - TestimonialsSection from @/components/sections/TestimonialsSection (created in this task)
      - CaseStudiesPreview from @/components/sections/CaseStudiesPreview (created in this task)
      - CTASection from @/components/sections/CTASection (created in this task)
    - Call await fetchSettings() to get stats data for StatsBarSection
    - No "use client" — page.tsx is a Server Component
    - Render all 9 sections in locked narrative order:
      1. HeroSection
      2. StatsBarSection with settings prop
      3. ServicesTabSection
      4. SolutionsGridSection
      5. ClientLogosMarquee
      6. WhyBuilderaSection
      7. TestimonialsSection
      8. CaseStudiesPreview
      9. CTASection
    - No placeholder comments — all 9 sections are rendered
  </action>
  <verify>
    <automated>cd buildera-frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - TestimonialCard.tsx is NOT "use client" — Server Component
    - TestimonialCard uses glass-card CSS class for glassmorphism
    - TestimonialCard renders star rating with IconStar from @tabler/icons-react
    - TestimonialsSection defines 3 hardcoded testimonials with realistic content
    - Testimonials use depth stagger: y offsets [40, 24, 56], delays [0, 0.15, 0.3]
    - COMP-03 Clutch badge rendered as static badge in testimonials section
    - CaseStudyPreviewCard.tsx has "use client" with animated metric counter
    - CaseStudyPreviewCard uses useMotionValue + useSpring pattern
    - CaseStudiesPreview defines 3 hardcoded case studies with metric values
    - CaseStudiesPreview background is bg-[var(--brand-surface)]
    - CTASection.tsx is NOT "use client" — CSS gradient + shimmer only
    - CTASection background uses inline style linear-gradient with brand-gradient-from/to
    - CTA button has class "cta-button-shimmer" for hover shimmer effect
    - CTA button links to /book-a-call
    - page.tsx is NOT "use client" — async Server Component
    - page.tsx imports all 9 section components (HeroSection, StatsBarSection, ServicesTabSection, SolutionsGridSection, ClientLogosMarquee, WhyBuilderaSection, TestimonialsSection, CaseStudiesPreview, CTASection)
    - page.tsx renders all 9 sections in locked narrative order
    - No placeholder comments remain in page.tsx
    - No lucide-react imports in any file
    - npm run build exits 0
  </acceptance_criteria>
  <done>Homepage is fully composed with all 9 sections in locked narrative order. WhyBuilderaSection differentiates with animated ring, comparison table, and metrics. TestimonialsSection shows glassmorphism cards with depth stagger. CaseStudiesPreview shows 3 cards with animated result metrics. CTASection provides the conversion endpoint with gradient and shimmer button. page.tsx is fully assembled and compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | All content is hardcoded — no API data, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Tampering | Hardcoded testimonial content | accept | All content is static JSX; no user-submitted data; React auto-escapes text content |
| T-03-16 | Tampering | CTA link destinations | accept | Hardcoded to /book-a-call; no dynamic routing or user-controlled hrefs |
| T-03-17 | Tampering | dangerouslySetInnerHTML | mitigate | NEVER used in any component; all content rendered as JSX text children |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
cd buildera-frontend && npm run build
# Validates: Complete homepage composition, all 9 sections, all Server/Client boundaries, TypeScript
</verification>

<success_criteria>
- Homepage renders all 9 sections in correct narrative order
- Why Buildera has 3-column layout with animated ring, comparison table, metrics card
- SVG ring draws to 98% on scroll
- Comparison checkmarks animate with spring physics
- Bar chart bars grow from bottom
- Testimonials render in glassmorphism cards with depth stagger
- Clutch-style badge visible in testimonials section
- Case study cards show animated metric counters
- Bottom CTA has gradient background with shimmer button on hover
- page.tsx imports all 9 section components from their respective source plans
- npm run build exits 0 — the complete homepage compiles without errors
</success_criteria>

<output>
Create `.planning/phases/03-homepage-design-system/03-06-SUMMARY.md` when done
</output>
