---
phase: 03-homepage-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - buildera-frontend/src/components/layout/SiteNav.tsx
  - buildera-frontend/src/components/layout/SiteNavClient.tsx
  - buildera-frontend/src/components/layout/MegaDropdown.tsx
  - buildera-frontend/src/components/layout/MobileNavDrawer.tsx
  - buildera-frontend/src/app/layout.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Sticky nav starts transparent and activates glassmorphism after 50px scroll"
    - "Mega dropdown renders 4 panels (Services, Solutions, Work, Resources) on desktop hover"
    - "Services panel shows 6 categories with sub-service links from hardcoded SERVICES_MENU"
    - "Solutions/Work/Resources panels populate from fetchNavItems() with empty fallback"
    - "Mobile drawer slides from right, accordion groups expand one-at-a-time"
    - "Book a Call CTA is visible in desktop nav and pinned at mobile drawer bottom"
    - "150ms hover delay prevents accidental dropdown triggers"
    - "fetchNavItems() with { next: { tags: ['nav-items'] } } cache tag establishes Phase 3 ISR wiring; Filament NavItem admin resource delivered in Phase 8 (NAV-05 partial)"
    - "NAV-07 (breadcrumbs) is out of scope for Phase 3 — deep pages don't exist until Phases 4-6; BreadcrumbList JSON-LD delivered in Phase 7 (plan 07-02)"
  artifacts:
    - path: "buildera-frontend/src/components/layout/SiteNav.tsx"
      provides: "Server Component shell — fetches nav items, passes to SiteNavClient"
      exports: ["SiteNav"]
    - path: "buildera-frontend/src/components/layout/SiteNavClient.tsx"
      provides: "Client Component — scroll glass effect, dropdown state, mobile drawer toggle"
      contains: "use client"
    - path: "buildera-frontend/src/components/layout/MegaDropdown.tsx"
      provides: "Dropdown panel content component"
    - path: "buildera-frontend/src/components/layout/MobileNavDrawer.tsx"
      provides: "Full-screen mobile overlay with accordion groups"
      contains: "use client"
  key_links:
    - from: "buildera-frontend/src/components/layout/SiteNav.tsx"
      to: "buildera-frontend/src/lib/api.ts"
      via: "fetchNavItems() called at build time"
      pattern: "fetchNavItems"
    - from: "buildera-frontend/src/components/layout/SiteNav.tsx"
      to: "buildera-frontend/src/components/layout/SiteNavClient.tsx"
      via: "Server shell renders client component with data props"
      pattern: "SiteNavClient"
    - from: "buildera-frontend/src/app/layout.tsx"
      to: "buildera-frontend/src/components/layout/SiteNav.tsx"
      via: "SiteNav rendered in RootLayout body before main"
      pattern: "SiteNav"
---

<objective>
Build the complete SiteNav component system: server shell that fetches nav items at build time, client component for scroll-triggered glassmorphism and dropdown hover state, mega dropdown with 4 panels (Services hardcoded, Solutions/Work/Resources from API), mobile full-screen drawer with accordion groups, and wire SiteNav into layout.tsx.

Purpose: Navigation is the primary wayfinding mechanism. Every page in Phases 4-10 inherits this nav from layout.tsx. It must work on desktop (hover mega dropdown) and mobile (full-screen overlay drawer).
Output: 4 component files in components/layout/, layout.tsx updated with SiteNav import.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-design-system/03-01-SUMMARY.md
@.planning/phases/03-homepage-design-system/03-RESEARCH.md
@.planning/phases/03-homepage-design-system/03-UI-SPEC.md
@.planning/phases/03-homepage-design-system/03-PATTERNS.md
@.planning/phases/03-homepage-design-system/03-CONTEXT.md

<interfaces>
From buildera-frontend/src/lib/api.ts (after 03-01):
```typescript
export interface NavItem {
  id: number; label: string; url: string;
  group: 'solutions' | 'work' | 'resources'; display_order: number;
}
export async function fetchNavItems(): Promise<NavItem[]>
```

From buildera-frontend/src/app/globals.css (after 03-01):
```css
@utility glass-nav {
  background: hsl(0 0% 100% / 80%);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--brand-glass);
}
```

From buildera-frontend/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SiteNav server shell + SiteNavClient with scroll glassmorphism + MegaDropdown</name>
  <files>buildera-frontend/src/components/layout/SiteNav.tsx, buildera-frontend/src/components/layout/SiteNavClient.tsx, buildera-frontend/src/components/layout/MegaDropdown.tsx</files>
  <read_first>
    - buildera-frontend/src/lib/api.ts (NavItem interface shape, fetchNavItems signature)
    - buildera-frontend/src/app/globals.css (glass-nav utility class definition)
    - buildera-frontend/src/lib/utils.ts (cn helper signature)
    - buildera-frontend/src/components/ui/button.tsx (Button component API for CTA button usage)
    - .planning/phases/03-homepage-design-system/03-PATTERNS.md (Pattern Assignments 4, 5, 6 — SiteNav, SiteNavClient, MegaDropdown)
    - .planning/phases/03-homepage-design-system/03-UI-SPEC.md (Nav Architecture Contract — desktop structure, SERVICES_MENU const, mega dropdown panels, sticky behavior)
    - .planning/phases/03-homepage-design-system/03-CONTEXT.md (D-13 hover delay 150ms, D-14 full-width panel, D-15 scroll behavior, D-17 data sourcing split)
  </read_first>
  <action>
    Create SiteNav.tsx as an async Server Component that:
    - Imports fetchNavItems from @/lib/api and SiteNavClient from ./SiteNavClient
    - Defines the SERVICES_MENU constant with all 6 categories exactly as specified in UI-SPEC.md Nav Architecture Contract (category name, Tabler icon component name string, slug, subServices array). Export this const for reuse in ServicesTabSection (plan 03-05).
    - Calls await fetchNavItems() (SSG build-time fetch with nav-items cache tag, fallback empty array)
    - Renders a header element with classes fixed top-0 left-0 right-0 z-50
    - Passes servicesMenu and navItems as props to SiteNavClient

    Create SiteNavClient.tsx as a "use client" component that:
    - Accepts props: servicesMenu (the SERVICES_MENU type), navItems (NavItem[])
    - Uses useState for isScrolled (boolean), activeDropdown (string | null), isMobileOpen (boolean)
    - Uses useEffect with passive scroll listener to set isScrolled when window.scrollY > 50
    - Renders a nav element with cn() conditional classes: when isScrolled apply glass-nav, else bg-transparent. Transition duration-300.
    - Inside nav: a container div (max-w-7xl mx-auto px-4 sm:px-6 lg:px-8) with flex items-center justify-between h-16 lg:h-20
    - Left: Buildera text logo as Link to "/" with text-xl font-bold text-foreground
    - Center (hidden on mobile, flex on lg): 4 nav items — Services, Solutions, Work, Resources. Each is a button with onMouseEnter (set activeDropdown to panel name after 150ms delay via setTimeout — per D-13 this prevents accidental triggers on cursor passing through) and onMouseLeave (clear the setTimeout and set activeDropdown to null). Store the timeout ref with useRef so it can be cancelled on leave. Use relative positioning for dropdown trigger.
    - Right: "Book a Call" as Link to /book-a-call styled with bg-[var(--brand-primary)] text-white px-5 py-2.5 rounded-lg font-medium min-h-[48px] items-center (hidden on mobile). Plus hamburger button (lg:hidden) using IconMenu2 from @tabler/icons-react.
    - Below the nav bar: render MegaDropdown when activeDropdown is not null, passing activeDropdown name, servicesMenu, navItems, and mouse handlers to keep dropdown open when hovering panel
    - Render MobileNavDrawer with isOpen={isMobileOpen}, onClose toggle, servicesMenu, navItems

    Create MegaDropdown.tsx as a component (can be Server or Client depending on needs — if it only receives props and renders JSX, keep as regular component rendered inside SiteNavClient):
    - Accepts activePanel (string), servicesMenu, navItems (NavItem[])
    - Renders a full-width dropdown panel: absolute left-0 right-0 top-full, bg-white border-t border-border shadow-lg
    - Inner content constrained: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8
    - Services panel: 3-column grid of 6 categories. Each category shows Tabler icon, category name as bold link to /services/{slug}, sub-services listed below as smaller links.
    - Solutions panel: filter navItems by group === 'solutions', render as a 4-column grid of tiles (icon + label, link to url)
    - Work panel: filter navItems by group === 'work', render as link list
    - Resources panel: filter navItems by group === 'resources', render as link list
    - Add onMouseEnter on the panel container to keep activeDropdown alive, onMouseLeave to close
    - Use motion.div for enter animation: initial opacity 0 y -8, animate opacity 1 y 0, exit opacity 0 y -8, transition duration 0.2
    - Wrap in AnimatePresence in SiteNavClient for enter/exit animation

    Icon mapping for SERVICES_MENU: import IconWorldWww, IconCloud, IconSettings, IconRobot, IconCode, IconUsers from @tabler/icons-react. Use a mapping object or conditional render based on the icon string in SERVICES_MENU.

    All links use next/link Link component for client-side navigation. All interactive elements have min-h-[48px] for touch targets. Use cn() for all conditional classes.
  </action>
  <verify>
    <automated>cd buildera-frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <acceptance_criteria>
    - SiteNav.tsx is an async function component (not "use client"), exports SiteNav
    - SiteNav.tsx calls fetchNavItems() and passes result to SiteNavClient
    - SiteNav.tsx defines and exports SERVICES_MENU with 6 categories matching UI-SPEC.md
    - SiteNavClient.tsx has "use client" as first line
    - SiteNavClient.tsx has scroll listener that sets isScrolled at 50px threshold
    - SiteNavClient.tsx conditionally applies glass-nav class when isScrolled is true
    - SiteNavClient.tsx renders "Book a Call" link to /book-a-call with bg-[var(--brand-primary)]
    - SiteNavClient.tsx renders hamburger button with IconMenu2 (not lucide Menu)
    - Dropdown open is delayed 150ms after hover — implemented via setTimeout in onMouseEnter handler (setTimeout ref stored in useRef, cleared on onMouseLeave to prevent open if cursor immediately leaves)
    - MegaDropdown renders 4 distinct panel layouts based on activePanel prop
    - Services panel content is hardcoded from SERVICES_MENU (not from API)
    - Solutions/Work/Resources panels filter navItems by group field
    - No lucide-react imports in any file
    - All icons from @tabler/icons-react
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>SiteNav server shell fetches nav data at build time and passes to SiteNavClient. SiteNavClient handles scroll glassmorphism activation at 50px, renders desktop nav bar with 4-panel mega dropdown on hover with 150ms delay, and hamburger button for mobile. MegaDropdown renders Services (hardcoded), Solutions, Work, Resources panels with appropriate layouts.</done>
</task>

<task type="auto">
  <name>Task 2: Create MobileNavDrawer + wire SiteNav into layout.tsx</name>
  <files>buildera-frontend/src/components/layout/MobileNavDrawer.tsx, buildera-frontend/src/app/layout.tsx</files>
  <read_first>
    - buildera-frontend/src/app/layout.tsx (current state after 03-01 — has skip-to-content and main wrapper)
    - buildera-frontend/src/components/layout/SiteNav.tsx (just created — SERVICES_MENU export, SiteNav component)
    - .planning/phases/03-homepage-design-system/03-PATTERNS.md (Pattern Assignment 6: MobileNavDrawer, Pattern Assignment 2: layout.tsx additions)
    - .planning/phases/03-homepage-design-system/03-UI-SPEC.md (Mobile Nav Structure — slide from right, accordion, pinned CTA)
    - .planning/phases/03-homepage-design-system/03-CONTEXT.md (D-16 mobile nav spec)
  </read_first>
  <action>
    Create MobileNavDrawer.tsx as a "use client" component:
    - Props: isOpen (boolean), onClose (function), servicesMenu (SERVICES_MENU type), navItems (NavItem[])
    - Uses useState for openGroup (string | null) — tracks which accordion group is expanded
    - Wraps entire render in AnimatePresence from motion/react
    - When isOpen is true, renders:
      1. Backdrop: motion.div with fixed inset-0 bg-black/50 z-40, onClick calls onClose, initial opacity 0, animate opacity 1, exit opacity 0
      2. Drawer: motion.div with fixed top-0 right-0 h-full w-full max-w-sm bg-background z-50 flex flex-col overflow-y-auto, initial x "100%", animate x 0, exit x "100%", transition duration 0.35 ease easeOut
    - Drawer header: flex justify-between items-center p-4 border-b border-border. Left: close button (IconX from @tabler/icons-react, min-h-[48px] min-w-[48px]). Right: Buildera text logo.
    - Drawer body: flex-1 overflow-y-auto py-4. Render 4 accordion groups:
      - Services: button with category name + IconChevronDown (rotates 180deg when open). When open, show subServices list from servicesMenu with links to /services/{slug}/{sub-slug}.
      - Solutions: accordion with links from navItems filtered by group 'solutions'
      - Work: accordion with links from navItems filtered by group 'work'
      - Resources: accordion with links from navItems filtered by group 'resources'
    - Only one accordion group open at a time: clicking a group sets openGroup to its name, or null if already open.
    - Each accordion item animates height with motion.div initial height 0 opacity 0, animate height auto opacity 1, exit height 0 opacity 0.
    - Drawer footer: mt-auto p-4 border-t border-border. Full-width "Book a Free Call" link to /book-a-call, styled bg-[var(--brand-primary)] text-white text-center py-3 rounded-lg font-medium min-h-[48px].
    - All nav links call onClose when clicked (close drawer on navigation).
    - All interactive elements have min-h-[48px] for touch targets (A11Y-02 prep).

    Update layout.tsx to import and render SiteNav:
    - Add import: import { SiteNav } from "@/components/layout/SiteNav"
    - Render SiteNav AFTER the skip-to-content link and BEFORE the main element
    - Do NOT import SiteFooter yet (plan 03-03 creates it)
    - Keep the existing skip-to-content link and main wrapper unchanged
  </action>
  <verify>
    <automated>cd buildera-frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - MobileNavDrawer.tsx has "use client" as first line
    - MobileNavDrawer uses AnimatePresence to wrap conditional isOpen render
    - Drawer slides from right via motion.div initial x "100%"
    - Close button uses IconX from @tabler/icons-react (not lucide X)
    - Accordion chevron uses IconChevronDown (not lucide ChevronDown)
    - Only one accordion group open at a time (openGroup state)
    - "Book a Free Call" CTA pinned at drawer bottom with mt-auto
    - CTA links to /book-a-call
    - All interactive elements have min-h-[48px]
    - layout.tsx imports SiteNav from @/components/layout/SiteNav
    - layout.tsx renders SiteNav between skip-to-content and main element
    - npm run build exits 0
  </acceptance_criteria>
  <done>MobileNavDrawer renders full-screen overlay from right with backdrop, accordion navigation groups (Services hardcoded, Solutions/Work/Resources from API), and pinned "Book a Free Call" CTA. layout.tsx renders SiteNav as a shared layout component visible on all pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> nav rendering | NavItem data from GET /api/nav-items rendered as link text and href values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | NavItem.url rendered as href | mitigate | All hrefs rendered via next/link Link component which handles routing internally; API-sourced URLs are relative paths controlled by admin; no user-submitted content in nav |
| T-03-05 | Tampering | NavItem.label rendered as text | accept | Labels rendered as text content (not innerHTML); React auto-escapes; no XSS vector |
| T-03-06 | Spoofing | SERVICES_MENU hardcoded links | accept | Hardcoded in source code; not user-modifiable |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; components are file scaffolds from official shadcn registry |
</threat_model>

<verification>
cd buildera-frontend && npm run build
# Validates: SiteNav renders in layout, all imports resolve, Server/Client boundary correct
</verification>

<success_criteria>
- SiteNav visible on all pages via layout.tsx
- Scroll past 50px activates glassmorphism effect on nav
- Mega dropdown opens on hover with 4 distinct panels after 150ms delay
- Services panel shows 6 hardcoded categories with sub-services
- Mobile hamburger opens full-screen drawer from right
- "Book a Call" CTA visible on both desktop and mobile nav
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/03-homepage-design-system/03-02-SUMMARY.md` when done
</output>
