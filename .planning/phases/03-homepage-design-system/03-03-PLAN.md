---
phase: 03-homepage-design-system
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - buildera-frontend/src/components/layout/SiteFooter.tsx
  - buildera-frontend/src/app/layout.tsx
autonomous: true
requirements:
  - NAV-04

must_haves:
  truths:
    - "Footer renders multi-column layout with Services, Solutions, Company, Resources columns"
    - "Footer shows social icons (LinkedIn, Twitter/X, Instagram) from settings API"
    - "Footer shows contact info from settings API with hardcoded fallback"
    - "Newsletter opt-in strip with email input and Get Updates button renders"
    - "Footer attribution line reads 'Developed and managed by Buildera Technologies LLP' with rel=nofollow"
    - "Footer links populate from fetchFooterLinks() at build time with hardcoded fallback"
    - "fetchFooterLinks() with { next: { tags: ['footer-links'] } } cache tag establishes Phase 3 ISR wiring; Filament FooterLink admin resource delivered in Phase 8 (NAV-06 partial)"
    - "Newsletter form action='#' is an intentional Phase 3 stub — POST /api/subscribers wiring deferred to Phase 5 (plan 05-04)"
  artifacts:
    - path: "buildera-frontend/src/components/layout/SiteFooter.tsx"
      provides: "Server Component — multi-column footer with API data"
      exports: ["SiteFooter"]
  key_links:
    - from: "buildera-frontend/src/components/layout/SiteFooter.tsx"
      to: "buildera-frontend/src/lib/api.ts"
      via: "fetchFooterLinks() + fetchSettings() parallel fetch"
      pattern: "Promise.all.*fetchFooterLinks.*fetchSettings"
    - from: "buildera-frontend/src/app/layout.tsx"
      to: "buildera-frontend/src/components/layout/SiteFooter.tsx"
      via: "SiteFooter rendered in RootLayout after main"
      pattern: "SiteFooter"
---

<objective>
Build the SiteFooter server component with multi-column layout, social icons, newsletter opt-in strip (visual stub — Phase 5 wires submission), contact info, legal links, and attribution line. Wire it into layout.tsx so it appears on all pages.

Purpose: Footer provides secondary navigation, contact information, newsletter signup, and legal compliance for every page. It fetches data at build time (SSG) with hardcoded fallbacks when API is unavailable.
Output: SiteFooter.tsx component, layout.tsx updated with SiteFooter import.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-design-system/03-01-SUMMARY.md
@.planning/phases/03-homepage-design-system/03-RESEARCH.md
@.planning/phases/03-homepage-design-system/03-UI-SPEC.md
@.planning/phases/03-homepage-design-system/03-PATTERNS.md
@.planning/phases/03-homepage-design-system/03-CONTEXT.md
@.planning/CLIENT-CONTEXT.md

<interfaces>
From buildera-frontend/src/lib/api.ts (after 03-01):
```typescript
export interface FooterLink {
  id: number; label: string; url: string;
  column: 'services' | 'solutions' | 'company' | 'resources'; display_order: number;
}
export interface Settings {
  company_name: string; email: string; phone: string; address: string;
  linkedin_url: string; instagram_url: string; twitter_url: string;
  footer_tagline: string; /* ... other fields */
}
export async function fetchFooterLinks(): Promise<FooterLink[]>
export async function fetchSettings(): Promise<Settings>
```

From buildera-frontend/src/components/ui/separator.tsx (installed in 03-01):
shadcn Separator component for visual dividers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SiteFooter component with all sections</name>
  <files>buildera-frontend/src/components/layout/SiteFooter.tsx</files>
  <read_first>
    - buildera-frontend/src/lib/api.ts (FooterLink, Settings interfaces, fetchFooterLinks, fetchSettings signatures, SETTINGS_FALLBACK values)
    - buildera-frontend/src/components/ui/separator.tsx (Separator component API)
    - .planning/phases/03-homepage-design-system/03-PATTERNS.md (Pattern Assignment 7: SiteFooter — Promise.all parallel fetch pattern, multi-column structure)
    - .planning/phases/03-homepage-design-system/03-UI-SPEC.md (Footer Architecture Contract — column structure, content, social icons, newsletter strip, legal links)
    - .planning/phases/03-homepage-design-system/03-CONTEXT.md (D-12 footer background not specified — use dark footer per UI-SPEC)
    - .planning/CLIENT-CONTEXT.md (phone +91-82994 06767, email info@buildera.co, address, attribution line)
  </read_first>
  <action>
    Create SiteFooter.tsx as an async Server Component (no "use client"):
    - Import fetchFooterLinks, fetchSettings from @/lib/api
    - Import IconBrandLinkedin, IconBrandTwitter, IconBrandInstagram from @tabler/icons-react
    - Import Separator from @/components/ui/separator
    - Import Link from next/link

    Data fetching: Use Promise.all to fetch footerLinks and settings in parallel. Both have .catch() fallbacks built into the helpers, so the footer never crashes.

    Define HARDCODED_FOOTER_LINKS constant for Phase 3 fallback (used when API returns empty):
    - Services column: Website Dev (/services/website-development), Salesforce Dev (/services/salesforce-development), DevOps Dev (/services/devops-development), AI Agent Dev (/services/ai-agent-development), Software Dev (/services/software-development), Hire a Developer (/services/hire-a-developer)
    - Solutions column: Top 6 solutions with slugs, plus "View All Solutions" link to /solutions
    - Company column: About Us (/about), How We Work (/how-we-work), Case Studies (/case-studies), Testimonials (/testimonials), FAQ (/faq)
    - Resources column: Blog (/blog), Guides (/guides), Contact Us (/contact), Book a Call (/book-a-call), Privacy Policy (/privacy-policy), Terms (/terms)

    Determine which links to use: if footerLinks from API has items, group them by column field. Otherwise use HARDCODED_FOOTER_LINKS.

    Layout structure (dark footer: bg-slate-900 text-slate-300):
    1. Top section (py-16): 5-column grid on desktop (lg:grid-cols-5), single column on mobile
       - Column 1: Buildera text logo (text-white font-bold text-xl), footer_tagline from settings, contact info (phone, email, address from settings)
       - Columns 2-5: Services, Solutions, Company, Resources link columns. Each column has a heading (text-sm font-semibold text-white uppercase tracking-wider mb-4) and a list of links (text-sm text-slate-400 hover:text-white transition-colors). All links use Link component.

    2. Social icons row: flex gap-4, centered. Render LinkedIn, Twitter/X, Instagram icons only when settings has the corresponding URL (non-empty string). Each icon link opens in new tab (target="_blank" rel="noopener noreferrer"). Icon size: size-5. Color: text-slate-400 hover:text-white transition-colors.

    3. Separator

    4. Newsletter strip: flex justify-between items-center py-6. Left: heading "Stay updated with Buildera" + description text. Right: email input (bg-slate-800 border border-slate-700 text-white placeholder-slate-500 rounded-lg px-4 py-2.5 min-h-[48px]) + "Get Updates" submit button (bg-[var(--brand-primary)] text-white px-5 py-2.5 rounded-lg font-medium min-h-[48px]). The newsletter form uses action="#" (intentional Phase 3 visual stub — POST /api/subscribers wiring is deferred to Phase 5 plan 05-04; do not wire a submit handler here).

    5. Separator

    6. Bottom bar: flex justify-between items-center py-6. Left: copyright text ("2026 Buildera. All rights reserved." using settings.company_name). Right: legal links (Privacy Policy /privacy-policy, Terms /terms) separated by a middot.

    7. Attribution line at very bottom: text-xs text-slate-500 text-center pb-4. Text: "Developed and managed by" followed by an a element linking to https://buildera.co with rel="nofollow" class hover:text-white transition-colors, text "Buildera Technologies LLP".

    All link hover effects: text-slate-400 to text-white with transition-colors. Use cn() for class composition where conditional logic exists.
  </action>
  <verify>
    <automated>cd buildera-frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <acceptance_criteria>
    - SiteFooter.tsx is an async function (Server Component, no "use client")
    - SiteFooter calls Promise.all with fetchFooterLinks() and fetchSettings()
    - Footer has 5-column grid layout on desktop (logo+info, Services, Solutions, Company, Resources)
    - Hardcoded fallback links exist for when API returns empty
    - Social icons use IconBrandLinkedin, IconBrandTwitter, IconBrandInstagram from @tabler/icons-react
    - Social icons only render when settings URL is non-empty
    - Newsletter strip has email input and "Get Updates" button with action="#" (visual only — not wired to POST /api/subscribers; that is Phase 5 work)
    - Attribution line contains href="https://buildera.co" with rel="nofollow"
    - Attribution text is "Buildera Technologies LLP"
    - Footer background is bg-slate-900
    - All interactive elements have min-h-[48px]
    - No lucide-react imports
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>SiteFooter renders complete multi-column layout with contact info, social icons, newsletter form stub (action="#"), legal links, and attribution per CLIENT-CONTEXT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SiteFooter into layout.tsx</name>
  <files>buildera-frontend/src/app/layout.tsx</files>
  <read_first>
    - buildera-frontend/src/app/layout.tsx (current state after 03-02 wired SiteNav — has skip-to-content, SiteNav, main wrapper)
    - buildera-frontend/src/components/layout/SiteFooter.tsx (just created — export name)
  </read_first>
  <action>
    Update layout.tsx to add SiteFooter:
    - Add import: import { SiteFooter } from "@/components/layout/SiteFooter"
    - Render SiteFooter AFTER the closing main tag, before the closing body tag
    - Keep all existing elements unchanged (skip-to-content, SiteNav, main with id="main-content")
    - Final body structure: skip-to-content link, SiteNav, main#main-content wrapping {children}, SiteFooter
  </action>
  <verify>
    <automated>cd buildera-frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx imports SiteFooter from @/components/layout/SiteFooter
    - layout.tsx renders SiteFooter after main element
    - Body order is: skip-to-content, SiteNav, main, SiteFooter
    - npm run build exits 0
  </acceptance_criteria>
  <done>SiteFooter is rendered in RootLayout, appearing on all pages below the main content area.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> footer rendering | FooterLink and Settings data from API rendered as text and href values |
| Newsletter form | Email input rendered (not submitted in Phase 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | FooterLink.url rendered as href | mitigate | Rendered via next/link Link component; admin-controlled content; no user-submitted URLs |
| T-03-08 | Information Disclosure | Settings.phone/email in footer | accept | Public contact info intentionally displayed; same values as on business cards |
| T-03-09 | Tampering | Newsletter form submission | accept | Form is visual-only in Phase 3 (action="#"); POST /api/subscribers with rate limiting is wired in Phase 5 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
cd buildera-frontend && npm run build
# Validates: SiteFooter renders in layout, async data fetch compiles, Server Component boundary correct
</verification>

<success_criteria>
- Footer visible on all pages via layout.tsx
- 5-column layout renders on desktop with correct column headings
- Social icons render when settings URLs are present
- Newsletter input and button are visible (not functional — Phase 5 wires submission)
- Attribution with rel="nofollow" appears at bottom
- Contact info matches CLIENT-CONTEXT.md values
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/03-homepage-design-system/03-03-SUMMARY.md` when done
</output>
