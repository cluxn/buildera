---
plan: "04-01"
wave: 1
phase: 4
depends_on: []
files_modified:
  - buildera-frontend/src/types/service-page.ts
  - buildera-frontend/src/data/services/index.ts
  - buildera-frontend/src/data/services/website-development.ts
  - buildera-frontend/src/data/services/salesforce.ts
  - buildera-frontend/src/data/services/devops.ts
  - buildera-frontend/src/data/services/ai-agent.ts
  - buildera-frontend/src/data/services/software-dev.ts
  - buildera-frontend/src/data/services/hire-a-developer.ts
  - buildera-frontend/src/data/solutions/solutions.ts
  - buildera-frontend/src/components/ui/AnimatedRingStat.tsx
  - buildera-frontend/src/components/ui/StaggeredRevealGrid.tsx
  - buildera-frontend/src/components/ui/SequentialSteps.tsx
  - buildera-frontend/src/components/ui/TechStaggerGrid.tsx
  - buildera-frontend/src/components/ui/VideoEmbed.tsx
  - buildera-frontend/src/components/ui/Breadcrumb.tsx
  - buildera-frontend/src/components/sections/service/ServiceHero.tsx
  - buildera-frontend/src/components/sections/service/ServicePainPoints.tsx
  - buildera-frontend/src/components/sections/service/ServiceOutcomeCards.tsx
  - buildera-frontend/src/components/sections/service/ServiceProcess.tsx
  - buildera-frontend/src/components/sections/service/ServiceIndustries.tsx
  - buildera-frontend/src/components/sections/service/ServiceRelatedSolutions.tsx
  - buildera-frontend/src/components/sections/service/ServiceCaseStudy.tsx
  - buildera-frontend/src/components/sections/service/ServiceTestimonials.tsx
  - buildera-frontend/src/components/sections/service/ServiceFaq.tsx
  - buildera-frontend/src/components/sections/service/ServiceCta.tsx
  - buildera-frontend/src/components/sections/service/TechShowcase.tsx
  - buildera-frontend/src/components/sections/solution/SolutionHero.tsx
  - buildera-frontend/src/components/sections/solution/SolutionProblem.tsx
  - buildera-frontend/src/components/sections/solution/SolutionFeatures.tsx
  - buildera-frontend/src/components/sections/solution/SolutionIndustries.tsx
  - buildera-frontend/src/components/sections/solution/SolutionRelatedServices.tsx
  - buildera-frontend/src/components/sections/solution/SolutionCaseStudy.tsx
  - buildera-frontend/src/components/sections/solution/SolutionTestimonials.tsx
  - buildera-frontend/src/components/sections/solution/SolutionCta.tsx
  - buildera-frontend/src/components/layouts/ServiceDetailLayout.tsx
  - buildera-frontend/src/components/layouts/SolutionDetailLayout.tsx
  - buildera-frontend/src/lib/api.ts
  - buildera-frontend/src/app/services/page.tsx
  - buildera-frontend/src/app/services/[category]/page.tsx
  - buildera-frontend/src/app/services/[category]/[slug]/page.tsx
  - buildera-frontend/src/app/solutions/page.tsx
  - buildera-frontend/src/app/solutions/[slug]/page.tsx
requirements_addressed:
  - SVC-01
  - SVC-02
  - SVC-03
  - SOL-01
  - SOL-02
  - COMP-04
  - COMP-05
autonomous: true

must_haves:
  truths:
    - "ServiceDetailLayout renders all 10 sections in correct order"
    - "SolutionDetailLayout renders all 8 sections in correct order"
    - "AnimatedRingStat accepts value+suffix+label props and counts up"
    - "generateStaticParams returns correct combos for nested routes"
    - "npm run build passes with 0 pre-rendered service/solution pages (empty stubs)"
    - "TechShowcase renders category headers as plain h3 with animation in TechStaggerGrid leaf only"
    - "All animated leaf components are use client; all layouts and sections are Server Components"
  artifacts:
    - path: "buildera-frontend/src/types/service-page.ts"
      provides: "ServicePageData, SolutionPageData, TestimonialData, CaseStudyData interfaces"
    - path: "buildera-frontend/src/components/layouts/ServiceDetailLayout.tsx"
      provides: "10-section Server Component layout shell"
    - path: "buildera-frontend/src/components/layouts/SolutionDetailLayout.tsx"
      provides: "8-section Server Component layout shell"
    - path: "buildera-frontend/src/components/ui/AnimatedRingStat.tsx"
      provides: "Extended ring stat with value+suffix+label+size props"
    - path: "buildera-frontend/src/data/services/index.ts"
      provides: "allServices array, getServicesByCategory helper, CATEGORY_LABELS map"
  key_links:
    - from: "src/app/services/[category]/[slug]/page.tsx"
      to: "src/data/services/index.ts"
      via: "import allServices"
    - from: "src/app/services/[category]/[slug]/page.tsx"
      to: "src/lib/api.ts"
      via: "fetchTestimonials, fetchCaseStudies"
    - from: "src/components/layouts/ServiceDetailLayout.tsx"
      to: "src/components/ui/AnimatedRingStat.tsx"
      via: "ServiceHero renders AnimatedRingStat leaf"
    - from: "src/app/solutions/[slug]/page.tsx"
      to: "src/components/layouts/SolutionDetailLayout.tsx"
      via: "render SolutionDetailLayout with data+testimonials+caseStudy props"
---

<objective>
Build the complete service/solution page template system: TypeScript interfaces, all animated leaf components, 10+8 section components, both layout shells, route templates with generateStaticParams, API fetch helpers, data file stubs, services overview page, and solutions listing page. After this plan every downstream data-authoring plan (04-02 through 04-05) can fill in data and immediately see pages.

Purpose: This is the foundation plan. Every other Phase 4 plan depends on the types, components, and routes created here. Stubs returning empty arrays let `npm run build` pass immediately so plans 04-02 through 04-05 can add data incrementally.

Output: ~42 new/modified files. All route templates, layouts, section components, animated leaf nodes, breadcrumb, video embed, API helpers, and data stubs.
</objective>

<execution_context>
@.planning/phases/04-services-solutions-pages/04-RESEARCH.md
@.planning/phases/04-services-solutions-pages/04-UI-SPEC.md
@.planning/phases/04-services-solutions-pages/04-PATTERNS.md
@.planning/phases/04-services-solutions-pages/04-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing interfaces from src/lib/api.ts (lines 26-76) that executor must extend -->

From buildera-frontend/src/lib/api.ts:
```typescript
export async function fetchFromApi<T>(path: string, options?: RequestInit): Promise<T>
export async function fetchNavItems(): Promise<NavItem[]>
export async function fetchFooterLinks(): Promise<FooterLink[]>
export async function fetchSettings(): Promise<Settings>
export const SETTINGS_FALLBACK: Settings
```

From buildera-frontend/src/components/ui/AnimatedRingStat.tsx (CURRENT — will be modified):
```typescript
// Current props: { percentage: number; label: string }
// Will become: { value: number; suffix: string; label: string; size?: number }
```

From buildera-frontend/src/components/ui/AnimatedCounter.tsx (pattern source — do NOT modify):
```typescript
// Props: { target: number; suffix?: string; delay?: number }
// Uses: useMotionValue, useSpring, useTransform, useInView, useReducedMotion
// Spring config: { stiffness: 60, damping: 20 }
```

From buildera-frontend/src/components/ui/TestimonialCard.tsx (reuse — do NOT modify):
```typescript
interface Props {
  quote: string; name: string; title: string; company: string; rating: number; className?: string
}
```

From buildera-frontend/src/components/ui/SolutionTile.tsx (reuse — do NOT modify):
```typescript
interface Props {
  name: string; slug: string; icon: React.ReactNode; className?: string
}
```
</interfaces>
</context>

<tasks>

<task id="04-01-01" type="execute">
  <title>Create TypeScript interfaces + data file stubs + API helpers</title>
  <read_first>
    - buildera-frontend/src/lib/api.ts
    - buildera-frontend/src/components/ui/AnimatedRingStat.tsx
    - buildera-frontend/src/components/ui/TestimonialCard.tsx
    - .planning/phases/04-services-solutions-pages/04-RESEARCH.md (TypeScript Interface Design section)
    - .planning/phases/04-services-solutions-pages/04-CONTEXT.md (D-01 through D-10)
    - .planning/phases/04-services-solutions-pages/04-UI-SPEC.md (Breadcrumb Contract, Component Inventory)
  </read_first>
  <action>
    **1. Create `buildera-frontend/src/types/service-page.ts`** per D-05. Define and export all these interfaces:
    - `HeroStat` with fields: `value: number`, `suffix: string`, `label: string`
    - `OutcomeCard` with fields: `title: string`, `description: string`, `metric?: string`, `icon?: string`
    - `ProcessStep` with fields: `stepNumber: number`, `title: string`, `description: string`
    - `FaqItem` with fields: `question: string`, `answer: string`
    - `Technology` with fields: `name: string`, `icon?: string`, `category: 'frontend' | 'backend' | 'cloud-devops' | 'tools'`
    - `ServicePageData` with ALL required fields from RESEARCH.md: `categorySlug`, `slug`, `heroHeadline`, `heroSubheadline`, `heroCta` (default "Book a Free Call"), `heroStat: HeroStat`, `painPoints: string[]`, `outcomeCards: OutcomeCard[]`, `processSteps: ProcessStep[]`, `industriesServed: string[]`, `relatedSolutions: string[]`, `faqItems: FaqItem[]`, `ctaHeadline: string`, `ctaSubtext?: string`, `technologies: Technology[]`, `videoUrl?: string`
    - `SolutionPageData` with fields: `slug`, `title`, `heroHeadline`, `heroSubheadline`, `problemPoints: string[]`, `featureCards: OutcomeCard[]`, `industriesServed: string[]`, `relatedServices: string[]`, `ctaHeadline: string`
    - `TestimonialData` with fields: `id: number`, `client_name: string`, `client_title?: string`, `client_company?: string`, `content: string`, `rating: number`
    - `CaseStudyData` with fields: `id: number`, `title: string`, `slug: string`, `client_name: string`, `industry: string`, `challenge: string`, `solution: string`, `results: string`
    Use `export interface` syntax (not `type`). `Technology.category` must be a union literal type.

    **2. Create data file stubs** per D-01, D-02, D-03:
    - `buildera-frontend/src/data/services/website-development.ts` — export `websiteDevelopmentServices: ServicePageData[]` as empty array `[]`
    - `buildera-frontend/src/data/services/salesforce.ts` — export `salesforceServices: ServicePageData[]` as `[]`
    - `buildera-frontend/src/data/services/devops.ts` — export `devopsServices: ServicePageData[]` as `[]`
    - `buildera-frontend/src/data/services/ai-agent.ts` — export `aiAgentServices: ServicePageData[]` as `[]`
    - `buildera-frontend/src/data/services/software-dev.ts` — export `softwareDevServices: ServicePageData[]` as `[]`
    - `buildera-frontend/src/data/services/hire-a-developer.ts` — export `hireADeveloperServices: ServicePageData[]` as `[]`
    - `buildera-frontend/src/data/services/index.ts` — import all 6 category arrays, spread into `export const allServices: ServicePageData[]`. Add `export function getServicesByCategory(categorySlug: string): ServicePageData[]` that filters by `categorySlug`. Add `export const CATEGORY_LABELS: Record<string, string>` mapping the 6 canonical slugs (per D-09: `website-development`, `salesforce-development`, `devops-development`, `ai-agent-development`, `software-development`, `hire-a-developer`) to human-readable names ("Website Development", "Salesforce Development", "DevOps Development", "AI Agent Development", "Software Development", "Hire a Developer"). Export `CATEGORY_SLUGS` as `string[]` of the 6 slugs.
    - `buildera-frontend/src/data/solutions/solutions.ts` — export `solutions: SolutionPageData[]` as empty array `[]`

    **3. Extend `buildera-frontend/src/lib/api.ts`** with two new fetch helpers:
    - `fetchTestimonials(filters: { service?: string; solution?: string; industry?: string } = {}): Promise<TestimonialData[]>` — builds URLSearchParams from provided filter keys, calls `fetchFromApi<TestimonialData[]>('/api/testimonials${query}', { next: { tags: ['testimonials'], revalidate: 3600 } } as RequestInit)`, `.catch(() => [])`. Import `TestimonialData` from `@/types/service-page`.
    - `fetchCaseStudies(filters: { service?: string; solution?: string; industry?: string } = {}): Promise<CaseStudyData[]>` — same pattern, endpoint `/api/case-studies`, tag `['case-studies']`. Import `CaseStudyData` from `@/types/service-page`.

    **4. Create `buildera-frontend/src/components/ui/Breadcrumb.tsx`** — Server Component (no "use client"). Props: `items: BreadcrumbItem[]` where `BreadcrumbItem` is `{ label: string; href?: string }`. Renders `<nav aria-label="Breadcrumb">` with `<ol>` using flexbox, `/` separators, last item has `aria-current="page"` and no link. Use label size (14px = `text-sm`), `text-muted-foreground`, current page item `text-foreground font-medium`. Container: `py-3 container mx-auto px-8 max-w-7xl`.

    **5. Create `buildera-frontend/src/components/ui/VideoEmbed.tsx`** — Server Component. Props: `{ url: string }`. Internal `getEmbedUrl(url)` function with allowlist for YouTube (`youtube.com/watch?v=` and `youtu.be/` patterns) and Vimeo (`vimeo.com/` pattern). Returns embed URL or `null`. If `null`, component returns `null`. Otherwise renders `<section className="py-16"><div className="container mx-auto px-8 max-w-4xl">` with `aspect-video rounded-xl overflow-hidden shadow-lg` wrapper, `<iframe>` with `loading="lazy"`, `allowFullScreen`, `title="Service explainer video"`.
  </action>
  <acceptance_criteria>
    - `buildera-frontend/src/types/service-page.ts` exports ServicePageData, SolutionPageData, TestimonialData, CaseStudyData, HeroStat, OutcomeCard, ProcessStep, FaqItem, Technology
    - Technology.category typed as `'frontend' | 'backend' | 'cloud-devops' | 'tools'`
    - `src/data/services/index.ts` exports `allServices`, `getServicesByCategory`, `CATEGORY_LABELS`, `CATEGORY_SLUGS`
    - All 6 service data files export typed empty arrays
    - `src/data/solutions/solutions.ts` exports empty array
    - `src/lib/api.ts` exports `fetchTestimonials` and `fetchCaseStudies` with correct filter params
    - `Breadcrumb.tsx` has `aria-label="Breadcrumb"` and `aria-current="page"` on last item
    - `VideoEmbed.tsx` returns null for non-YouTube/non-Vimeo URLs
    - `npx tsc --noEmit` passes in `buildera-frontend/`
  </acceptance_criteria>
</task>

<task id="04-01-02" type="execute">
  <title>Build all animated leaf components and extend AnimatedRingStat</title>
  <read_first>
    - buildera-frontend/src/components/ui/AnimatedRingStat.tsx (CURRENT — will be rewritten)
    - buildera-frontend/src/components/ui/AnimatedCounter.tsx (count-up pattern source)
    - buildera-frontend/src/components/ui/FeatureCheckList.tsx (sequential row animation pattern)
    - buildera-frontend/src/components/sections/SolutionsGridSection.tsx (stagger pattern)
    - .planning/phases/04-services-solutions-pages/04-PATTERNS.md (all animation pattern assignments)
    - .planning/phases/04-services-solutions-pages/04-UI-SPEC.md (Animation Contract section)
  </read_first>
  <action>
    **1. Extend `buildera-frontend/src/components/ui/AnimatedRingStat.tsx`** (per D-16, PATTERNS.md):
    - Change props interface from `{ percentage: number; label: string }` to `{ value: number; suffix: string; label: string; size?: number }`. Default `size` to 120 (the SVG viewBox width, not the wrapper width).
    - Keep SVG ring parameters EXACTLY as they are: `cx="60" cy="60" r="52" strokeWidth="8" strokeDasharray="326.7"`. Track ring stroke: `hsl(217 91% 60% / 15%)`. Animated ring stroke: `var(--brand-primary)`.
    - Change `pathLength: percentage / 100` to `pathLength: 1` (always full ring for hero stat).
    - Replace the static `<text>` center element with a count-up counter using the exact pattern from `AnimatedCounter.tsx`: `useMotionValue(0)`, `useSpring(count, { stiffness: 60, damping: 20 })`, `useTransform(spring, (v) => Math.round(v).toString() + suffix)`. Use `useInView(ref, { once: true })` on a wrapper div ref. Set `count.set(value)` when `isInView` becomes true. Render via `<foreignObject x="20" y="35" width="80" height="50">` containing a centered `<motion.span>` showing the rounded value.
    - Keep `useReducedMotion()` — gate animation behind it. If prefersReducedMotion, show static value immediately (set count to value, pathLength to 1 with duration 0).
    - Remove the `CHECKLIST_ITEMS` array and the checklist rendering below the ring — those were homepage-specific. The Phase 4 component is ring + label only.
    - Wrapper responsiveness: add `className` prop. The caller sets `w-[100px] md:w-[140px]` on the wrapper to handle responsive sizing. SVG uses `width="100%" height="100%" viewBox="0 0 120 120"`.
    - Label below ring: `<p className="text-sm font-medium text-center mt-2">{label}</p>`.
    - Keep `style={{ rotate: -90, transformOrigin: "60px 60px" }}` on `motion.circle` — starts ring at 12 o'clock.

    **2. Create `buildera-frontend/src/components/ui/StaggeredRevealGrid.tsx`** ("use client"):
    - Props: `{ children: React.ReactNode[]; delayMs?: number; className?: string }`. Default `delayMs` to 80.
    - Import `motion, useReducedMotion` from `"motion/react"`.
    - Wrap each child in `motion.div` with `initial={{ opacity: 0, y: 24 }}`, `whileInView={{ opacity: 1, y: 0 }}`, `viewport={{ once: true, margin: "-100px" }}`, `transition={{ delay: i * (delayMs / 1000), duration: 0.5, ease: "easeOut" }}`.
    - If `prefersReducedMotion`, render children without motion wrapper.
    - Outer container is a `<div className={className}>`.

    **3. Create `buildera-frontend/src/components/ui/SequentialSteps.tsx`** ("use client"):
    - Props: `{ steps: ProcessStep[] }` (import `ProcessStep` from `@/types/service-page`).
    - Import `motion, useInView, useReducedMotion` from `"motion/react"`, `useRef` from `"react"`.
    - Single `useInView(ref, { once: true, margin: "-80px" })` on parent div — all step animations triggered from this one ref.
    - Each step: `motion.div` with `initial={{ opacity: 0, x: -16 }}`, `animate={isInView ? { opacity: 1, x: 0 } : {}}`, `transition={{ delay: i * 0.12, duration: 0.4, ease: "easeOut" }}`.
    - Connector line between steps (not after last): `motion.div` with `initial={{ scaleY: 0 }}`, `animate={isInView ? { scaleY: 1 } : {}}`, `transition={{ delay: i * 0.12 + 0.4, duration: 0.3, ease: "easeOut" }}`, `style={{ transformOrigin: "top" }}`. Styled as `absolute left-6 top-12 w-0.5 h-8 bg-[var(--brand-primary)]`.
    - Step circle: `w-12 h-12 rounded-full bg-[var(--brand-primary)] text-white flex items-center justify-center font-bold text-xl flex-shrink-0`.
    - Layout: each step `flex gap-4 mb-8`. Step title: `text-xl font-semibold`. Step description: `text-base text-muted-foreground mt-1`.
    - If `prefersReducedMotion`, render static (no animation).

    **4. Create `buildera-frontend/src/components/ui/TechStaggerGrid.tsx`** ("use client"):
    - Props: `{ technologies: Technology[]; categoryStartDelay?: number }` (import `Technology` from `@/types/service-page`).
    - Import `motion` from `"motion/react"`.
    - Each tech badge: `motion.div` with `initial={{ opacity: 0, scale: 0.9 }}`, `whileInView={{ opacity: 1, scale: 1 }}`, `viewport={{ once: true }}`, `transition={{ delay: categoryStartDelay + i * 0.06, duration: 0.3, ease: "easeOut" }}`.
    - Badge styling: `flex items-center gap-2 px-3 py-1.5 rounded-full border border-border text-sm font-medium bg-background hover:border-[var(--brand-primary)] transition-colors duration-200`.
    - Render icon using `@tabler/icons-react` dynamic lookup if `tech.icon` is provided (use a TECH_ICON_MAP mapping icon name strings to tabler components for common tech: `IconBrandReact`, `IconBrandNextjs`, `IconBrandTypescript`, `IconBrandTailwind`, `IconBrandLaravel`, `IconBrandNodejs`, `IconBrandPhp`, `IconBrandPython`, `IconBrandAws`, `IconBrandDocker`, `IconCloud`, `IconDatabase`, `IconBrandSalesforce`, `IconApi`). If no icon match, render text-only badge.
    - Container: `flex flex-wrap gap-3`.
  </action>
  <acceptance_criteria>
    - `AnimatedRingStat.tsx` has props `{ value: number; suffix: string; label: string; size?: number; className?: string }`
    - `AnimatedRingStat.tsx` uses `useMotionValue`, `useSpring`, `useTransform` for count-up (matching AnimatedCounter pattern)
    - `AnimatedRingStat.tsx` SVG ring uses `pathLength: 1` (not percentage-based)
    - `AnimatedRingStat.tsx` has NO `CHECKLIST_ITEMS` — ring + label only
    - `StaggeredRevealGrid.tsx` has `"use client"` directive and 80ms default stagger
    - `SequentialSteps.tsx` has `"use client"` directive and single `useInView` ref
    - `TechStaggerGrid.tsx` has `"use client"` directive and 60ms per-logo stagger
    - All four components import from `"motion/react"` (NOT `"framer-motion"`)
    - `npx tsc --noEmit` passes in `buildera-frontend/`
  </acceptance_criteria>
</task>

<task id="04-01-03" type="execute">
  <title>Build 10 service section components, ServiceDetailLayout, and service route pages</title>
  <read_first>
    - buildera-frontend/src/types/service-page.ts (just created in 04-01-01)
    - buildera-frontend/src/components/ui/AnimatedRingStat.tsx (just modified in 04-01-02)
    - buildera-frontend/src/components/ui/StaggeredRevealGrid.tsx (just created in 04-01-02)
    - buildera-frontend/src/components/ui/SequentialSteps.tsx (just created in 04-01-02)
    - buildera-frontend/src/components/ui/TechStaggerGrid.tsx (just created in 04-01-02)
    - buildera-frontend/src/components/ui/Breadcrumb.tsx (just created in 04-01-01)
    - buildera-frontend/src/components/ui/VideoEmbed.tsx (just created in 04-01-01)
    - buildera-frontend/src/components/ui/TestimonialCard.tsx (reuse — props: quote, name, title, company, rating)
    - buildera-frontend/src/components/sections/CTASection.tsx (gradient CTA pattern to follow)
    - buildera-frontend/src/components/sections/ServicesTabSection.tsx (reuse for SVC-01)
    - buildera-frontend/src/lib/api.ts (fetchTestimonials, fetchCaseStudies)
    - buildera-frontend/src/data/services/index.ts (allServices, getServicesByCategory, CATEGORY_LABELS, CATEGORY_SLUGS)
    - .planning/phases/04-services-solutions-pages/04-UI-SPEC.md (Service section layout specs — MANDATORY)
    - .planning/phases/04-services-solutions-pages/04-PATTERNS.md (section wrapper patterns, hover card effects)
    - .planning/phases/04-services-solutions-pages/04-CONTEXT.md (D-18 use client boundary rules, D-20 TechShowcase)
  </read_first>
  <action>
    **Part A: 10 Service Section Components** (all Server Components unless noted)

    Create these in `buildera-frontend/src/components/sections/service/`:

    1. **ServiceHero.tsx** — Server Component. Props: `{ data: ServicePageData }`. 2-column grid (`grid grid-cols-1 md:grid-cols-2 gap-16`) per UI-SPEC. Left column: eyebrow (`text-xs font-medium uppercase tracking-widest text-[var(--brand-primary)]`), H1 headline (`text-4xl md:text-5xl font-bold tracking-tight`), sub-headline (`text-lg text-muted-foreground max-w-xl`), CTA row (Link to `/book-a-call` primary, Link to `/case-studies` outline), social proof badge strip "150+ Projects | 50+ Clients | 98% Satisfaction". Right column: `<AnimatedRingStat value={data.heroStat.value} suffix={data.heroStat.suffix} label={data.heroStat.label} className="w-[100px] md:w-[140px]" />` centered. Section `py-20 md:py-24 bg-background`.

    2. **ServicePainPoints.tsx** — Server Component. Props: `{ points: string[] }`. Background `bg-[var(--brand-surface)]`. Eyebrow "COMMON CHALLENGES", H2 "Sound Familiar?". 3-card grid `grid-cols-1 md:grid-cols-3 gap-6`. Card: `bg-background rounded-xl p-6 border border-border`.

    3. **ServiceOutcomeCards.tsx** — Server Component shell. Props: `{ cards: OutcomeCard[] }`. Background `bg-background`. Eyebrow "WHAT YOU GET", H2 "The Outcomes We Deliver". Wraps cards in `<StaggeredRevealGrid className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">`. Each card glassmorphism: style `background: var(--brand-glass); backdrop-filter: blur(12px); border: 1px solid hsl(217 91% 60% / 15%);` + `rounded-xl p-6`. Icon, H4 title, body description, optional metric badge `text-sm font-medium bg-[hsl(217_91%_60%/10%)] text-[var(--brand-primary)] px-2 py-1 rounded-full`.

    4. **ServiceProcess.tsx** — Server Component shell. Props: `{ steps: ProcessStep[] }`. Background `bg-[var(--brand-surface)]`. Eyebrow "HOW WE WORK", H2 "Our Delivery Process". Renders `<SequentialSteps steps={steps} />`.

    5. **ServiceIndustries.tsx** — Server Component. Props: `{ industries: string[] }`. If empty, return null. Background `bg-background`. Eyebrow "INDUSTRIES WE SERVE", H2 "Built for Your Industry". `grid-cols-2 md:grid-cols-4 gap-4` Link cards to `/industries/{slug}`. Local `INDUSTRY_LABELS` map: manufacturing="Manufacturing", retail="Retail & E-Commerce", hospitality="Hospitality", logistics="Logistics", finance="Finance", healthcare="Healthcare", real-estate="Real Estate", professional-services="Professional Services". Card: `bg-background border border-border rounded-xl p-4 hover:border-[var(--brand-primary)] transition-colors`.

    6. **ServiceRelatedSolutions.tsx** — Server Component. Props: `{ solutions: string[] }`. If empty, return null. Background `bg-[var(--brand-surface)]`. Eyebrow "RELATED SOLUTIONS", H2 "Tools We Build for This Service". Link cards to `/solutions/{slug}` using local `SOLUTION_LABELS` map (all 18 slugs from RESEARCH.md content outline).

    7. **ServiceCaseStudy.tsx** — Server Component. Props: `{ caseStudy: CaseStudyData | null }`. If null, return null. Background `bg-background`. Eyebrow "CLIENT STORY", H2 "From Challenge to Result". Full-width card: client_name, industry badge, results metric `text-4xl font-bold text-[var(--brand-primary)]`, challenge, solution, "Read Full Case Study →" link to `/case-studies/{slug}`. Card: `bg-background border border-border rounded-2xl p-8`.

    8. **ServiceTestimonials.tsx** — Server Component. Props: `{ testimonials: TestimonialData[] }`. If empty, return null. Background `bg-background`. Eyebrow "WHAT OUR CLIENTS SAY", H2 "Trusted by Businesses Like Yours". 1-2 `<TestimonialCard>` in `grid-cols-1 md:grid-cols-2 gap-6`. Map: `quote={t.content}`, `name={t.client_name}`, `title={t.client_title ?? ""}`, `company={t.client_company ?? ""}`, `rating={t.rating}`.

    9. **ServiceFaq.tsx** — Server Component. Props: `{ items: FaqItem[]; headline?: string }`. Background `bg-[var(--brand-surface)]`. Eyebrow "FREQUENTLY ASKED QUESTIONS". Run `npx shadcn add accordion` to install shadcn accordion. Use `Accordion type="single" collapsible`. Question in trigger `text-xl font-semibold`; answer in content `text-base text-muted-foreground`.

    10. **ServiceCta.tsx** — Server Component. Props: `{ headline: string; subtext?: string }`. Gradient `style={{ background: "linear-gradient(135deg, var(--brand-gradient-from), var(--brand-gradient-to))" }}`. Eyebrow "READY TO START?", H2, optional subtext, CTA button `bg-white text-[var(--brand-primary)] font-semibold px-6 py-3 rounded-lg` to `/book-a-call`. Secondary "View Our Work" to `/case-studies`.

    11. **TechShowcase.tsx** — Server Component (D-20). Props: `{ technologies: Technology[] }`. Group by category. Category header: plain `<h3>` (NOT motion.h3 — STATE.md constraint). `<h3 className="text-sm font-medium uppercase tracking-widest text-[var(--brand-primary)] mb-3">`. Each group renders `<TechStaggerGrid technologies={group.techs} categoryStartDelay={catIdx * 0.1 + 0.1} />`. Eyebrow "OUR TECHNOLOGY STACK", H2 "Technologies We Use", `space-y-6` between groups.

    **Part B: ServiceDetailLayout + Service Routes**

    1. **`buildera-frontend/src/components/layouts/ServiceDetailLayout.tsx`** — Server Component. Props: `{ data: ServicePageData; testimonials: TestimonialData[]; caseStudy: CaseStudyData | null }`. Renders `<main>`: Breadcrumb (Home/Services/CategoryName/ServiceName), ServiceHero, ServicePainPoints, ServiceOutcomeCards, ServiceProcess, `{data.videoUrl && <VideoEmbed url={data.videoUrl} />}`, ServiceIndustries, ServiceRelatedSolutions, `{caseStudy && <ServiceCaseStudy>}`, `{testimonials.length > 0 && <ServiceTestimonials>}`, ServiceFaq, TechShowcase, ServiceCta.

    2. **`buildera-frontend/src/app/services/page.tsx`** (SVC-01) — Server Component. Import `ServicesTabSection`. Render `<main>` with eyebrow "SERVICES", H1 "Our Services", then `<ServicesTabSection />`.

    3. **`buildera-frontend/src/app/services/[category]/page.tsx`** (SVC-02) — Async Server Component. `params: Promise<{ category: string }>`, `await params`. `generateStaticParams` returns `CATEGORY_SLUGS.map(s => ({ category: s }))`. `getServicesByCategory(category)`. If category not in CATEGORY_SLUGS → `notFound()`. Render: Breadcrumb, category hero H1 from CATEGORY_LABELS + description per UI-SPEC copywriting table, sub-service cards in StaggeredRevealGrid, category CTA.

    4. **`buildera-frontend/src/app/services/[category]/[slug]/page.tsx`** (SVC-03) — Async Server Component. `params: Promise<{ category: string; slug: string }>`, `await params`. `generateStaticParams` → `allServices.map(s => ({ category: s.categorySlug, slug: s.slug }))`. Find service; if not found → `notFound()`. `Promise.all([fetchTestimonials({ service: category }), fetchCaseStudies({ service: category })])`. Render `<ServiceDetailLayout data={service} testimonials={testimonials.slice(0, 2)} caseStudy={caseStudies[0] ?? null} />`.
  </action>
  <acceptance_criteria>
    - All 10 service section files exist in `src/components/sections/service/`
    - `TechShowcase.tsx` uses plain `<h3>` (NOT `motion.h3`)
    - `ServiceDetailLayout.tsx` has no "use client" directive and renders all 10 sections
    - `app/services/page.tsx` imports and renders `ServicesTabSection`
    - `app/services/[category]/page.tsx` has `generateStaticParams` returning 6 slugs
    - `app/services/[category]/[slug]/page.tsx` has `params: Promise<{ category: string; slug: string }>` and `await params`
    - `ServiceCaseStudy` and `ServiceTestimonials` return null when data is null/empty
    - `npx tsc --noEmit` passes in `buildera-frontend/`
  </acceptance_criteria>
</task>

<task id="04-01-04" type="execute">
  <title>Build 8 solution section components, SolutionDetailLayout, and solution route pages</title>
  <read_first>
    - buildera-frontend/src/types/service-page.ts (created in 04-01-01)
    - buildera-frontend/src/components/ui/StaggeredRevealGrid.tsx (created in 04-01-02)
    - buildera-frontend/src/components/ui/Breadcrumb.tsx (created in 04-01-01)
    - buildera-frontend/src/components/ui/TestimonialCard.tsx (reuse)
    - buildera-frontend/src/components/sections/service/ServiceCta.tsx (just created in 04-01-03 — reuse gradient CTA pattern)
    - buildera-frontend/src/components/sections/service/ServiceCaseStudy.tsx (just created — same spec)
    - buildera-frontend/src/components/sections/service/ServiceTestimonials.tsx (just created — same spec)
    - buildera-frontend/src/lib/api.ts (fetchCaseStudies, fetchTestimonials)
    - buildera-frontend/src/data/solutions/solutions.ts (solutions stub)
    - .planning/phases/04-services-solutions-pages/04-UI-SPEC.md (Solution section layout specs — MANDATORY)
    - .planning/phases/04-services-solutions-pages/04-CONTEXT.md (D-17 no ring stats/bar charts on solution pages)
  </read_first>
  <action>
    **Part A: 8 Solution Section Components** in `buildera-frontend/src/components/sections/solution/`

    All Server Components. No glassmorphism on solution pages (D-17): cards use `bg-background border border-border rounded-xl p-6`.

    1. **SolutionHero.tsx** — Props: `{ data: SolutionPageData }`. Single-column (no ring stat per D-17). Eyebrow "SOLUTION". H1 `text-4xl md:text-5xl font-bold tracking-tight`, sub-headline `text-lg text-muted-foreground`, CTAs ("Book a Free Call" to `/book-a-call` primary, "See Related Services" outline to `/services`). `py-16 md:py-20 bg-background`. `max-w-3xl`.

    2. **SolutionProblem.tsx** — Props: `{ points: string[] }`. Background `bg-[var(--brand-surface)]`. Eyebrow "THE CHALLENGE", H2 "Does This Sound Like Your Business?". 3-card grid `grid-cols-1 md:grid-cols-3 gap-6`. Card: `bg-background rounded-xl p-6 border border-border`.

    3. **SolutionFeatures.tsx** — Server Component shell. Props: `{ title: string; cards: OutcomeCard[] }`. Background `bg-background`. Eyebrow "THE SOLUTION", H2 `How ${title} Transforms Your Business`. 4 cards in `<StaggeredRevealGrid className="grid grid-cols-1 md:grid-cols-2 gap-6">`. No glassmorphism.

    4. **SolutionIndustries.tsx** — Same as ServiceIndustries (same INDUSTRY_LABELS map, same grid, same card style). Props: `{ industries: string[] }`. If empty, return null.

    5. **SolutionRelatedServices.tsx** — Props: `{ services: string[] }`. Eyebrow "BUILT WITH THESE SERVICES", H2 "The Services Behind This Solution". `grid-cols-1 md:grid-cols-3 gap-4` Link cards to `/services/{categorySlug}`. Display label from CATEGORY_LABELS. Card: `bg-background border border-border rounded-xl p-5 hover:border-[var(--brand-primary)] transition-colors`.

    6. **SolutionCaseStudy.tsx** — Identical to ServiceCaseStudy. Props: `{ caseStudy: CaseStudyData | null }`. If null, return null. Background `bg-[var(--brand-surface)]`.

    7. **SolutionTestimonials.tsx** — Identical to ServiceTestimonials. Props: `{ testimonials: TestimonialData[] }`. If empty, return null. Background `bg-background`.

    8. **SolutionCta.tsx** — Identical to ServiceCta. Props: `{ headline: string }`. Same gradient background.

    **Part B: SolutionDetailLayout + Solution Routes**

    1. **`buildera-frontend/src/components/layouts/SolutionDetailLayout.tsx`** — Server Component. Props: `{ data: SolutionPageData; testimonials: TestimonialData[]; caseStudy: CaseStudyData | null }`. Renders `<main>`: Breadcrumb (3-item: Home/Solutions/SolutionName), SolutionHero, SolutionProblem, SolutionFeatures, SolutionIndustries, SolutionRelatedServices, `{caseStudy && <SolutionCaseStudy>}`, `{testimonials.length > 0 && <SolutionTestimonials>}`, SolutionCta.

    2. **`buildera-frontend/src/app/solutions/page.tsx`** (SOL-01) — Server Component. Import `solutions` from `@/data/solutions/solutions`. Eyebrow "SOLUTIONS", H1 "Find the Right Solution for Your Business", descriptor "We build purpose-built software for the specific problem you're trying to solve." Render 18-tile grid (`grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4`) with StaggeredRevealGrid using diagonal cascade: each tile `delay: (Math.floor(i/6) + (i%6)) * 0.05`. Each tile links to `/solutions/{s.slug}`. If solutions array empty, render header only.

    3. **`buildera-frontend/src/app/solutions/[slug]/page.tsx`** (SOL-02) — Async Server Component. `params: Promise<{ slug: string }>`, `await params`. `generateStaticParams` → `solutions.map(s => ({ slug: s.slug }))`. Find solution; if not found → `notFound()`. `Promise.all([fetchTestimonials({ solution: slug }), fetchCaseStudies({ solution: slug })])`. Render `<SolutionDetailLayout data={solution} testimonials={testimonials.slice(0, 2)} caseStudy={caseStudies[0] ?? null} />`.
  </action>
  <acceptance_criteria>
    - All 8 solution section files exist in `src/components/sections/solution/`
    - `SolutionDetailLayout.tsx` has no "use client" directive and renders all 8 sections
    - `SolutionCaseStudy` and `SolutionTestimonials` return null when data is null/empty
    - `app/solutions/[slug]/page.tsx` has `params: Promise<{ slug: string }>` and `await params`
    - `app/solutions/page.tsx` renders 18-tile grid with diagonal cascade stagger
    - `npm run build` passes in `buildera-frontend/` — 0 service/solution pages pre-rendered (empty stubs), 0 errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data file content to JSX | All marketing copy is hardcoded TypeScript strings — React auto-escapes |
| Video URL to iframe src | User-provided URL string in data file crosses into iframe embedding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | VideoEmbed.tsx | mitigate | Server-side getEmbedUrl() allowlist rejects unknown hosts; only YouTube and Vimeo produce embed URLs; returns null for all others |
| T-04-02 | Tampering | Data file marketing copy | accept | All copy is hardcoded TypeScript strings, not user input; React escapes automatically; no dangerouslySetInnerHTML |
| T-04-03 | Information Disclosure | fetchTestimonials/fetchCaseStudies | mitigate | .slice(0, 2) on testimonials limits response data shown; .catch(() => []) prevents error leakage |
</threat_model>

<verification>
- `npx tsc --noEmit` passes in buildera-frontend/ (TypeScript clean)
- `npm run build` passes in buildera-frontend/ — build completes with 0 errors
- `generateStaticParams` in service detail page returns empty array (since stubs are empty), build renders 0 service pages but no errors
- All animated components have `"use client"` directive
- All layout and section components do NOT have `"use client"` directive
- TechShowcase uses plain h3 tags (not motion.h3)
- AnimatedRingStat new props: value, suffix, label, size, className
</verification>

<success_criteria>
- All TypeScript interfaces defined and exported
- All data file stubs in place returning empty arrays
- API fetch helpers added to api.ts
- All 4 animated leaf components built with correct animation contracts
- All 10+8 section components built matching UI-SPEC exactly
- Both layout shells wired to sections
- All 5 route pages created with generateStaticParams
- Breadcrumb and VideoEmbed utility components in place
- `npm run build` passes cleanly
</success_criteria>

<output>
Create `.planning/phases/04-services-solutions-pages/04-01-SUMMARY.md` when done
</output>
