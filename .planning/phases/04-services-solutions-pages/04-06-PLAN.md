---
plan: "04-06"
wave: 3
phase: 4
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
  - "04-04"
  - "04-05"
files_modified:
  - buildera-backend/database/migrations/2026_05_27_000001_add_solution_slug_to_testimonials_and_case_studies.php
  - buildera-backend/app/Models/Testimonial.php
  - buildera-backend/app/Models/CaseStudy.php
  - buildera-backend/app/Http/Controllers/Api/TestimonialController.php
  - buildera-backend/app/Http/Controllers/Api/CaseStudyController.php
  - buildera-backend/database/seeders/CaseStudySeeder.php
  - buildera-backend/database/seeders/TestimonialSeeder.php
  - buildera-backend/database/seeders/DatabaseSeeder.php
requirements_addressed:
  - TRUST-06
autonomous: true

must_haves:
  truths:
    - "solution_slug column exists on both testimonials and case_studies tables"
    - "TestimonialController filters by ?service= (existing), ?industry= (existing), and ?solution= (new)"
    - "CaseStudyController filters by ?industry= (existing), ?service= (new), and ?solution= (new)"
    - "8 case studies seeded from CLIENT-CONTEXT.md with correct tag mapping"
    - "~20 testimonials seeded covering all 6 service categories"
    - "Social proof appears on at least one service page via API fetch in dev"
  artifacts:
    - path: "buildera-backend/database/migrations/2026_05_27_000001_add_solution_slug_to_testimonials_and_case_studies.php"
      provides: "solution_slug nullable string column on testimonials and case_studies"
    - path: "buildera-backend/database/seeders/CaseStudySeeder.php"
      provides: "8 seeded case study records from CLIENT-CONTEXT.md"
    - path: "buildera-backend/database/seeders/TestimonialSeeder.php"
      provides: "~20 seeded testimonial records across all service categories"
  key_links:
    - from: "TestimonialController"
      to: "Testimonial model"
      via: "where('solution_slug', request('solution'))"
    - from: "CaseStudyController"
      to: "CaseStudy model"
      via: "whereJsonContains('service_tags', request('service'))"
    - from: "DatabaseSeeder"
      to: "CaseStudySeeder + TestimonialSeeder"
      via: "$this->call([CaseStudySeeder::class, TestimonialSeeder::class])"
---

<objective>
Add the solution_slug migration, extend API controllers with new filter parameters, seed 8 case studies and ~20 testimonials, and wire social proof to appear on service/solution pages via API.

Purpose: This plan connects the frontend pages (built in 04-01 through 04-05) to real backend data. After this plan, visiting a service page in dev shows actual testimonials and case studies.

Output: Migration, model updates, controller extensions, 2 seeder files, DatabaseSeeder wiring.
</objective>

<execution_context>
@.planning/phases/04-services-solutions-pages/04-RESEARCH.md
@.planning/phases/04-services-solutions-pages/04-CONTEXT.md
@.planning/phases/04-services-solutions-pages/04-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CLIENT-CONTEXT.md

<interfaces>
From buildera-backend/app/Models/Testimonial.php (current $fillable):
```php
protected $fillable = [
    'client_name', 'client_title', 'client_company', 'client_photo',
    'content', 'rating', 'service_tags', 'industry_tags',
    'is_featured', 'is_published', 'sort_order',
];
// service_tags and industry_tags cast to 'array'
```

From buildera-backend/app/Models/CaseStudy.php (current $fillable):
```php
protected $fillable = [
    'title', 'slug', 'client_name', 'industry', 'challenge', 'solution', 'results',
    'featured_image', 'featured_image_alt', 'service_tags', 'industry_tags',
    'is_featured', 'is_published', 'published_at',
    'seo_title', 'seo_description', 'seo_og_image',
];
// service_tags and industry_tags cast to 'array'
```

From buildera-backend/app/Http/Controllers/Api/TestimonialController.php:
```php
// Already has: whereJsonContains('service_tags', $service) and whereJsonContains('industry_tags', $industry)
// Needs: where('solution_slug', $solution)
```

From buildera-backend/app/Http/Controllers/Api/CaseStudyController.php:
```php
// Already has: where('industry', request('industry'))
// Needs: whereJsonContains('service_tags', request('service'))
// Needs: where('solution_slug', request('solution'))
```

From buildera-backend/database/seeders/DatabaseSeeder.php:
```php
class DatabaseSeeder extends Seeder {
    use WithoutModelEvents;
    public function run(): void {
        User::factory()->create([...]);
        $this->call(SettingsSeeder::class);
    }
}
```
</interfaces>
</context>

<tasks>

<task id="04-06-01" type="execute">
  <title>Create migration, update models, extend controllers</title>
  <read_first>
    - buildera-backend/app/Models/Testimonial.php (current $fillable and $casts arrays)
    - buildera-backend/app/Models/CaseStudy.php (current $fillable and $casts arrays)
    - buildera-backend/app/Http/Controllers/Api/TestimonialController.php (existing filter pattern)
    - buildera-backend/app/Http/Controllers/Api/CaseStudyController.php (existing filter pattern)
    - buildera-backend/database/migrations/ (list existing migrations to determine filename timestamp convention)
    - .planning/phases/04-services-solutions-pages/04-RESEARCH.md (Critical Schema Conflict section — MANDATORY)
    - .planning/phases/04-services-solutions-pages/04-PATTERNS.md (migration pattern, controller extension patterns)
  </read_first>
  <action>
    **CRITICAL: Read the Schema Conflict resolution in RESEARCH.md before writing ANY migration code.**

    The existing schema already has `service_tags` (JSON) and `industry_tags` (JSON) on both tables. Do NOT add `service_category` or `industry_category` columns. Only `solution_slug` is missing.

    **1. Create migration** `buildera-backend/database/migrations/2026_05_27_000001_add_solution_slug_to_testimonials_and_case_studies.php`:
    - `up()`: `Schema::table('testimonials', fn (Blueprint $table) => $table->string('solution_slug')->nullable()->after('industry_tags'))` and same for `case_studies`
    - `down()`: `Schema::table('testimonials', fn (Blueprint $table) => $table->dropColumn('solution_slug'))` and same for `case_studies`
    - Use the anonymous class migration format matching existing project conventions: `return new class extends Migration { ... }`

    **2. Update `buildera-backend/app/Models/Testimonial.php`:**
    - Add `'solution_slug'` to the `$fillable` array (after `'industry_tags'`)
    - No cast needed for solution_slug — it's a plain string

    **3. Update `buildera-backend/app/Models/CaseStudy.php`:**
    - Add `'solution_slug'` to the `$fillable` array (after `'industry_tags'`)
    - No cast needed

    **4. Extend `buildera-backend/app/Http/Controllers/Api/TestimonialController.php`:**
    - Add a new `?solution=` filter block AFTER the existing `?industry=` filter block:
      `if (request()->has('solution')) { $query->where('solution_slug', request('solution')); }`
    - Keep existing `?service=` and `?industry=` filters EXACTLY as they are (they use `whereJsonContains`)

    **5. Extend `buildera-backend/app/Http/Controllers/Api/CaseStudyController.php`:**
    - Add `?service=` filter: `if (request()->has('service')) { $query->whereJsonContains('service_tags', request('service')); }`
    - Add `?solution=` filter: `if (request()->has('solution')) { $query->where('solution_slug', request('solution')); }`
    - Add both AFTER the existing `?industry=` filter block
    - Keep existing `?industry=` filter EXACTLY as-is
  </action>
  <acceptance_criteria>
    - Migration file exists and adds ONLY `solution_slug` (string, nullable) to both tables
    - Migration does NOT add `service_category` or `industry_category` columns
    - `Testimonial::$fillable` includes `'solution_slug'`
    - `CaseStudy::$fillable` includes `'solution_slug'`
    - `TestimonialController@index` responds to `?service=`, `?industry=`, and `?solution=` params
    - `CaseStudyController@index` responds to `?industry=`, `?service=`, and `?solution=` params
    - `?service=` on CaseStudyController uses `whereJsonContains('service_tags', ...)` (NOT `where('service_category', ...)`)
    - `php artisan migrate` runs without error in buildera-backend/
  </acceptance_criteria>
</task>

<task id="04-06-02" type="execute">
  <title>Create CaseStudySeeder and TestimonialSeeder, wire into DatabaseSeeder</title>
  <read_first>
    - .planning/CLIENT-CONTEXT.md (Previous Clients section — MANDATORY for case study content)
    - .planning/phases/04-services-solutions-pages/04-CONTEXT.md (D-11 canonical tag mapping table)
    - .planning/phases/04-services-solutions-pages/04-RESEARCH.md (Seeder Strategy section, Seeder Coverage Matrix)
    - .planning/phases/04-services-solutions-pages/04-PATTERNS.md (SettingsSeeder pattern, WithoutModelEvents, column names)
    - buildera-backend/database/seeders/SettingsSeeder.php (existing seeder pattern — array + foreach)
    - buildera-backend/database/seeders/DatabaseSeeder.php (current call list)
    - buildera-backend/app/Models/CaseStudy.php (HasSlug trait — slug auto-generated from title)
    - buildera-backend/app/Models/Testimonial.php ($fillable list)
  </read_first>
  <action>
    **1. Create `buildera-backend/database/seeders/CaseStudySeeder.php`:**
    - `use WithoutModelEvents;` trait (prevents observer side effects)
    - Namespace: `Database\Seeders`
    - Import: `App\Models\CaseStudy`, `Illuminate\Database\Seeder`, `Illuminate\Database\Console\Seeds\WithoutModelEvents`
    - 8 records, one per client from CLIENT-CONTEXT.md, following D-11 canonical tag mapping EXACTLY:

      | Client | title | client_name | industry | service_tags | solution_slug | industry_tags |
      |--------|-------|-------------|----------|-------------|---------------|---------------|
      | Saharsh Packaging | "Custom ERP System for Saharsh Packaging" | "Saharsh Packaging" | "manufacturing" | `["software-development"]` | "erp" | `["manufacturing"]` |
      | Ease My Hotel | "Hotel Management SaaS for Ease My Hotel" | "Ease My Hotel" | "hospitality" | `["software-development"]` | "hotels-resorts" | `["hospitality"]` |
      | Shivaansh / Equi Brief | "MVP Development for Equi Brief" | "Equi Brief" | "finance" | `["software-development"]` | "project-mgmt" | `["finance"]` |
      | Insurance Mgmt (PV Krishnan) | "Insurance CRM for PV Krishnan Associates" | "PV Krishnan Associates" | "finance" | `["software-development"]` | "crm" | `["finance"]` |
      | Aroma Monk | "E-Commerce Website for Aroma Monk" | "Aroma Monk" | "retail" | `["website-development"]` | "lead-mgmt" | `["retail"]` |
      | SRJ | "Industrial B2B Website for SRJ Industries" | "SRJ Industries" | "manufacturing" | `["website-development"]` | "vendor-mgmt" | `["manufacturing"]` |
      | Barrel Books | "Liquor Shop Stock Management SaaS" | "Barrel Books" | "retail" | `["software-development"]` | "warehouse-mgmt" | `["retail"]` |
      | GNC Exports | "Export Showcase Website for GNC Exports" | "GNC Exports" | "manufacturing" | `["website-development"]` | "supply-chain" | `["manufacturing"]` |

    - Each record must have: `challenge` (2-3 sentences describing the problem), `solution` (2-3 sentences describing what was built), `results` (specific metric like "60% reduction in stockouts" or "3x more wholesale inquiries"). Fabricate realistic details based on CLIENT-CONTEXT.md notes (packaging = inventory/order mgmt; Ease My Hotel = booking/revenue; Aroma Monk = perfume e-commerce B2B/B2C; SRJ = PHE plates B2B; Barrel Books = liquor stock tracking; GNC Exports = technical textiles B2B).
    - `service_tags` and `industry_tags` MUST use `json_encode([...])` — these are JSON columns
    - `solution_slug` is a plain string — no `json_encode`
    - All records: `is_published => true`, `published_at => now()->subMonths(rand(2, 6))`, `is_featured => true` (for first 3), `featured_image => null`, `featured_image_alt => null`
    - Use `CaseStudy::create($data)` in a foreach loop. The `HasSlug` trait auto-generates slug from title.

    **2. Create `buildera-backend/database/seeders/TestimonialSeeder.php`:**
    - `use WithoutModelEvents;` trait
    - ~20 testimonial records covering ALL 6 service categories with minimum counts per RESEARCH.md coverage matrix:
      - `website-development`: 4 testimonials
      - `salesforce-development`: 3 testimonials
      - `devops-development`: 3 testimonials
      - `ai-agent-development`: 3 testimonials
      - `software-development`: 4 testimonials
      - `hire-a-developer`: 3 testimonials
    - Each record: `client_name`, `client_title` (job title), `client_company`, `content` (2-3 sentence testimonial mentioning a specific outcome), `rating` (4 or 5), `service_tags` (JSON array via `json_encode`), `industry_tags` (JSON array via `json_encode`), `solution_slug` (nullable — set where a specific solution applies), `is_published => true`, `is_featured` (first 5 featured), `sort_order` (sequential 1-20)
    - Fabricate from real clients where plausible: Saharsh Packaging (Rajesh Kumar, Operations Manager), Ease My Hotel (Priya Sharma, Hotel Manager), Aroma Monk (Amit Patel, Founder), SRJ (Vikram Singh, Sales Director), Barrel Books (Nikhil Reddy, Owner), GNC Exports (Sanjay Gupta, Export Manager). Fill remaining ~14 with fictional but realistic Indian SMB names.
    - Cross-tag testimonials: a testimonial about website development for a manufacturing client gets `service_tags: ["website-development"]` AND `industry_tags: ["manufacturing"]`. This allows the same testimonial to appear on both a service page and an industry page.
    - Use `Testimonial::create($data)` in a foreach loop.

    **3. Wire into `buildera-backend/database/seeders/DatabaseSeeder.php`:**
    - Add to the `$this->call()` array: `CaseStudySeeder::class` and `TestimonialSeeder::class`
    - Add AFTER `SettingsSeeder::class`
    - Do NOT modify the existing `User::factory()->create(...)` line
    - The `DatabaseSeeder` already has `use WithoutModelEvents` — the new seeders should ALSO have their own `use WithoutModelEvents` for when they are called independently via `php artisan db:seed --class=CaseStudySeeder`
  </action>
  <acceptance_criteria>
    - `CaseStudySeeder.php` exists with `use WithoutModelEvents` trait and creates 8 records
    - `TestimonialSeeder.php` exists with `use WithoutModelEvents` trait and creates ~20 records
    - Case study records use `json_encode()` for `service_tags` and `industry_tags` columns
    - Case study records use plain string for `solution_slug` column
    - Testimonial records use `json_encode()` for `service_tags` and `industry_tags`
    - All records have `is_published => true` and valid `published_at` dates (for case studies)
    - All 8 case studies match the D-11 canonical tag mapping table exactly
    - Testimonial coverage: at least 3 records per service category (verified by counting `service_tags` arrays)
    - `DatabaseSeeder.php` calls both new seeders after `SettingsSeeder`
    - `php artisan db:seed --class=CaseStudySeeder` runs without error
    - `php artisan db:seed --class=TestimonialSeeder` runs without error
    - `GET /api/testimonials?service=website-development` returns at least 4 records
    - `GET /api/case-studies?service=software-development` returns at least 4 records
    - `GET /api/case-studies?solution=erp` returns at least 1 record
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API query params to SQL | Query params (?service=, ?solution=, ?industry=) used in Eloquent queries |
| Seeder data to database | Hardcoded PHP strings inserted into MySQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | API query filters | mitigate | Eloquent parameterized queries (whereJsonContains, where) prevent SQL injection; no raw queries |
| T-04-05 | Information Disclosure | Testimonial/CaseStudy API | accept | All returned data is public (published records only); no PII beyond client names that are already public |
| T-04-06 | Tampering | Seeder overwrite risk | mitigate | WithoutModelEvents trait prevents observer side effects; seeders use create() not truncate(); DatabaseSeeder preserves existing admin user |
</threat_model>

<verification>
- `php artisan migrate` runs without error (adds solution_slug to both tables)
- `php artisan db:seed --class=CaseStudySeeder` creates 8 records
- `php artisan db:seed --class=TestimonialSeeder` creates ~20 records
- `GET /api/testimonials?service=website-development` returns records (test in browser or curl)
- `GET /api/case-studies?service=software-development` returns records
- `GET /api/case-studies?solution=erp` returns at least 1 record
- `GET /api/testimonials?solution=crm` returns at least 1 record
- Frontend: `npm run dev` and visit `/services/website-development/custom-websites` — testimonials section and case study section render with seeded data
</verification>

<success_criteria>
- Migration adds only solution_slug to both tables
- Controllers filter by all 3 params (service, solution, industry)
- 8 case studies and ~20 testimonials seeded with correct tags
- Social proof appears on service pages when dev server is running with seeded backend
- No existing data corrupted (admin user, settings intact)
</success_criteria>

<output>
Create `.planning/phases/04-services-solutions-pages/04-06-SUMMARY.md` when done
</output>
