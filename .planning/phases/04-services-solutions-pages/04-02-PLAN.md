---
plan: "04-02"
wave: 2
phase: 4
depends_on:
  - "04-01"
files_modified:
  - buildera-frontend/src/data/services/website-development.ts
  - buildera-frontend/src/app/services/[category]/page.tsx
requirements_addressed:
  - SVC-04
autonomous: true

must_haves:
  truths:
    - "4 Website Development sub-service pages accessible at correct slugs"
    - "Each page has full persuasive marketing copy — not placeholder text"
    - "generateStaticParams produces 4 category+slug combos for website-development"
    - "npm run build pre-renders all 4 pages without error"
  artifacts:
    - path: "buildera-frontend/src/data/services/website-development.ts"
      provides: "4 ServicePageData objects with full marketing copy"
  key_links:
    - from: "src/data/services/website-development.ts"
      to: "src/data/services/index.ts"
      via: "imported and spread into allServices"
    - from: "src/app/services/[category]/[slug]/page.tsx"
      to: "src/data/services/index.ts"
      via: "generateStaticParams returns 4 combos"
---

<objective>
Write full persuasive marketing copy for all 4 Website Development sub-service pages. This is the first category to ship real content — it proves the template system works end-to-end.

Purpose: First complete data file validates the entire template pipeline. After this plan, 4 real pages are accessible at their canonical URLs.

Output: `website-development.ts` with 4 `ServicePageData` objects containing real marketing copy.
</objective>

<execution_context>
@.planning/phases/04-services-solutions-pages/04-RESEARCH.md
@.planning/phases/04-services-solutions-pages/04-UI-SPEC.md
@.planning/phases/04-services-solutions-pages/04-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From buildera-frontend/src/types/service-page.ts (created in 04-01):
```typescript
export interface ServicePageData {
  categorySlug: string;
  slug: string;
  heroHeadline: string;
  heroSubheadline: string;
  heroCta: string;
  heroStat: HeroStat;
  painPoints: string[];
  outcomeCards: OutcomeCard[];
  processSteps: ProcessStep[];
  industriesServed: string[];
  relatedSolutions: string[];
  faqItems: FaqItem[];
  ctaHeadline: string;
  ctaSubtext?: string;
  technologies: Technology[];
  videoUrl?: string;
}
```
</interfaces>
</context>

<tasks>

<task id="04-02-01" type="execute">
  <title>Write Website Development data file with 4 full sub-service page objects</title>
  <read_first>
    - buildera-frontend/src/types/service-page.ts (interface contracts — ServicePageData, HeroStat, OutcomeCard, ProcessStep, FaqItem, Technology)
    - buildera-frontend/src/data/services/website-development.ts (current stub — will be replaced)
    - buildera-frontend/src/data/services/index.ts (imports this file — verify export name matches)
    - .planning/phases/04-services-solutions-pages/04-RESEARCH.md (Content Outline — slugs, copy guidance)
    - .planning/phases/04-services-solutions-pages/04-UI-SPEC.md (Service Category Page copy table)
    - .planning/phases/04-services-solutions-pages/04-CONTEXT.md (D-04 copy depth requirement, D-09 canonical slugs)
  </read_first>
  <action>
    Replace the empty stub in `buildera-frontend/src/data/services/website-development.ts` with 4 `ServicePageData` objects. Export as `export const websiteDevelopmentServices: ServicePageData[]`.

    All 4 pages use `categorySlug: "website-development"`. The 4 slugs (from RESEARCH.md Content Outline): `custom-websites`, `ecommerce-websites`, `app-development`, `progressive-web-apps`.

    **Per D-04: Full persuasive marketing copy required.** Not placeholder. Real headlines addressing SMB decision-makers. Pain points they recognize. Outcome cards with specific metrics. FAQ answers that address buying concerns.

    **Page 1: Custom Websites** (`slug: "custom-websites"`)
    - `heroHeadline`: "Custom Websites That Convert Visitors Into Clients"
    - `heroSubheadline`: "Your website is your best salesperson — make sure it works 24/7. We build fast, mobile-first websites tailored to your brand that turn traffic into booked calls."
    - `heroStat`: `{ value: 150, suffix: "+", label: "Projects Delivered" }`
    - `painPoints`: 3 bullets — slow load times losing visitors, outdated design undermining credibility, no clear conversion path for visitors
    - `outcomeCards`: 4 cards — Sub-2-Second Load Time (metric: "3x faster than industry average"), Mobile-First Responsive Design, Built-In Lead Capture Forms (metric: "2-5x more inquiries"), SEO-Ready Architecture
    - `processSteps`: 3 steps — Discovery & Strategy (understand business goals), Design & Development (build iteratively with feedback), Launch & Optimize (deploy, monitor, improve)
    - `industriesServed`: ["manufacturing", "retail", "hospitality", "professional-services"]
    - `relatedSolutions`: ["lead-mgmt", "operations-mgmt", "crm"]
    - `faqItems`: 4 items — "How long does a custom website take?" (4-8 weeks typical), "What technology do you use?" (Next.js, React, Tailwind, PHP/Laravel backend), "Do you handle hosting?" (we configure on your hosting; recommend Hostinger for cost-effectiveness), "Can I update content myself?" (yes — admin panel included)
    - `ctaHeadline`: "Ready for a Website That Actually Converts?"
    - `technologies`: Frontend (React, Next.js, TypeScript, Tailwind), Backend (Laravel, PHP, Node.js), Cloud (AWS), Tools (Stripe, Google Analytics)

    **Page 2: E-Commerce Websites** (`slug: "ecommerce-websites"`)
    - Hero angle: "E-Commerce That Sells While You Sleep"
    - Hero stat: `{ value: 40, suffix: "%", label: "Avg Revenue Increase" }`
    - Pain points: cart abandonment, manual inventory tracking, no mobile shopping experience
    - Outcome cards: Optimized Checkout Flow (metric: "35% fewer abandoned carts"), Real-Time Inventory Sync, Mobile Commerce Ready, Payment Gateway Integration
    - Industries: ["retail", "manufacturing", "hospitality"]
    - Related solutions: ["vendor-mgmt", "warehouse-mgmt", "accounting-mgmt"]
    - Technologies: Frontend (React, Next.js, TypeScript, Tailwind), Backend (Laravel, PHP), Cloud (AWS), Tools (Stripe, Shopify API, Razorpay)

    **Page 3: App Development** (`slug: "app-development"`)
    - Hero angle: "Web Apps That Solve Real Business Problems"
    - Hero stat: `{ value: 98, suffix: "%", label: "Client Satisfaction" }`
    - Pain points: spreadsheets replacing real software, team wasting time on manual processes, off-the-shelf tools not fitting the workflow
    - Outcome cards: Custom Business Logic, Role-Based Access Control, Real-Time Dashboards (metric: "Decisions in minutes, not days"), API Integration Ready
    - Industries: ["manufacturing", "finance", "logistics", "professional-services"]
    - Related solutions: ["project-mgmt", "operations-mgmt", "hr-mgmt"]
    - Technologies: Frontend (React, Next.js, TypeScript), Backend (Laravel, Node.js, PHP, Python), Cloud (AWS, Docker), Tools (n8n, Stripe, Twilio)

    **Page 4: Progressive Web Apps** (`slug: "progressive-web-apps"`)
    - Hero angle: "App-Like Experiences Without the App Store"
    - Hero stat: `{ value: 60, suffix: "%", label: "Lower Dev Cost vs Native" }`
    - Pain points: native app budget out of reach, users won't download another app, separate codebases for iOS and Android
    - Outcome cards: Works Offline (metric: "Zero downtime for field teams"), Instant Loading (service worker caching), Push Notifications, One Codebase — All Platforms
    - Industries: ["retail", "hospitality", "logistics", "manufacturing"]
    - Related solutions: ["fleet-mgmt", "warehouse-mgmt", "operations-mgmt"]
    - Technologies: Frontend (React, Next.js, TypeScript, Tailwind), Cloud (AWS, Cloudflare), Tools (Workbox, Firebase)

    All 4 pages share `heroCta: "Book a Free Call"`, 3 processSteps with the same 3-step framework (Discovery/Build/Launch — customize descriptions per service), and service-specific FAQ content (4 items each).

    **Also ensure** the category page `app/services/[category]/page.tsx` works for `website-development`: it should find 4 sub-services via `getServicesByCategory("website-development")` and render them as cards. The page already exists from 04-01 — no changes needed to the route file itself; the data file being populated is what makes the category page render content.
  </action>
  <acceptance_criteria>
    - `website-development.ts` exports `websiteDevelopmentServices` with exactly 4 objects
    - Each object has all required `ServicePageData` fields filled (no empty strings, no placeholder text)
    - Slugs are exactly: `custom-websites`, `ecommerce-websites`, `app-development`, `progressive-web-apps`
    - All `categorySlug` values are `"website-development"`
    - Each `heroStat` has numeric `value`, string `suffix`, string `label`
    - Each page has exactly 3 `painPoints`, 3-4 `outcomeCards`, 3 `processSteps`, 3-4 `faqItems`
    - `technologies` arrays use valid category values: `'frontend' | 'backend' | 'cloud-devops' | 'tools'`
    - `npm run build` in buildera-frontend/ produces 4 pre-rendered pages at `/services/website-development/*` slugs with 0 errors
    - Marketing copy is persuasive and SMB-focused — not generic "we deliver quality solutions" filler
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data file strings to JSX | Hardcoded TypeScript strings rendered via React auto-escape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | Data file copy | accept | All copy is hardcoded TypeScript — not user input; React escapes automatically |
</threat_model>

<verification>
- `npm run build` in buildera-frontend/ completes with 4 website-development pages pre-rendered
- `npx tsc --noEmit` passes
- Build output shows routes: `/services/website-development/custom-websites`, `/services/website-development/ecommerce-websites`, `/services/website-development/app-development`, `/services/website-development/progressive-web-apps`
</verification>

<success_criteria>
- 4 Website Development sub-service pages pre-rendered at build time
- Full persuasive marketing copy on all pages — not placeholder content
- Category page at `/services/website-development` shows 4 sub-service cards
- No TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/04-services-solutions-pages/04-02-SUMMARY.md` when done
</output>
