---
id: "01-01"
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - buildera-frontend/next.config.ts
  - buildera-frontend/package.json
  - buildera-frontend/src/app/globals.css
  - buildera-frontend/src/app/layout.tsx
  - buildera-frontend/src/app/not-found.tsx
  - buildera-frontend/src/app/error.tsx
  - buildera-frontend/src/lib/api.ts
  - buildera-frontend/src/lib/env.ts
  - buildera-frontend/scripts/postbuild.js
  - buildera-frontend/ecosystem.config.js
  - buildera-frontend/.env.example
  - buildera-frontend/.gitignore
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
  - FOUND-04
  - INFRA-03

must_haves:
  truths:
    - "npm run dev starts the frontend dev server with no TypeScript or module errors"
    - "npm run build completes and produces .next/standalone/server.js"
    - "After build, .next/standalone/public/ and .next/standalone/.next/static/ both exist"
    - "src/lib/api.ts exists and exports a typed ApiError class and a typed fetchFromApi function stub"
    - "src/lib/env.ts validates NEXT_PUBLIC_API_URL at module load time and throws if missing"
    - "not-found.tsx renders a 404 page with a Go Home link pointing to /"
    - "error.tsx is a 'use client' component rendering an error boundary with Go Home link and reset button"
    - ".env.example documents NEXT_PUBLIC_API_URL, NEXT_PUBLIC_API_KEY, and NEXTJS_REVALIDATE_SECRET"
    - "ecosystem.config.js references .next/standalone/server.js as its script target"
  artifacts:
    - path: "buildera-frontend/next.config.ts"
      provides: "Next.js config with output standalone, optimizePackageImports, remotePatterns"
      contains: "output: 'standalone'"
    - path: "buildera-frontend/scripts/postbuild.js"
      provides: "Postbuild copy script that copies public/ and .next/static/ into .next/standalone/"
      contains: "cpSync"
    - path: "buildera-frontend/ecosystem.config.js"
      provides: "pm2 process definition pointing to standalone server.js"
      contains: "standalone/server.js"
    - path: "buildera-frontend/src/lib/api.ts"
      provides: "Typed fetch helper stubs consumed by all page components"
      exports: ["ApiError", "fetchFromApi"]
    - path: "buildera-frontend/src/lib/env.ts"
      provides: "Validated env exports; throws on missing required vars at startup"
      contains: "NEXT_PUBLIC_API_URL"
    - path: "buildera-frontend/src/app/not-found.tsx"
      provides: "Custom 404 page"
    - path: "buildera-frontend/src/app/error.tsx"
      provides: "Custom error boundary — must be Client Component"
  key_links:
    - from: "buildera-frontend/package.json"
      to: "buildera-frontend/scripts/postbuild.js"
      via: "build script — next build && node scripts/postbuild.js"
      pattern: "postbuild"
    - from: "buildera-frontend/src/app/layout.tsx"
      to: "src/lib/env.ts"
      via: "import at module top (runs at build time)"
      pattern: "import.*env"
---

<objective>
Scaffold the buildera-frontend Next.js 15 repository from scratch. This plan runs the create-next-app scaffold, installs all required packages, configures next.config.ts for standalone output and performance, initializes shadcn/ui for Tailwind 4, sets up the Inter font, writes the globals.css @theme structure, creates the api.ts and env.ts lib stubs, writes the postbuild copy script, creates the pm2 ecosystem config, and creates the error page stubs.

Purpose: Every subsequent frontend phase depends on this scaffold being correct. Getting standalone output, Tailwind 4 config, and the api.ts pattern right here prevents rework across all 9 remaining phases.

Output: A runnable buildera-frontend directory at C:\Users\ASUS\Desktop\Buildera\buildera-frontend\ with npm run dev working and npm run build producing a standalone output with static assets correctly copied.
</objective>

<execution_context>
@C:\Users\ASUS\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ASUS\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\ASUS\Desktop\Buildera\.planning\PROJECT.md
@C:\Users\ASUS\Desktop\Buildera\.planning\ROADMAP.md
@C:\Users\ASUS\Desktop\Buildera\.planning\phases\01-project-foundation\01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 repo, install packages, configure next.config.ts</name>
  <read_first>
    - C:\Users\ASUS\Desktop\Buildera\.planning\phases\01-project-foundation\01-RESEARCH.md (sections: Frontend Installation Commands, Pattern 2: Next.js 15 Standalone, Standard Stack table)
    - C:\Users\ASUS\Desktop\Buildera\CLAUDE.md (Technology Stack — Frontend table)
  </read_first>
  <action>
Working directory for all commands: C:\Users\ASUS\Desktop\Buildera\

Step 1 — Scaffold. Run exactly:
  npx create-next-app@15 buildera-frontend --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbopack

This creates buildera-frontend/ with Next.js 15.x. Do NOT use create-next-app@latest (resolves to v16 as of May 2026).

Step 2 — Install packages. From inside buildera-frontend/:
  npm install motion@12.40.0 @tabler/icons-react@3.44.0 clsx tailwind-merge tw-animate-css

Step 3 — Initialize shadcn/ui. From inside buildera-frontend/:
  npx shadcn@latest init
  When prompted: select the "Default" style, use CSS variables yes, Tailwind config path is globals.css (Tailwind 4 mode). Accept all defaults.

Step 4 — Write next.config.ts. Replace the entire file at buildera-frontend/next.config.ts with a NextConfig that sets:
  - output: 'standalone'
  - images.remotePatterns: one entry with protocol 'https', hostname 'api.buildera.co'
  - experimental.optimizePackageImports: ['@tabler/icons-react', 'motion']
  Do not add any other settings. Use TypeScript syntax (import type { NextConfig } from 'next'; export default nextConfig).

Step 5 — Update package.json build script. In buildera-frontend/package.json, change the "build" script from "next build" to "next build && node scripts/postbuild.js". Do not change any other script.

Step 6 — Pin Next.js version. In package.json, change the next dependency from whatever create-next-app set to "^15.0.0" to prevent accidental upgrade to v16.

Step 7 — Add .gitignore entry. Verify buildera-frontend/.gitignore contains .env on its own line (not just .env.local). The create-next-app scaffold includes .env.local but not .env — add .env as a separate line.
  </action>
  <verify>
    <automated>grep -c "standalone" C:\Users\ASUS\Desktop\Buildera\buildera-frontend\next.config.ts</automated>
    <automated>grep "postbuild" C:\Users\ASUS\Desktop\Buildera\buildera-frontend\package.json</automated>
    <automated>grep "@tabler/icons-react" C:\Users\ASUS\Desktop\Buildera\buildera-frontend\package.json</automated>
  </verify>
  <acceptance_criteria>
    - buildera-frontend/package.json has "@tabler/icons-react" in dependencies
    - buildera-frontend/package.json has "motion" in dependencies
    - buildera-frontend/package.json build script contains "node scripts/postbuild.js"
    - buildera-frontend/package.json "next" dependency is "^15.0.0" (not ^16 or latest)
    - buildera-frontend/next.config.ts contains the string "standalone"
    - buildera-frontend/next.config.ts contains the string "optimizePackageImports"
    - buildera-frontend/next.config.ts contains "api.buildera.co" in remotePatterns
    - buildera-frontend/src/components/ui/ directory exists (created by shadcn init)
    - buildera-frontend/.gitignore contains ".env" as a standalone line
  </acceptance_criteria>
  <done>Next.js 15 project scaffolded with all required packages installed, next.config.ts configured for standalone output with optimizePackageImports and remotePatterns, build script updated to run postbuild, Next.js version pinned to ^15.0.0.</done>
</task>

<task type="auto">
  <name>Task 2: Configure globals.css @theme, Inter font, create lib stubs, error pages, postbuild script, ecosystem.config.js, .env.example</name>
  <read_first>
    - C:\Users\ASUS\Desktop\Buildera\buildera-frontend\src\app\globals.css (see current state from shadcn init)
    - C:\Users\ASUS\Desktop\Buildera\buildera-frontend\src\app\layout.tsx (see current state from create-next-app)
    - C:\Users\ASUS\Desktop\Buildera\.planning\phases\01-project-foundation\01-RESEARCH.md (sections: Pattern 1 Tailwind 4 globals.css, Pattern 2 standalone pm2, Code Examples: Next.js env validation and error pages)
  </read_first>
  <action>
All files are relative to C:\Users\ASUS\Desktop\Buildera\buildera-frontend\

globals.css — Replace the entire file. The file must start with:
  @import "tailwindcss";
  @import "tw-animate-css";
  @custom-variant dark (&:where(.dark, .dark *));
  @theme inline { --color-background: var(--background); --color-foreground: var(--foreground); --color-primary: var(--primary); --color-primary-foreground: var(--primary-foreground); --color-secondary: var(--secondary); --color-secondary-foreground: var(--secondary-foreground); --color-muted: var(--muted); --color-muted-foreground: var(--muted-foreground); --color-card: var(--card); --color-card-foreground: var(--card-foreground); --color-border: var(--border); --color-input: var(--input); --color-ring: var(--ring); --radius-sm: calc(var(--radius) - 4px); --radius-md: calc(var(--radius) - 2px); --radius-lg: var(--radius); --radius-xl: calc(var(--radius) + 4px); }
  Then the :root block with shadcn/ui CSS variables. Merge any CSS variables shadcn init already wrote — do not delete them. Add these brand tokens inside :root:
    --primary: hsl(217 91% 60%);
    --primary-foreground: hsl(0 0% 100%);
  Do NOT include a tailwind.config.js reference or require() call anywhere.

layout.tsx — Update the root layout to:
  - Import Inter from 'next/font/google' with subsets: ['latin'], variable: '--font-inter', display: 'swap'
  - Apply inter.variable to the html element's className alongside the existing antialiased class
  - Import './globals.css'
  - Keep the existing metadata export (title/description can remain as-is for now; Phase 3 refines them)
  - Add import of src/lib/env.ts at the top of the file (bare import — no named imports needed; the module validates env on load)

src/lib/env.ts — Create this file. It must:
  - Validate that process.env.NEXT_PUBLIC_API_URL is defined and non-empty; throw Error('Missing required environment variable: NEXT_PUBLIC_API_URL') if not
  - Export a const env object with: apiUrl (string, from NEXT_PUBLIC_API_URL), apiKey (string, from NEXT_PUBLIC_API_KEY with empty string fallback), revalidateSecret (string, from NEXTJS_REVALIDATE_SECRET with empty string fallback)

src/lib/api.ts — Create this file. It must:
  - Export class ApiError extends Error with properties: status (number), message (string). Constructor takes (status: number, message: string).
  - Export async function fetchFromApi<T>(path: string, options?: RequestInit): Promise<T> — this is a stub. Implement the function body as: fetch from `${process.env.NEXT_PUBLIC_API_URL}${path}` with the given options, check response.ok and throw new ApiError(response.status, response.statusText) if not ok, return response.json() as T. Add no other fetch helpers — those come in Phase 2.

src/lib/utils.ts — If shadcn init did not create this file, create it with: import { clsx } from 'clsx'; import { twMerge } from 'tailwind-merge'; export function cn(...inputs: any[]) { return twMerge(clsx(inputs)); }. If it already exists, leave it untouched.

src/app/not-found.tsx — Create this file as a Server Component (no 'use client'). It must render an h1 "404 — Page Not Found", a paragraph "The page you're looking for doesn't exist.", and a Link from 'next/link' pointing to "/" with text "Go Home". Apply brand-appropriate classes: center the content vertically and horizontally, use text-primary for the heading color. No external dependencies beyond next/link.

src/app/error.tsx — Create this file. It MUST have 'use client' as the very first line. It accepts props { error: Error & { digest?: string }; reset: () => void }. It must render an h2 "Something went wrong", a paragraph showing error.message, a button with onClick={() => reset()} labeled "Try again", and a Link from 'next/link' to "/" labeled "Go Home". Apply brand-appropriate classes matching not-found.tsx styling.

scripts/postbuild.js — Create this file (create scripts/ directory if needed). It must:
  - Use Node.js built-in 'fs' module (no external deps): const fs = require('fs')
  - Copy public/ into .next/standalone/public using fs.cpSync('public', '.next/standalone/public', { recursive: true })
  - Copy .next/static/ into .next/standalone/.next/static using fs.cpSync('.next/static', '.next/standalone/.next/static', { recursive: true })
  - Log "Postbuild: copied public/ and .next/static/ to .next/standalone/" on completion

ecosystem.config.js — Create in the root of buildera-frontend/. It must use module.exports = { apps: [{ name: 'buildera-frontend', script: '.next/standalone/server.js', env_production: { NODE_ENV: 'production', PORT: 3000, HOSTNAME: '0.0.0.0' } }] }

.env.example — Create in the root of buildera-frontend/. Include exactly these entries with inline comments explaining each:
  NEXT_PUBLIC_API_URL=https://api.buildera.co       # Backend API base URL (no trailing slash). Use http://localhost:8000 for local dev.
  NEXT_PUBLIC_API_KEY=your-api-key-here              # API key for POST /api/leads. Get from backend .env APP_API_KEY.
  NEXTJS_REVALIDATE_SECRET=your-revalidate-secret   # HMAC secret for ISR cache purge. Must match NEXTJS_REVALIDATE_SECRET in backend .env.
  </action>
  <verify>
    <automated>node -e "require('fs').existsSync('C:/Users/ASUS/Desktop/Buildera/buildera-frontend/src/lib/api.ts') && require('fs').existsSync('C:/Users/ASUS/Desktop/Buildera/buildera-frontend/src/lib/env.ts') && require('fs').existsSync('C:/Users/ASUS/Desktop/Buildera/buildera-frontend/scripts/postbuild.js') && require('fs').existsSync('C:/Users/ASUS/Desktop/Buildera/buildera-frontend/ecosystem.config.js') && require('fs').existsSync('C:/Users/ASUS/Desktop/Buildera/buildera-frontend/.env.example') && require('fs').existsSync('C:/Users/ASUS/Desktop/Buildera/buildera-frontend/src/app/not-found.tsx') && require('fs').existsSync('C:/Users/ASUS/Desktop/Buildera/buildera-frontend/src/app/error.tsx') ? console.log('all files exist') : process.exit(1)"</automated>
    <automated>grep -c "use client" C:\Users\ASUS\Desktop\Buildera\buildera-frontend\src\app\error.tsx</automated>
    <automated>grep -c "standalone" C:\Users\ASUS\Desktop\Buildera\buildera-frontend\ecosystem.config.js</automated>
    <automated>grep -c "@import \"tailwindcss\"" C:\Users\ASUS\Desktop\Buildera\buildera-frontend\src\app\globals.css</automated>
    <automated>grep -c "NEXT_PUBLIC_API_URL" C:\Users\ASUS\Desktop\Buildera\buildera-frontend\.env.example</automated>
    <automated>cd C:\Users\ASUS\Desktop\Buildera\buildera-frontend && npm run dev -- --port 3000 &amp; sleep 8 &amp;&amp; curl -s http://localhost:3000 | head -5; kill %1 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css first line is "@import \"tailwindcss\";"
    - src/app/globals.css contains "@theme inline" block
    - src/app/globals.css does NOT contain "require(" or "tailwind.config"
    - src/app/layout.tsx imports Inter from 'next/font/google'
    - src/app/layout.tsx imports from './globals.css'
    - src/app/layout.tsx imports from '@/lib/env' (bare import)
    - src/lib/env.ts exports const env with apiUrl, apiKey, revalidateSecret keys
    - src/lib/env.ts throws Error when NEXT_PUBLIC_API_URL is missing (validated by throw statement in source)
    - src/lib/api.ts exports class ApiError and async function fetchFromApi
    - src/app/not-found.tsx does NOT have 'use client' on line 1
    - src/app/error.tsx has 'use client' as first line
    - src/app/error.tsx contains reset() call in a button onClick handler
    - scripts/postbuild.js contains both cpSync calls for public/ and .next/static/
    - ecosystem.config.js contains '.next/standalone/server.js' as script value
    - .env.example contains NEXT_PUBLIC_API_URL, NEXT_PUBLIC_API_KEY, NEXTJS_REVALIDATE_SECRET entries
    - npm run dev starts without compilation errors (TypeScript strict mode passes)
  </acceptance_criteria>
  <done>All lib stubs, error pages, globals.css @theme structure, Inter font, postbuild script, pm2 ecosystem config, and .env.example are written and correct. npm run dev serves the app with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env file → build process | .env vars are embedded at build time into NEXT_PUBLIC_* values; invalid values cause build failure, not runtime crash |
| standalone server → filesystem | server.js reads public/ and .next/static/ from its own directory; missing files cause 404s not crashes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env / .env.example | mitigate | .gitignore must include .env (not just .env.local); .env.example must use placeholder values only — no real keys |
| T-01-02 | Tampering | package.json next version | mitigate | Pin "next": "^15.0.0" — prevents npm update from pulling v16 which has breaking App Router changes |
| T-01-03 | Denial of Service | missing postbuild copy | accept | Handled by scripts/postbuild.js; missing static assets cause 404 on CSS/JS but not security risk |
| T-01-SC | Tampering | npm package installs | mitigate | All packages in Package Legitimacy Audit table in 01-RESEARCH.md marked Approved; no [ASSUMED] or [SUS] packages |
</threat_model>

<verification>
After both tasks complete, run in order from C:\Users\ASUS\Desktop\Buildera\buildera-frontend\:

1. npm run dev — dev server must start with no TypeScript errors
2. NEXT_PUBLIC_API_URL=https://api.buildera.co npm run build — build must exit 0 and produce .next/standalone/server.js
3. After build: verify .next/standalone/public/ exists (postbuild copy ran)
4. After build: verify .next/standalone/.next/static/ exists (postbuild copy ran)
5. node -e "require('./ecosystem.config.js')" — must not throw
</verification>

<success_criteria>
1. npm run dev starts on localhost:3000 with no errors or TypeScript warnings
2. npm run build (with NEXT_PUBLIC_API_URL set) exits 0
3. .next/standalone/server.js exists after build
4. .next/standalone/public/ and .next/standalone/.next/static/ both exist after build (postbuild ran)
5. .env.example documents all three required frontend env vars
6. next.config.ts has output: 'standalone', optimizePackageImports for @tabler/icons-react and motion, and remotePatterns for api.buildera.co
7. src/app/error.tsx has 'use client' as first line
</success_criteria>

<output>
Create .planning/phases/01-project-foundation/01-01-SUMMARY.md when done
</output>
