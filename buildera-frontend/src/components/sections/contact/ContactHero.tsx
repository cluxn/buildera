import { PageHero } from '@/components/sections/PageHero'

export function ContactHero() {
  return (
    <PageHero
      eyebrow="Contact Us"
      heading="Start Your Project Today"
      description="Tell us what you're building. We'll respond within 4 business hours with a clear next step — no generic proposal, no sales pitch."
      ctaLabel="Send a Message"
      ctaHref="#contact-form"
    />
  )
}
